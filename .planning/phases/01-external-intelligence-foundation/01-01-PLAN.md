---
phase: 01-external-intelligence-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/shared/external_intelligence/SOURCES.yaml
  - assets/shared/external_intelligence/LATEST_SUMMARY.md
  - docs/STATE_CONTRACT.md
autonomous: true
requirements:
  - SIGN-01
  - SIGN-02
  - ANLY-01
user_setup: []
must_haves:
  truths:
    - The repo defines a discovery contract for collecting pain-point signals from across the public web instead of assuming one fixed fact source.
    - The repo contains a documented normalized signal schema and storage layout for raw captures, run logs, and signal history.
    - Shared-state governance explicitly describes how external intelligence artifacts are written and read.
  artifacts:
    - assets/shared/external_intelligence/SOURCES.yaml
    - assets/shared/external_intelligence/LATEST_SUMMARY.md
    - docs/STATE_CONTRACT.md
  key_links:
    - docs/STATE_CONTRACT.md authorizes external intelligence artifacts under assets/shared/external_intelligence/ without allowing direct ledger writes.
    - SOURCES.yaml must act as a discovery seed/config contract, not a single approved source-of-truth list.
---

<objective>
Define the web-wide pain-signal discovery artifact model for Phase 1 so later plans can implement collection against a stable, reviewable contract.

Purpose: Phase 1 must stabilize discovery configuration, normalized fields, and storage locations before adding live collection logic.
Output: A checked-in discovery seed/config registry, an operator-facing latest-summary template, and state-contract updates for the new artifact family.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/phases/01-external-intelligence-foundation/01-RESEARCH.md
@.planning/phases/01-external-intelligence-foundation/01-PATTERNS.md
@docs/STATE_CONTRACT.md
@assets/shared/TEMPLATES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the discovery seed/config registry for pain-signal intake</name>
  <files>assets/shared/external_intelligence/SOURCES.yaml</files>
  <read_first>assets/shared/external_intelligence/SOURCES.yaml, assets/shared/TEMPLATES.md, docs/STATE_CONTRACT.md</read_first>
  <action>Create or update `assets/shared/external_intelligence/SOURCES.yaml` so it serves as a discovery seed/config registry rather than a canonical approved-source list. Preserve existing starter IDs where helpful, but document that they are optional seed inputs, search surfaces, or collection hints for web-wide discovery. Keep support for complaint, trend, and competitor categories, but avoid wording that implies all relevant truth must come from a fixed enumerated list. For each entry, keep the keys `id`, `category`, `kind`, `display_name`, `base_url`, `collection_method`, `enabled`, `poll_window_hours`, `dedupe_key_fields`, and `notes` where they remain useful.</action>
  <verify>Read `assets/shared/external_intelligence/SOURCES.yaml` and confirm it no longer implies Phase 1 depends on one fixed approved fact source list.</verify>
  <acceptance_criteria>
    - `assets/shared/external_intelligence/SOURCES.yaml` remains a valid checked-in config artifact
    - The file preserves complaint/trend/competitor categories as supported discovery dimensions
    - The file is described as seed/config input for discovery, not the sole source of truth
  </acceptance_criteria>
  <done>The repo has a discovery-oriented config contract instead of a fixed-source assumption.</done>
</task>

<task type="auto">
  <name>Task 2: Document the normalized signal record and latest-summary structure</name>
  <files>assets/shared/external_intelligence/LATEST_SUMMARY.md</files>
  <read_first>assets/shared/external_intelligence/LATEST_SUMMARY.md, assets/shared/TEMPLATES.md, .planning/phases/01-external-intelligence-foundation/01-RESEARCH.md</read_first>
  <action>Create or update `assets/shared/external_intelligence/LATEST_SUMMARY.md` as the operator-facing template for the newest collection run. Include a `## Run Metadata` section with placeholders for `run_id`, `started_at`, `completed_at`, `new_signal_count`, `duplicate_signal_count`, and `failed_source_count`. Add a `## Normalized Signal Schema` section that enumerates the exact normalized fields `signal_id`, `source_id`, `source_category`, `collected_at`, `published_at`, `title`, `summary`, `url`, `evidence_text`, `entities`, `tags`, `language`, `confidence`, `raw_artifact_path`, and `normalization_version`. Keep separate markdown sections titled `## Trend Signals`, `## Competitor Signals`, and `## Complaint Signals`, but treat them as buckets of discovered evidence rather than a claim that all discovery must begin from fixed sites.</action>
  <verify>Read `assets/shared/external_intelligence/LATEST_SUMMARY.md` and confirm all required section titles and normalized field names are present.</verify>
  <acceptance_criteria>
    - `assets/shared/external_intelligence/LATEST_SUMMARY.md` contains `## Run Metadata`
    - `assets/shared/external_intelligence/LATEST_SUMMARY.md` contains `## Normalized Signal Schema`
    - `assets/shared/external_intelligence/LATEST_SUMMARY.md` contains `signal_id`, `source_id`, `source_category`, and `raw_artifact_path`
    - `assets/shared/external_intelligence/LATEST_SUMMARY.md` retains complaint/trend/competitor sections as reporting buckets
  </acceptance_criteria>
  <done>The repo has a concrete normalized record contract and a human-readable summary artifact shape for discovered signals.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the state contract for external intelligence artifacts</name>
  <files>docs/STATE_CONTRACT.md</files>
  <read_first>docs/STATE_CONTRACT.md, docs/MULTI_PROFILE_COORDINATION.md, .planning/phases/01-external-intelligence-foundation/01-RESEARCH.md</read_first>
  <action>Update `docs/STATE_CONTRACT.md` to add `assets/shared/external_intelligence/` as a governed shared-artifact family. Explicitly document that raw captures, signal history, run logs, and latest summaries live under that directory; collection writes are performed by the external-intelligence collector workflow; all profiles may read these artifacts; and this addition does not change the existing rule that `assets/shared/LEDGER.json` may only be written through `python3 assets/shared/manage_finance.py ...`.</action>
  <verify>Read docs/STATE_CONTRACT.md and confirm it mentions `assets/shared/external_intelligence/` plus the unchanged `manage_finance.py` ledger rule.</verify>
  <acceptance_criteria>
    - `docs/STATE_CONTRACT.md` contains `assets/shared/external_intelligence/`
    - `docs/STATE_CONTRACT.md` describes raw captures, signal history, run logs, or latest summary as artifacts under that directory
    - `docs/STATE_CONTRACT.md` still contains `python3 assets/shared/manage_finance.py`
    - `docs/STATE_CONTRACT.md` states that profiles can read the external intelligence artifacts
  </acceptance_criteria>
  <done>The shared-state contract now covers external-intelligence storage without weakening existing governance rules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Read `assets/shared/external_intelligence/SOURCES.yaml` and confirm it expresses discovery seeds/config rather than a fixed source-of-truth list.
- [ ] Read `assets/shared/external_intelligence/LATEST_SUMMARY.md` and confirm the normalized schema fields and category sections are present.
- [ ] Read `docs/STATE_CONTRACT.md` and confirm it references `assets/shared/external_intelligence/` while preserving the `manage_finance.py` ledger-write rule.
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- The repo has a concrete discovery config, normalized schema, and storage contract for Phase 1
- Phase 2 implementation work can refer to exact file paths and field names instead of inferring them
</success_criteria>

<output>
After completion, create `.planning/phases/01-external-intelligence-foundation/01-01-SUMMARY.md`
</output>
