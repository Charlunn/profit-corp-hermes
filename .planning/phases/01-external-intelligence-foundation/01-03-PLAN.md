---
phase: 01-external-intelligence-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - scripts/run_external_intelligence.sh
  - orchestration/cron/external_intelligence.prompt.md
  - orchestration/cron/commands.sh
  - orchestration/cron/daily_pipeline.prompt.md
  - scripts/smoke_test_pipeline.sh
autonomous: true
requirements:
  - SIGN-01
  - SIGN-02
  - ANLY-01
user_setup: []
must_haves:
  truths:
    - The operator has a stable command path to run web-wide pain-signal discovery outside ad-hoc manual steps.
    - Cron/orchestration artifacts can invoke the collector as part of the repo's Hermes operating rhythm.
    - Smoke checks validate the new intake flow and fail loudly when key intake artifacts or commands are broken.
  artifacts:
    - scripts/run_external_intelligence.sh
    - orchestration/cron/external_intelligence.prompt.md
    - orchestration/cron/commands.sh
    - orchestration/cron/daily_pipeline.prompt.md
    - scripts/smoke_test_pipeline.sh
  key_links:
    - scripts/run_external_intelligence.sh calls scripts/collect_external_signals.py.
    - orchestration/cron/commands.sh exposes a named action for the intake flow.
    - scripts/smoke_test_pipeline.sh verifies the collector and orchestration wiring.
---

<objective>
Persist the web-wide pain-signal discovery flow into the repo's daily operating rhythm so the operator can run it reliably and verify it with existing smoke tooling.

Purpose: A collector script alone is insufficient; Phase 1 succeeds only when discovery is runnable, schedulable, and validated through the repo's established operational paths.
Output: A stable shell wrapper, cron prompt wiring, command-hub integration, and smoke checks for web-wide pain-signal collection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-external-intelligence-foundation/01-RESEARCH.md
@.planning/phases/01-external-intelligence-foundation/01-PATTERNS.md
@.planning/phases/01-external-intelligence-foundation/01-02-PLAN.md
@orchestration/cron/commands.sh
@orchestration/cron/daily_pipeline.prompt.md
@scripts/smoke_test_pipeline.sh
@scripts/collect_external_signals.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a stable operator entrypoint for web-wide discovery runs</name>
  <files>scripts/run_external_intelligence.sh</files>
  <read_first>scripts/smoke_test_pipeline.sh, scripts/run_external_intelligence.sh, scripts/collect_external_signals.py, orchestration/cron/commands.sh</read_first>
  <action>Create `scripts/run_external_intelligence.sh` as a bash wrapper that resolves the repo root like the existing shell scripts, defaults to `python3` but falls back to `python`, and runs `scripts/collect_external_signals.py --window-hours 24`. Support passthrough CLI arguments so operators can run `bash scripts/run_external_intelligence.sh --dry-run` and `bash scripts/run_external_intelligence.sh --source-id <id>`. Fail fast with `set -euo pipefail`.</action>
  <verify>Read `scripts/run_external_intelligence.sh` and confirm it contains `set -euo pipefail`, the path `scripts/collect_external_signals.py`, and passthrough of script arguments.</verify>
  <acceptance_criteria>
    - `scripts/run_external_intelligence.sh` contains `set -euo pipefail`
    - `scripts/run_external_intelligence.sh` contains `collect_external_signals.py`
    - `scripts/run_external_intelligence.sh` contains `--window-hours 24`
    - `scripts/run_external_intelligence.sh` passes through additional arguments with `"$@"` or equivalent
  </acceptance_criteria>
  <done>The operator has a stable shell command for dry runs and live discovery runs.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the discovery flow into cron artifacts and command helpers</name>
  <files>orchestration/cron/external_intelligence.prompt.md, orchestration/cron/commands.sh, orchestration/cron/daily_pipeline.prompt.md</files>
  <read_first>orchestration/cron/external_intelligence.prompt.md, orchestration/cron/daily_pipeline.prompt.md, orchestration/cron/commands.sh, scripts/run_external_intelligence.sh, docs/MULTI_PROFILE_COORDINATION.md</read_first>
  <action>Create `orchestration/cron/external_intelligence.prompt.md` with a concise CEO-facing instruction to run `bash scripts/run_external_intelligence.sh`, summarize `assets/shared/external_intelligence/LATEST_SUMMARY.md`, and surface failed discovery paths. Update `orchestration/cron/commands.sh` to expose a named action `run-intelligence` that invokes the new prompt or wrapper consistently with the existing command-hub style. Update `orchestration/cron/daily_pipeline.prompt.md` so the daily rhythm begins by reading the external-intelligence latest summary or explicitly invoking the intake step before Scout analysis, without removing the existing downstream Scout → CMO → Architect → CEO → Accountant sequence.</action>
  <verify>Read the three files and confirm the exact string `run-intelligence` exists, the new prompt references `bash scripts/run_external_intelligence.sh`, and `daily_pipeline.prompt.md` references `assets/shared/external_intelligence/LATEST_SUMMARY.md` or the intake step before Scout output.</verify>
  <acceptance_criteria>
    - `orchestration/cron/external_intelligence.prompt.md` exists
    - `orchestration/cron/external_intelligence.prompt.md` contains `bash scripts/run_external_intelligence.sh`
    - `orchestration/cron/commands.sh` contains `run-intelligence`
    - `orchestration/cron/daily_pipeline.prompt.md` contains `assets/shared/external_intelligence/LATEST_SUMMARY.md` or `run_external_intelligence.sh`
    - `orchestration/cron/daily_pipeline.prompt.md` still contains the existing accountant audit step `python3 assets/shared/manage_finance.py audit`
  </acceptance_criteria>
  <done>The discovery workflow is callable through the repo's cron-oriented operating surface and connected to the existing daily loop.</done>
</task>

<task type="auto">
  <name>Task 3: Extend smoke coverage for the discovery flow</name>
  <files>scripts/smoke_test_pipeline.sh</files>
  <read_first>scripts/smoke_test_pipeline.sh, scripts/run_external_intelligence.sh, scripts/collect_external_signals.py, orchestration/cron/commands.sh</read_first>
  <action>Update `scripts/smoke_test_pipeline.sh` to check that `assets/shared/external_intelligence/SOURCES.yaml`, `scripts/collect_external_signals.py`, `scripts/run_external_intelligence.sh`, and `orchestration/cron/external_intelligence.prompt.md` all exist and are non-empty. Add a syntax check for `scripts/collect_external_signals.py` using the resolved Python binary, add a dry-run style command such as `bash "$ROOT_DIR/scripts/run_external_intelligence.sh" --dry-run`, and add a command-hub check that `bash "$ROOT_DIR/orchestration/cron/commands.sh" run-intelligence` is a recognized action path or can be safely exercised in dry-run/no-op mode.</action>
  <verify>Read `scripts/smoke_test_pipeline.sh` and confirm it contains the new external-intelligence file checks, Python compile check for the collector, and command references to `run_external_intelligence.sh --dry-run` and `run-intelligence`.</verify>
  <acceptance_criteria>
    - `scripts/smoke_test_pipeline.sh` contains `assets/shared/external_intelligence/SOURCES.yaml`
    - `scripts/smoke_test_pipeline.sh` contains `scripts/collect_external_signals.py`
    - `scripts/smoke_test_pipeline.sh` contains `scripts/run_external_intelligence.sh`
    - `scripts/smoke_test_pipeline.sh` contains `external_intelligence.prompt.md`
    - `scripts/smoke_test_pipeline.sh` contains `--dry-run`
    - `scripts/smoke_test_pipeline.sh` contains `run-intelligence`
  </acceptance_criteria>
  <done>The repo's standard smoke test now catches missing or broken discovery wiring.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Read `scripts/run_external_intelligence.sh` and confirm the wrapper path, default `--window-hours 24`, and argument passthrough.
- [ ] Read `orchestration/cron/external_intelligence.prompt.md`, `orchestration/cron/commands.sh`, and `orchestration/cron/daily_pipeline.prompt.md` to confirm discovery wiring and preserved accountant audit step.
- [ ] Read `scripts/smoke_test_pipeline.sh` and confirm external-intelligence file checks plus `--dry-run` coverage are present.
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- The operator can run web-wide pain-signal discovery through a stable repo command path
- The daily operating rhythm and smoke tests both recognize the new discovery flow
</success_criteria>

<output>
After completion, create `.planning/phases/01-external-intelligence-foundation/01-03-SUMMARY.md`
</output>
