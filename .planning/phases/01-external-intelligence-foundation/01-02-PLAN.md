---
phase: 01-external-intelligence-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scripts/collect_external_signals.py
  - assets/shared/external_intelligence/history/.gitkeep
  - assets/shared/external_intelligence/raw/.gitkeep
autonomous: true
requirements:
  - SIGN-01
  - SIGN-02
  - ANLY-01
user_setup: []
must_haves:
  truths:
    - The repo includes a deterministic collector that can discover signals from flexible public web inputs instead of only a fixed curated source list.
    - The collector can ingest pain-point discovery inputs and optionally enrich them with trend or competitor support sources.
    - Normalized records include stable IDs, evidence attribution, timestamps, and category metadata.
  artifacts:
    - scripts/collect_external_signals.py
    - assets/shared/external_intelligence/history/.gitkeep
    - assets/shared/external_intelligence/raw/.gitkeep
  key_links:
    - scripts/collect_external_signals.py reads assets/shared/external_intelligence/SOURCES.yaml as discovery config and writes under assets/shared/external_intelligence/history/ and raw/.
    - The collector uses the normalized schema defined in assets/shared/external_intelligence/LATEST_SUMMARY.md.
---

<objective>
Implement the deterministic pain-signal collector that converts flexible public web inputs into normalized machine-readable records.

Purpose: Phase 1 needs a real collector, not just a schema, so the operator can discover public pain-point signals repeatably and optionally enrich them with trend or competitor research later.
Output: A Python collector that reads the discovery config, fetches or scans public inputs, normalizes items into the agreed structure, and prepares raw/history storage directories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-external-intelligence-foundation/01-RESEARCH.md
@.planning/phases/01-external-intelligence-foundation/01-PATTERNS.md
@.planning/phases/01-external-intelligence-foundation/01-01-PLAN.md
@assets/shared/external_intelligence/SOURCES.yaml
@assets/shared/external_intelligence/LATEST_SUMMARY.md
@assets/shared/manage_finance.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the discovery-config-driven collector entrypoint</name>
  <files>scripts/collect_external_signals.py</files>
  <read_first>scripts/collect_external_signals.py, assets/shared/external_intelligence/SOURCES.yaml, assets/shared/external_intelligence/LATEST_SUMMARY.md, assets/shared/manage_finance.py</read_first>
  <action>Create `scripts/collect_external_signals.py` as a Python 3 script that resolves repo-relative paths for `assets/shared/external_intelligence/SOURCES.yaml`, `assets/shared/external_intelligence/history/signals.jsonl`, `assets/shared/external_intelligence/history/runs.jsonl`, and `assets/shared/external_intelligence/raw/`. Implement CLI flags `--window-hours`, `--dry-run`, and `--source-id`. Load `SOURCES.yaml`, iterate enabled discovery seeds or configured inputs, and optionally filter to one source when `--source-id` is provided. Use only deterministic standard-library or already-available dependencies; do not add browser automation or authenticated scraping. Return non-zero on malformed discovery definitions.</action>
  <verify>Run `python3 -m py_compile scripts/collect_external_signals.py` and read the file to confirm the exact flags `--window-hours`, `--dry-run`, and `--source-id` exist.</verify>
  <acceptance_criteria>
    - `scripts/collect_external_signals.py` contains `--window-hours`
    - `scripts/collect_external_signals.py` contains `--dry-run`
    - `scripts/collect_external_signals.py` contains `--source-id`
    - `scripts/collect_external_signals.py` references `SOURCES.yaml`, `signals.jsonl`, `runs.jsonl`, and `raw`
    - `python3 -m py_compile scripts/collect_external_signals.py` exits 0
  </acceptance_criteria>
  <done>The repo has a working collector entrypoint that can be invoked deterministically from scripts or cron.</done>
</task>

<task type="auto">
  <name>Task 2: Implement normalization and evidence-preserving persistence</name>
  <files>scripts/collect_external_signals.py, assets/shared/external_intelligence/history/.gitkeep, assets/shared/external_intelligence/raw/.gitkeep</files>
  <read_first>scripts/collect_external_signals.py, assets/shared/external_intelligence/LATEST_SUMMARY.md</read_first>
  <action>Inside `scripts/collect_external_signals.py`, implement normalized records with the exact fields `signal_id`, `source_id`, `source_category`, `collected_at`, `published_at`, `title`, `summary`, `url`, `evidence_text`, `entities`, `tags`, `language`, `confidence`, `raw_artifact_path`, and `normalization_version`. Derive `signal_id` deterministically from canonical URL, title, and published timestamp. Persist raw fetch payloads under `assets/shared/external_intelligence/raw/<source_id>/...json`, append normalized records to `assets/shared/external_intelligence/history/signals.jsonl`, and append run metadata to `assets/shared/external_intelligence/history/runs.jsonl`. Add `.gitkeep` files to `history/` and `raw/` so the directory structure is committed even before first live run.</action>
  <verify>Read `scripts/collect_external_signals.py` and confirm all normalized field names and output file paths are present. Confirm `.gitkeep` exists in both history and raw directories.</verify>
  <acceptance_criteria>
    - `scripts/collect_external_signals.py` contains `signal_id`, `source_category`, `raw_artifact_path`, and `normalization_version`
    - `scripts/collect_external_signals.py` contains `signals.jsonl`
    - `scripts/collect_external_signals.py` contains `runs.jsonl`
    - `scripts/collect_external_signals.py` contains `raw/`
    - `assets/shared/external_intelligence/history/.gitkeep` exists
    - `assets/shared/external_intelligence/raw/.gitkeep` exists
  </acceptance_criteria>
  <done>Normalized machine-readable history and raw evidence persistence are implemented and tied to the agreed schema.</done>
</task>

<task type="auto">
  <name>Task 3: Support flexible public discovery adapters and dedupe-safe reruns</name>
  <files>scripts/collect_external_signals.py</files>
  <read_first>scripts/collect_external_signals.py, assets/shared/external_intelligence/SOURCES.yaml, .planning/phases/01-external-intelligence-foundation/01-RESEARCH.md</read_first>
  <action>Implement source handlers for at least `rss` and `page` kinds so the discovery config can represent public pain-point seeds plus optional trend and competitor supports without browser automation. Preserve room for later search-oriented discovery expansion. For `rss`, parse feed items with title, link, and published timestamp. For `page`, capture the page title, canonical URL if present, fetched timestamp, and visible text excerpt for evidence. When appending to `signals.jsonl`, skip duplicate `signal_id` values already present and record duplicate counts in `runs.jsonl`. Preserve raw payload files even when every normalized signal for a run deduplicates out.</action>
  <verify>Read `scripts/collect_external_signals.py` and confirm it contains explicit branches for `rss` and `page`, duplicate detection against `signal_id`, and run metadata that records duplicate counts.</verify>
  <acceptance_criteria>
    - `scripts/collect_external_signals.py` contains the exact source-kind strings `rss` and `page`
    - `scripts/collect_external_signals.py` contains duplicate handling keyed by `signal_id`
    - `scripts/collect_external_signals.py` contains a run metadata field or key named `duplicate_signal_count`
    - `scripts/collect_external_signals.py` writes raw payloads before or regardless of dedupe outcome
  </acceptance_criteria>
  <done>The collector can ingest deterministic public web formats and rerun safely without duplicating history.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m py_compile scripts/collect_external_signals.py`
- [ ] Read `scripts/collect_external_signals.py` and confirm the exact CLI flags, normalized field names, source-kind branches, and history file paths are present.
- [ ] Confirm `assets/shared/external_intelligence/history/.gitkeep` and `assets/shared/external_intelligence/raw/.gitkeep` exist.
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- The repo has a deterministic collector that transforms discovered public web inputs into normalized records with raw evidence preservation
- The collector is safe to rerun and records duplicate/run metadata for later analysis
</success_criteria>

<output>
After completion, create `.planning/phases/01-external-intelligence-foundation/01-02-SUMMARY.md`
</output>
