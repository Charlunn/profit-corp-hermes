---
phase: 14-github-auth-and-repository-target-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase11_github_sync.py
  - tests/test_project_delivery_pipeline_bootstrap.py
  - tests/test_project_delivery_pipeline_resume.py
  - scripts/github_delivery_common.py
  - scripts/start_approved_project_delivery.py
autonomous: true
requirements:
  - GHAUTH-01
  - GHAUTH-02
  - GHOWN-01
  - GHOWN-02
must_haves:
  truths:
    - "Approved-project delivery accepts either explicit GH_TOKEN/GITHUB_TOKEN or a healthy authenticated gh CLI session as valid GitHub auth input."
    - "Repository owner/name/url resolution is deterministic for both fresh-run and resume-run flows, and owner never falls back to project_slug."
    - "Authority metadata and evidence artifacts clearly show the resolved repository identity and the auth path used for repository preparation."
  artifacts:
    - path: "tests/test_phase11_github_sync.py"
      provides: "Regression coverage for env-token auth, authenticated gh CLI auth, and GitHub helper blocked-state behavior."
    - path: "tests/test_project_delivery_pipeline_bootstrap.py"
      provides: "Fresh-run authority metadata coverage for owner/name/url derivation and repository-preparation stage behavior."
    - path: "tests/test_project_delivery_pipeline_resume.py"
      provides: "Resume-run coverage proving retries preserve or converge GitHub metadata correctly without reintroducing bad defaults."
    - path: "scripts/github_delivery_common.py"
      provides: "GitHub helper auth-source resolution and repository-prepare evidence payload updates."
    - path: "scripts/start_approved_project_delivery.py"
      provides: "Controller-side canonical owner/name/url derivation and shipping metadata persistence."
  key_links:
    - from: "tests/test_phase11_github_sync.py"
      to: "scripts/github_delivery_common.py"
      via: "helper-level assertions for auth-source resolution and blocked results"
      pattern: "prepare_github_repository|missing_github_auth"
    - from: "tests/test_project_delivery_pipeline_bootstrap.py"
      to: "scripts/start_approved_project_delivery.py"
      via: "fresh-run authority record assertions for shipping.github fields"
      pattern: "repository_owner|repository_name|repository_url"
    - from: "tests/test_project_delivery_pipeline_resume.py"
      to: "scripts/start_approved_project_delivery.py"
      via: "resume-path assertions that preserve or repair GitHub metadata"
      pattern: "resume_from_stage|shipping.github"
---

<objective>
Repair the GitHub entry contract for approved-project delivery so repository preparation works in the real operator environment without manual token export and without invalid owner fallback.

Purpose: Fix the two upstream failures exposed by the live Hermes instance test before touching downstream sync/deploy reliability.
Output: Tested controller/helper changes that resolve GitHub auth source detection and canonical repository targeting for fresh and resumed runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-github-auth-and-repository-target-resolution/14-RESEARCH.md
@.planning/phases/14-github-auth-and-repository-target-resolution/14-PATTERNS.md
@scripts/github_delivery_common.py
@scripts/start_approved_project_delivery.py
@tests/test_phase11_github_sync.py
@tests/test_project_delivery_pipeline_bootstrap.py
@tests/test_project_delivery_pipeline_resume.py

<interfaces>
From scripts/github_delivery_common.py:
```python
def _has_github_auth(env: Mapping[str, str] | None = None) -> bool: ...
def _require_gh(workspace: Path, *, which: Which | None = None, env: Mapping[str, str] | None = None) -> dict[str, Any] | None: ...
def prepare_github_repository(... ) -> dict[str, Any]: ...
```

From scripts/start_approved_project_delivery.py:
```python
def prepare_github_repository(authority_path: Path, *, mode: str, repository_name: str, repository_url: str, default_branch: str, workspace_path: Path) -> dict[str, Any]: ...
def run_pipeline_from_stage(authority_path: Path, record: dict[str, Any], *, start_stage: str, workspace_root: Path | None) -> dict[str, Any]: ...
def block_pipeline(... ) -> dict[str, Any]: ...
def update_pipeline_state(... ) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red tests for GitHub auth-source and repository-identity resolution</name>
  <read_first>
    - .planning/phases/14-github-auth-and-repository-target-resolution/14-RESEARCH.md
    - .planning/phases/14-github-auth-and-repository-target-resolution/14-PATTERNS.md
    - tests/test_phase11_github_sync.py
    - tests/test_project_delivery_pipeline_bootstrap.py
    - tests/test_project_delivery_pipeline_resume.py
  </read_first>
  <files>tests/test_phase11_github_sync.py, tests/test_project_delivery_pipeline_bootstrap.py, tests/test_project_delivery_pipeline_resume.py</files>
  <behavior>
    - Test 1: helper accepts explicit GH_TOKEN / GITHUB_TOKEN env input and preserves existing token-based behavior.
    - Test 2: helper accepts a healthy authenticated gh CLI session when token env vars are absent, instead of emitting missing_github_auth.
    - Test 3: fresh-run controller derives repository_owner from an operator-controlled/authenticated source and never from project_slug.
    - Test 4: resume-run controller preserves existing repository_owner/name/url when already set, and repairs only invalid or missing values.
    - Test 5: evidence/authority assertions require repository_owner, repository_name, repository_url, repository_mode, and auth-source details to be explicit after prepare success.
  </behavior>
  <action>Write failing regression coverage that reproduces the exact live failures: `gh auth status` healthy but token env absent, and owner fallback producing a slug-as-owner repo target. Keep tests in the current helper/controller suites rather than inventing a new test structure. Cover both fresh-run and resume-run behavior. Add explicit assertions that owner fallback must never use `project_slug`, even if repo-name fallback still may.</action>
  <acceptance_criteria>
    - `tests/test_phase11_github_sync.py` contains failing coverage for env-token auth and authenticated gh CLI auth.
    - `tests/test_project_delivery_pipeline_bootstrap.py` contains failing fresh-run assertions for canonical owner/name/url derivation.
    - `tests/test_project_delivery_pipeline_resume.py` contains failing resume-run assertions for metadata stability or repair.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_phase11_github_sync tests.test_project_delivery_pipeline_bootstrap tests.test_project_delivery_pipeline_resume -v</automated>
  </verify>
  <done>The tests fail for the real missing auth-source fallback and bad owner-resolution behavior, proving the phase target before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement layered GitHub auth-source resolution in the helper layer</name>
  <read_first>
    - scripts/github_delivery_common.py
    - tests/test_phase11_github_sync.py
  </read_first>
  <files>scripts/github_delivery_common.py, tests/test_phase11_github_sync.py</files>
  <behavior>
    - Test 1: explicit GH_TOKEN / GITHUB_TOKEN remains the first valid auth path.
    - Test 2: absent token env but healthy gh-authenticated CLI counts as valid auth.
    - Test 3: missing gh CLI and missing all auth sources still emit the current blocked reasons cleanly.
    - Test 4: repository-prepare evidence exposes which auth path was used so the operator surface is explainable.
  </behavior>
  <action>Refactor the helper auth check into a layered resolver instead of the current token-only boolean. Reuse the repo’s existing subprocess/blocked-result pattern. Do not broaden scope into sync behavior or repo-target policy. Keep `_require_gh()` as the gatekeeper, but allow it to accept either env-token auth or authenticated CLI auth. Persist auth-source details into the GitHub prepare evidence/result payload in a way the controller can later surface.</action>
  <acceptance_criteria>
    - Helper-level tests pass for token auth, CLI-session auth, and blocked-state fallbacks.
    - No regression is introduced for existing create/attach repository preparation behavior.
    - The helper still returns the standard blocked result shape for expected operator/runtime failures.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_phase11_github_sync -v</automated>
  </verify>
  <done>The GitHub helper accepts the real operator auth path without requiring manual token export and preserves the current blocked-result contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement canonical repository identity resolution in the controller layer</name>
  <read_first>
    - scripts/start_approved_project_delivery.py
    - tests/test_project_delivery_pipeline_bootstrap.py
    - tests/test_project_delivery_pipeline_resume.py
    - .planning/phases/14-github-auth-and-repository-target-resolution/14-RESEARCH.md
  </read_first>
  <files>scripts/start_approved_project_delivery.py, tests/test_project_delivery_pipeline_bootstrap.py, tests/test_project_delivery_pipeline_resume.py</files>
  <behavior>
    - Test 1: fresh-run metadata resolution uses operator-controlled/authenticated owner and canonical repo naming before helper invocation.
    - Test 2: resume-run resolution preserves valid existing metadata and only repairs invalid/missing values.
    - Test 3: `shipping.github` and authority evidence explicitly record repository_mode, repository_owner, repository_name, repository_url, and auth/evidence linkage.
    - Test 4: blocked results still route through `block_pipeline(...)` with stable stage and resume semantics.
  </behavior>
  <action>Move the Phase 14 policy fix into the controller: derive the canonical GitHub owner/name/url once, before calling `prepare_github_repository()`, and write the resulting metadata back into `shipping.github`. Ensure the code never falls back to `project_slug` as owner. Keep repo-name fallback conservative and deterministic. Preserve the existing create vs attach split, authority-first structure, and stage/event flow. Make sure both first-run and resumed runs converge to the same stable GitHub metadata shape.</action>
  <acceptance_criteria>
    - Fresh-run and resume-run tests pass for canonical owner/name/url derivation.
    - `shipping.github` contains explicit resolved metadata rather than ambiguous fallback values.
    - The prepare stage can hand clean canonical inputs to the helper for both create and attach modes.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_project_delivery_pipeline_bootstrap tests.test_project_delivery_pipeline_resume tests.test_phase11_github_sync -v</automated>
  </verify>
  <done>Approved-project repository preparation now resolves the correct repository identity in a stable, authority-first way for both new runs and retries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator machine auth state → approved delivery helper | Local CLI auth and env-token auth both cross into credential gating for repository preparation |
| approved-project authority record → GitHub target identity | Persisted repo metadata influences external GitHub actions and must not degrade to invalid defaults |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | T | scripts/start_approved_project_delivery.py | mitigate | Lock owner/name/url derivation in tests so invalid fallback values cannot silently reappear. |
| T-14-02 | S | scripts/github_delivery_common.py | mitigate | Explicitly distinguish valid auth sources and blocked reasons so the system cannot mistake “no token env” for “no auth exists.” |
| T-14-03 | R | approved-project evidence flow | mitigate | Require evidence payloads to expose resolved auth-source and repository identity for operator auditability. |
</threat_model>

<verification>
- `python -m unittest tests.test_phase11_github_sync -v`
- `python -m unittest tests.test_project_delivery_pipeline_bootstrap tests.test_project_delivery_pipeline_resume -v`
- Live-scenario spot check after implementation: approved-project prepare stage succeeds on a machine with healthy `gh auth status` and no manual token export.
</verification>

<success_criteria>
- Phase 14 removes the live `missing_github_auth` false negative for authenticated operator machines.
- Phase 14 removes the slug-as-owner fallback bug for new approved projects.
- Phase 14 preserves create/attach support while making authority metadata deterministic and auditable.
</success_criteria>

<output>
After completion, create `.planning/phases/14-github-auth-and-repository-target-resolution/14-01-SUMMARY.md`
</output>
