---
phase: 02-signal-triage-and-role-analysis-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/triage_external_signals.py
  - assets/shared/external_intelligence/triage/prioritized_signals.json
  - assets/shared/external_intelligence/triage/clusters.json
autonomous: true
requirements:
  - ANLY-02
user_setup: []
must_haves:
  truths:
    - The system builds a deterministic working set from Phase 1 signal history instead of asking later roles to consume raw append-only records directly.
    - Repeated or closely related pain signals are deduplicated or clustered before final synthesis.
    - The working set assigns explicit priority signals that later role outputs can consume consistently.
  artifacts:
    - scripts/triage_external_signals.py
    - assets/shared/external_intelligence/triage/prioritized_signals.json
    - assets/shared/external_intelligence/triage/clusters.json
  key_links:
    - scripts/triage_external_signals.py reads assets/shared/external_intelligence/history/signals.jsonl.
    - scripts/triage_external_signals.py writes both prioritized_signals.json and clusters.json.
    - Prioritized signals preserve evidence links back to raw normalized signal records.
---

<objective>
Build the deterministic triage layer for Phase 2 so raw discovered pain signals become a deduplicated, clustered, prioritized working set.

Purpose: Phase 2 cannot route useful analysis over raw append-only discovery history. It first needs a stable triage read model that groups repeated pain expressions and assigns explicit priorities.
Output: A triage CLI plus machine-readable prioritized and clustered signal artifacts for downstream role analysis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-signal-triage-and-role-analysis-loop/02-RESEARCH.md
@.planning/phases/02-signal-triage-and-role-analysis-loop/02-PATTERNS.md
@assets/shared/external_intelligence/history/signals.jsonl
@assets/shared/external_intelligence/LATEST_SUMMARY.md
@assets/shared/TEMPLATES.md
@scripts/collect_external_signals.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the triage CLI over normalized signal history</name>
  <files>scripts/triage_external_signals.py</files>
  <read_first>scripts/collect_external_signals.py, assets/shared/external_intelligence/history/signals.jsonl, assets/shared/TEMPLATES.md</read_first>
  <action>Create `scripts/triage_external_signals.py` as a Python 3 script that resolves repo-relative paths for `assets/shared/external_intelligence/history/signals.jsonl`, `assets/shared/external_intelligence/triage/prioritized_signals.json`, and `assets/shared/external_intelligence/triage/clusters.json`. Implement CLI flags `--window-hours`, `--limit`, and `--dry-run`. Load normalized signal history, select signals inside the requested time window, and fail non-zero on malformed records.</action>
  <verify>Run `python -m py_compile scripts/triage_external_signals.py` and read the file to confirm the exact flags `--window-hours`, `--limit`, and `--dry-run` exist.</verify>
  <acceptance_criteria>
    - `scripts/triage_external_signals.py` contains `--window-hours`
    - `scripts/triage_external_signals.py` contains `--limit`
    - `scripts/triage_external_signals.py` contains `--dry-run`
    - `scripts/triage_external_signals.py` references `signals.jsonl`, `prioritized_signals.json`, and `clusters.json`
    - `python -m py_compile scripts/triage_external_signals.py` exits 0
  </acceptance_criteria>
  <done>The repo has a deterministic triage entrypoint that can be invoked from scripts or cron.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deduplication and clustering rules</name>
  <files>scripts/triage_external_signals.py, assets/shared/external_intelligence/triage/clusters.json</files>
  <read_first>scripts/triage_external_signals.py, assets/shared/external_intelligence/history/signals.jsonl</read_first>
  <action>Inside `scripts/triage_external_signals.py`, implement deterministic grouping rules that cluster repeated or highly similar pain signals using title normalization, repeated URLs, and overlapping keywords. Write `assets/shared/external_intelligence/triage/clusters.json` with cluster identifiers, member signal IDs, evidence counts, recency metadata, and representative titles. Preserve evidence traceability back to the normalized source records.</action>
  <verify>Read `scripts/triage_external_signals.py` and confirm explicit clustering logic exists and that `clusters.json` is written with cluster IDs plus member signal references.</verify>
  <acceptance_criteria>
    - `scripts/triage_external_signals.py` contains deterministic similarity or grouping logic
    - `clusters.json` output includes cluster identifiers and member signal references
    - Cluster records preserve evidence count or recency information
  </acceptance_criteria>
  <done>The system now groups repeated discovery signals into reusable pain clusters.</done>
</task>

<task type="auto">
  <name>Task 3: Score and prioritize the triaged working set</name>
  <files>scripts/triage_external_signals.py, assets/shared/external_intelligence/triage/prioritized_signals.json</files>
  <read_first>scripts/triage_external_signals.py, assets/shared/TEMPLATES.md, assets/shared/external_intelligence/history/signals.jsonl</read_first>
  <action>Implement a deterministic first-pass prioritization model over clustered signals. Use dimensions compatible with later templates, such as evidence strength, recency, urgency-like heuristics, and confidence. Write `assets/shared/external_intelligence/triage/prioritized_signals.json` as an ordered working set that includes rank, cluster or lead ID, problem summary, evidence links, and score components. Do not yet implement role-specific markdown outputs or full CEO synthesis here.</action>
  <verify>Read `scripts/triage_external_signals.py` and confirm prioritized output includes explicit rank/order and score fields aligned with later template usage.</verify>
  <acceptance_criteria>
    - `prioritized_signals.json` contains an ordered prioritized set
    - prioritized records include evidence links or source references
    - prioritized records include score fields that can feed later Scout/CMO outputs
    - No role-specific markdown artifacts are generated in this plan
  </acceptance_criteria>
  <done>The repo has a machine-readable ranked shortlist that later plans can route through roles consistently.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m py_compile scripts/triage_external_signals.py`
- [ ] Read `scripts/triage_external_signals.py` and confirm the exact CLI flags, clustering logic, and prioritized output paths are present.
- [ ] Run the triage script and confirm `assets/shared/external_intelligence/triage/clusters.json` and `prioritized_signals.json` are created.
- [ ] Read both output files and confirm they contain evidence-linked, deduplicated, prioritized records.
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Raw Phase 1 signal history is converted into a deterministic, clustered, prioritized working set
- Later role-analysis plans can consume one shared shortlist instead of raw append-only discovery history
</success_criteria>

<output>
After completion, create `.planning/phases/02-signal-triage-and-role-analysis-loop/02-01-SUMMARY.md`
</output>
