---
phase: 02-signal-triage-and-role-analysis-loop
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - scripts/run_signal_analysis_loop.sh
  - orchestration/cron/commands.sh
  - orchestration/cron/daily_pipeline.prompt.md
  - scripts/smoke_test_pipeline.sh
  - assets/shared/CEO_RANKING.md
autonomous: true
requirements:
  - ANLY-03
user_setup: []
must_haves:
  truths:
    - The daily loop can complete an end-to-end analysis cycle over one shared prioritized signal set.
    - CEO synthesis produces a distinct ranking/recommendation output rather than collapsing all roles into one generic summary.
    - The end-to-end loop is runnable through a stable repo command path.
  artifacts:
    - scripts/run_signal_analysis_loop.sh
    - assets/shared/CEO_RANKING.md
    - orchestration/cron/commands.sh
    - orchestration/cron/daily_pipeline.prompt.md
    - scripts/smoke_test_pipeline.sh
  key_links:
    - run_signal_analysis_loop.sh executes triage and role handoff generation in sequence.
    - daily_pipeline.prompt.md reads or invokes the shared triage/role analysis path before CEO recommendation.
    - CEO_RANKING.md is derived from the same prioritized shortlist that fed Scout, CMO, and Architect.
---

<objective>
Integrate the prioritized triage and role handoff flow into one end-to-end analysis loop that produces a distinct CEO recommendation artifact.

Purpose: Phase 2 is only complete when the same prioritized signal set can drive Scout, CMO, Architect, and CEO outputs in one repeatable flow.
Output: A stable analysis-loop wrapper, cron wiring updates, smoke coverage, and a CEO ranking artifact derived from the shared shortlist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-signal-triage-and-role-analysis-loop/02-RESEARCH.md
@.planning/phases/02-signal-triage-and-role-analysis-loop/02-PATTERNS.md
@.planning/phases/02-signal-triage-and-role-analysis-loop/02-01-PLAN.md
@.planning/phases/02-signal-triage-and-role-analysis-loop/02-02-PLAN.md
@assets/shared/TEMPLATES.md
@orchestration/cron/commands.sh
@orchestration/cron/daily_pipeline.prompt.md
@scripts/smoke_test_pipeline.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a stable analysis-loop wrapper</name>
  <files>scripts/run_signal_analysis_loop.sh</files>
  <read_first>scripts/run_external_intelligence.sh, orchestration/cron/commands.sh, scripts/smoke_test_pipeline.sh</read_first>
  <action>Create `scripts/run_signal_analysis_loop.sh` as a bash wrapper that resolves the repo root, executes the triage script and role handoff generator in sequence, and fails fast with `set -euo pipefail`. Support passthrough CLI arguments so operators can run it directly in validation or cron contexts.</action>
  <verify>Read `scripts/run_signal_analysis_loop.sh` and confirm it contains `set -euo pipefail`, resolves the repo root, and invokes the expected Phase 2 scripts.</verify>
  <acceptance_criteria>
    - `scripts/run_signal_analysis_loop.sh` contains `set -euo pipefail`
    - It invokes the triage and role handoff generation scripts
    - It supports passthrough CLI arguments with `"$@"` or equivalent
  </acceptance_criteria>
  <done>The repo has a stable operator entrypoint for the Phase 2 analysis loop.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the CEO ranking artifact from the shared shortlist</name>
  <files>assets/shared/CEO_RANKING.md</files>
  <read_first>assets/shared/TEMPLATES.md, assets/shared/PAIN_POINTS.md, assets/shared/MARKET_PLAN.md, assets/shared/TECH_SPEC.md</read_first>
  <action>Generate `assets/shared/CEO_RANKING.md` as the distinct CEO recommendation artifact for the current ranked shortlist. Follow the intent of the CEO template in `assets/shared/TEMPLATES.md`: include a Top 3 view plus a recommended idea and one-line rationale. Ensure this artifact is derived from the same prioritized shortlist that fed the Scout, CMO, and Architect outputs rather than a fresh independent ranking pass.</action>
  <verify>Read `assets/shared/CEO_RANKING.md` and confirm it includes a ranked shortlist and a recommendation grounded in the shared Phase 2 working set.</verify>
  <acceptance_criteria>
    - `assets/shared/CEO_RANKING.md` exists
    - It contains a Top 3 style ranking or equivalent ranked recommendation view
    - It references the same shortlist context used by prior role artifacts
  </acceptance_criteria>
  <done>The CEO now has a distinct synthesis artifact over the shared prioritized signal set.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the end-to-end analysis loop into daily ops and smoke verification</name>
  <files>orchestration/cron/commands.sh, orchestration/cron/daily_pipeline.prompt.md, scripts/smoke_test_pipeline.sh</files>
  <read_first>orchestration/cron/commands.sh, orchestration/cron/daily_pipeline.prompt.md, scripts/smoke_test_pipeline.sh, scripts/run_signal_analysis_loop.sh</read_first>
  <action>Update `orchestration/cron/commands.sh` to expose a named action for the Phase 2 analysis loop. Update `orchestration/cron/daily_pipeline.prompt.md` so the daily sequence consumes the triage-driven role loop explicitly before final CEO recommendation, without collapsing the roles into one generic summary. Extend `scripts/smoke_test_pipeline.sh` only as needed to validate the new analysis-loop wiring and generated artifacts.</action>
  <verify>Read the updated files and confirm the analysis loop has a stable command path, daily-pipeline integration, and smoke-test coverage.</verify>
  <acceptance_criteria>
    - `orchestration/cron/commands.sh` exposes a named analysis-loop action
    - `orchestration/cron/daily_pipeline.prompt.md` reflects the shared prioritized role flow
    - `scripts/smoke_test_pipeline.sh` validates the new Phase 2 wiring
  </acceptance_criteria>
  <done>The end-to-end Phase 2 analysis loop is runnable and verifiable through the repo's operational surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Read `scripts/run_signal_analysis_loop.sh` and confirm it runs triage plus role handoff generation.
- [ ] Run the analysis loop and confirm `PAIN_POINTS.md`, `MARKET_PLAN.md`, `TECH_SPEC.md`, and `CEO_RANKING.md` are produced from one shared prioritized set.
- [ ] Read `orchestration/cron/commands.sh`, `orchestration/cron/daily_pipeline.prompt.md`, and `scripts/smoke_test_pipeline.sh` to confirm command, pipeline, and smoke wiring.
- [ ] Confirm the end-to-end flow no longer collapses into one generic summary.
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- The daily loop can complete a role-distinct analysis cycle over one prioritized signal set
- CEO recommendation is produced as a distinct synthesis artifact without prematurely implementing Phase 3 polish
</success_criteria>

<output>
After completion, create `.planning/phases/02-signal-triage-and-role-analysis-loop/02-03-SUMMARY.md`
</output>
