---
phase: 09-claude-code-delivery-team-orchestration
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - scripts/validate_delivery_handoff.py
  - scripts/request_scope_reopen.py
  - orchestration/cron/commands.sh
  - assets/workspaces/ceo/AGENTS.md
  - tests/test_scope_reopen_flow.py
  - tests/test_delivery_run_replay.py
autonomous: true
requirements:
  - TEAM-04
  - TEAM-05
  - TEAM-06
requirements_addressed:
  - TEAM-04
  - TEAM-05
  - TEAM-06
must_haves:
  truths:
    - "Out-of-scope or protected-platform delivery changes are blocked unless a governed scope reopen is explicitly requested."
    - "A full five-stage delivery run can be replayed from stable handoff artifacts plus role-attributed events."
    - "Operators can trigger the delivery workflow through existing command-entry patterns instead of manually orchestrating each step."
  artifacts:
    - path: "scripts/request_scope_reopen.py"
      provides: "Governance-backed scope reopen wrapper"
      exports: ["parse_args", "main"]
    - path: "scripts/validate_delivery_handoff.py"
      provides: "Stage/final artifact contract validator"
      exports: ["parse_args", "main"]
    - path: "tests/test_scope_reopen_flow.py"
      provides: "Protected-surface and scope-reopen enforcement tests"
      contains: "scope_reopen_requested"
    - path: "tests/test_delivery_run_replay.py"
      provides: "Five-stage replay and handoff chain tests"
      contains: "design -> development -> testing -> git versioning -> release readiness"
    - path: "orchestration/cron/commands.sh"
      provides: "Operator command wrappers for delivery lifecycle"
      contains: "start-delivery-run"
  key_links:
    - from: "scripts/request_scope_reopen.py"
      to: "scripts/request_governance_approval.py"
      via: "delivery-friendly governed wrapper"
      pattern: "governance"
    - from: "tests/test_delivery_run_replay.py"
      to: ".hermes/stage-handoffs/"
      via: "artifact chain and event replay assertions"
      pattern: "01-design|05-release-readiness"
    - from: "orchestration/cron/commands.sh"
      to: "scripts/start_delivery_run.py"
      via: "existing command-entry wrapper"
      pattern: "start_delivery_run.py"
---

<objective>
Finish Phase 9 by wiring scope governance, handoff validation, replay coverage, and command-entry integration onto the contract and artifact foundation.

Purpose: Enforce D-07 through D-12 using the existing governance and conformance stack so approved-project delivery becomes repeatable without owner microrouting, while protected changes still require explicit reopen approval.
Output: Scope reopen wrapper, delivery handoff validator, replay/integration tests, and command wrappers exposed through the current orchestration entrypoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-claude-code-delivery-team-orchestration/09-CONTEXT.md
@.planning/phases/09-claude-code-delivery-team-orchestration/09-RESEARCH.md
@.planning/phases/09-claude-code-delivery-team-orchestration/09-PATTERNS.md
@.planning/phases/09-claude-code-delivery-team-orchestration/09-VALIDATION.md
@.planning/phases/09-claude-code-delivery-team-orchestration/09-01-PLAN.md
@.planning/phases/09-claude-code-delivery-team-orchestration/09-02-PLAN.md
@docs/platform/standalone-saas-template-contract.md
@docs/OPERATIONS.md
@assets/workspaces/ceo/AGENTS.md
@orchestration/cron/commands.sh
@scripts/check_template_conformance.py
@scripts/request_governance_approval.py
@scripts/enforce_governed_action.py
@scripts/governance_common.py

<interfaces>
From orchestration/cron/commands.sh:
```bash
run_governed_action() {
  [ "$#" -ge 2 ] || { echo "Usage: bash orchestration/cron/commands.sh run-governed-action <action-id> <command...>"; return 1; }
  local action_id="$1"
  shift
  python "$ROOT_DIR/scripts/enforce_governed_action.py" --action-id "$action_id" --command "$@"
}
```

From scripts/check_template_conformance.py:
```python
PROTECTED_PATHS = (...)
ALLOWED_SHARED_TABLES = {"users", "orders", "payments", "subscriptions"}
```

From assets/workspaces/ceo/AGENTS.md:
```markdown
- 委派时必须传完整上下文（目标、输入文件、输出文件、验收标准）。
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock replay and scope-reopen behavior with end-to-end tests</name>
  <read_first>
    .planning/phases/09-claude-code-delivery-team-orchestration/09-02-PLAN.md
    .planning/phases/09-claude-code-delivery-team-orchestration/09-RESEARCH.md
    .planning/phases/09-claude-code-delivery-team-orchestration/09-PATTERNS.md
    .planning/phases/09-claude-code-delivery-team-orchestration/09-VALIDATION.md
    docs/platform/standalone-saas-template-contract.md
    scripts/check_template_conformance.py
    scripts/request_governance_approval.py
    scripts/enforce_governed_action.py
    tests/test_check_template_conformance.py
    tests/test_derived_packages.py
    orchestration/cron/commands.sh
  </read_first>
  <files>tests/test_scope_reopen_flow.py, tests/test_delivery_run_replay.py</files>
  <behavior>
    - Test 1: A requested protected-platform or expanded-scope change triggers a governed scope reopen instead of continuing silently, and records a delivery event with a blocked or pending outcome per D-07/D-08/D-09.
    - Test 2: A healthy run with five stage handoffs plus valid events can be replayed in order from design through release readiness without terminal history.
    - Test 3: Missing handoff sections, skipped stage order, or absent final artifact fail replay/validation before the workflow is considered complete.
  </behavior>
  <action>Create `tests/test_scope_reopen_flow.py` and `tests/test_delivery_run_replay.py` first. Use temp workspaces seeded with the Phase 9 `.hermes` artifacts from Plan 09-02, the governance/conformance integration style from `tests/test_check_template_conformance.py`, and the replay/assertion discipline mapped to `tests/test_derived_packages.py` in `09-PATTERNS.md`. In `tests/test_scope_reopen_flow.py`, simulate a protected target such as `src/lib/paypal.ts` or an out-of-brief feature request and assert that the wrapper creates a governance-backed reopen request and a `scope_reopen_requested` or blocked delivery event rather than allowing the stage to continue per D-07/D-08/D-09. In `tests/test_delivery_run_replay.py`, seed five stage handoff files plus a final delivery artifact and assert that replay succeeds only when the event log, stage order, role attribution, artifact chain, and workspace-local `.hermes/delivery-events.jsonl` authority stream are complete per D-10/D-12.</action>
  <acceptance_criteria>
    - `tests/test_scope_reopen_flow.py` proves protected changes cannot proceed on convention alone.
    - `tests/test_delivery_run_replay.py` proves replay uses stage artifacts plus explicit events, not chat logs.
    - The tests fail when stage order is skipped, artifact sections are missing, or final delivery evidence is absent.
    - The tests encode TEAM-04, TEAM-05, and TEAM-06 behaviors explicitly.
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/42236/Desktop/dev/profit-corp-hermes && python -m unittest tests.test_scope_reopen_flow tests.test_delivery_run_replay -v</automated>
  </verify>
  <done>Phase 9’s high-risk behaviors are locked before implementation: scope creep must reopen, and delivery completion must remain replayable from durable artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement delivery handoff validation and governance-backed scope reopen</name>
  <read_first>
    tests/test_scope_reopen_flow.py
    tests/test_delivery_run_replay.py
    .planning/phases/09-claude-code-delivery-team-orchestration/09-PATTERNS.md
    scripts/request_governance_approval.py
    scripts/enforce_governed_action.py
    scripts/governance_common.py
    scripts/check_template_conformance.py
    docs/platform/standalone-saas-template-contract.md
  </read_first>
  <files>scripts/validate_delivery_handoff.py, scripts/request_scope_reopen.py</files>
  <behavior>
    - Test 1: `validate_delivery_handoff.py` rejects stage or final artifacts missing the required Plan 09-01 sections or required delivery metadata.
    - Test 2: `request_scope_reopen.py` maps delivery-language inputs to the existing governance approval flow instead of creating a second approval system.
    - Test 3: Protected targets and non-approved scope requests produce machine-readable delivery events and actionable governance references for operator review.
  </behavior>
  <action>Implement `scripts/validate_delivery_handoff.py` and `scripts/request_scope_reopen.py` as thin Python stdlib tools. `validate_delivery_handoff.py` must follow the validation analogs mapped in `09-PATTERNS.md`, validate `.hermes/stage-handoffs/*.md` and `.hermes/FINAL_DELIVERY.md` against the locked section structure from Plan 09-01, ensure metadata fields (`run_id`, `role`, `stage`, `scope_status`) are present, and confirm the final artifact includes impact surface, verification evidence, rollback plan, and release recommendation per D-06. `request_scope_reopen.py` must follow the wrapper pattern from `scripts/request_governance_approval.py`, stay a thin delivery-language adapter per the resolved `09-RESEARCH.md` choice, translate inputs like `--run-id`, `--stage`, `--role`, `--target-artifact`, and `--reason` into a governed action request, and append a matching workspace-local delivery event before re-rendering `DELIVERY_STATUS.md` per D-08/D-09/D-10. Do not duplicate approval storage, status rendering, or protected-path policy logic; reuse the current governance and conformance stack exactly.</action>
  <acceptance_criteria>
    - Delivery handoff validation fails on missing sections, metadata, or final release evidence.
    - Scope reopen uses the existing governance path rather than new state files or a second approval schema.
    - Protected-path and scope-expansion requests leave both governance evidence and delivery-event evidence.
    - The tests from Task 1 pass without stubbing away the existing governance/conformance contract.
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/42236/Desktop/dev/profit-corp-hermes && python -m unittest tests.test_scope_reopen_flow tests.test_delivery_run_replay -v && python scripts/validate_delivery_handoff.py --help && python scripts/request_scope_reopen.py --help</automated>
  </verify>
  <done>Phase 9 now has executable validators and governed reopen tooling that enforce approved-brief-only delivery and stable final handoff structure.</done>
</task>

<task type="auto">
  <name>Task 3: Expose repeatable delivery commands through the existing operator entrypoints</name>
  <read_first>
    .planning/phases/09-claude-code-delivery-team-orchestration/09-PATTERNS.md
    scripts/start_delivery_run.py
    scripts/append_delivery_event.py
    scripts/render_delivery_status.py
    scripts/validate_delivery_handoff.py
    scripts/request_scope_reopen.py
    orchestration/cron/commands.sh
    assets/workspaces/ceo/AGENTS.md
    docs/OPERATIONS.md
  </read_first>
  <files>orchestration/cron/commands.sh, assets/workspaces/ceo/AGENTS.md</files>
  <action>Extend `orchestration/cron/commands.sh` with explicit wrappers for `start-delivery-run`, `append-delivery-event`, `render-delivery-status`, `request-scope-reopen`, and `validate-delivery-handoff`, following the existing `run-visibility`, `run-governed-action`, and operator wrapper patterns identified in `09-PATTERNS.md`. Each wrapper must call the Phase 9 Python scripts directly and print a clear usage string on invalid args. Then update `assets/workspaces/ceo/AGENTS.md` so the CEO orchestration rules include a delivery-orchestrator delegation example with the full D-04 input bundle, artifact outputs, and acceptance criteria, plus explicit language that protected-path or expanded-scope requests must route through the thin scope reopen wrapper per D-01/D-03/D-08. Reuse the repo’s current delegate payload pattern; do not introduce a second orchestration syntax or owner-microrouting fallback as the default path.</action>
  <acceptance_criteria>
    - `orchestration/cron/commands.sh` exposes Phase 9 delivery lifecycle commands through the existing command-entry pattern.
    - `assets/workspaces/ceo/AGENTS.md` includes delivery orchestration examples with exact inputs, outputs, and acceptance criteria.
    - The command wrappers and AGENTS guidance reinforce approved-brief-only scope and governed reopen behavior.
    - No new command bypasses the existing governed-action or conformance pathways.
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/42236/Desktop/dev/profit-corp-hermes && bash orchestration/cron/commands.sh start-delivery-run --help && bash orchestration/cron/commands.sh append-delivery-event --help && bash orchestration/cron/commands.sh render-delivery-status --help && bash orchestration/cron/commands.sh request-scope-reopen --help && bash orchestration/cron/commands.sh validate-delivery-handoff --help && python -m unittest tests.test_scope_reopen_flow tests.test_delivery_run_replay -v</automated>
  </verify>
  <done>Operators can launch and supervise the approved-project delivery workflow through familiar repo entrypoints, and the workflow no longer depends on ad-hoc owner sequencing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| delivery workflow -> governance approval | Protected-surface changes must cross an explicit governed boundary before continuing |
| stage/final artifacts -> replay validator | Untrusted or incomplete artifacts must not be treated as proof of successful delivery |
| operator commands -> delivery scripts | Wrapper commands must preserve the same safety gates as direct script usage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | E | `scripts/request_scope_reopen.py` | mitigate | Route all protected or expanded-scope requests through the existing governance approval flow and record the resulting delivery event. |
| T-09-10 | T | `scripts/validate_delivery_handoff.py` | mitigate | Reject missing sections, missing metadata, and incomplete final evidence so forged or partial artifacts cannot pass as complete delivery. |
| T-09-11 | R | `tests/test_delivery_run_replay.py` | mitigate | Require ordered stage artifacts plus role-attributed events so completed runs can be replayed without relying on ephemeral chat history. |
| T-09-12 | D | `orchestration/cron/commands.sh` | mitigate | Keep command wrappers thin and usage-validated so operators get deterministic failure reasons instead of partial hidden behavior. |
</threat_model>

<verification>
Run the Phase 9 replay and governance suites, then confirm the new delivery lifecycle commands are exposed through the existing command wrapper.
</verification>

<success_criteria>
- Protected or out-of-scope changes cannot silently proceed.
- Delivery completion is provable from artifacts plus events.
- Operators can start, observe, reopen, validate, and replay delivery runs through the existing repo entrypoints.
- TEAM-04, TEAM-05, and TEAM-06 are executable and governed.
</success_criteria>

<output>
After completion, create `.planning/phases/09-claude-code-delivery-team-orchestration/09-claude-code-delivery-team-orchestration-03-SUMMARY.md`
</output>
