---
phase: 09-claude-code-delivery-team-orchestration
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - scripts/start_delivery_run.py
  - scripts/append_delivery_event.py
  - scripts/render_delivery_status.py
  - tests/test_start_delivery_run.py
  - tests/test_delivery_events.py
autonomous: true
requirements:
  - TEAM-02
  - TEAM-03
  - TEAM-05
requirements_addressed:
  - TEAM-02
  - TEAM-03
  - TEAM-05
must_haves:
  truths:
    - "An approved workspace can be initialized into a delivery run only when the fixed input bundle is present."
    - "Major delivery actions are recorded as role-attributed events in a machine-readable authority stream."
    - "Operators can read a latest-status view derived from the event stream without scraping chat or terminal history."
  artifacts:
    - path: "scripts/start_delivery_run.py"
      provides: "Workspace-local delivery run bootstrap"
      exports: ["parse_args", "main"]
    - path: "scripts/append_delivery_event.py"
      provides: "Validated append-only delivery event writer"
      exports: ["parse_args", "main"]
    - path: "scripts/render_delivery_status.py"
      provides: "Derived DELIVERY_STATUS.md renderer"
      exports: ["parse_args", "main"]
    - path: "tests/test_start_delivery_run.py"
      provides: "Blocking tests for missing-input and manifest creation behavior"
      contains: "delivery-run-manifest.json"
    - path: "tests/test_delivery_events.py"
      provides: "Event schema and latest-view rendering tests"
      contains: "delivery-events.jsonl"
  key_links:
    - from: "scripts/start_delivery_run.py"
      to: ".hermes/project-metadata.json"
      via: "required input validation and manifest generation"
      pattern: "project-metadata.json"
    - from: "scripts/append_delivery_event.py"
      to: ".hermes/delivery-events.jsonl"
      via: "append-only writer"
      pattern: "json.dumps"
    - from: "scripts/render_delivery_status.py"
      to: ".hermes/DELIVERY_STATUS.md"
      via: "derived latest view"
      pattern: "DELIVERY_STATUS.md"
---

<objective>
Build the delivery-run artifact foundation inside workspace-local `.hermes/` paths so Phase 9 gains durable initialization, event logging, and latest-view rendering.

Purpose: Implement D-04 and D-10 through D-12 using the repo’s existing JSONL-plus-markdown status pattern, while keeping project delivery state local to each generated workspace.
Output: Start-run, event-append, and status-render scripts plus tests that prove missing inputs block and valid runs produce replayable artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-claude-code-delivery-team-orchestration/09-CONTEXT.md
@.planning/phases/09-claude-code-delivery-team-orchestration/09-RESEARCH.md
@.planning/phases/09-claude-code-delivery-team-orchestration/09-PATTERNS.md
@.planning/phases/09-claude-code-delivery-team-orchestration/09-VALIDATION.md
@.planning/phases/09-claude-code-delivery-team-orchestration/09-01-PLAN.md
@.planning/phases/08-shared-supabase-backend-guardrails/08-02-SUMMARY.md
@scripts/instantiate_template_project.py
@scripts/check_template_conformance.py
@scripts/governance_common.py
@scripts/render_governance_status.py

<interfaces>
From scripts/instantiate_template_project.py:
```python
def write_hermes_handoff(workspace: Path, metadata: dict[str, str]) -> None:
    ...
```

From scripts/check_template_conformance.py:
```python
def load_json(path: Path, label: str) -> dict[str, Any]:
    ...

def write_report(path: Path, content: str) -> None:
    ...
```

From Plan 09-01 contract:
```text
Required inputs: approved brief, template contract, shared-backend guardrails, project metadata, GSD constraints.
Required metadata: run_id, role, stage, scope_status.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock start-run, event-stream, and latest-view behavior with workspace fixtures</name>
  <read_first>
    .planning/phases/09-claude-code-delivery-team-orchestration/09-01-PLAN.md
    .planning/phases/09-claude-code-delivery-team-orchestration/09-RESEARCH.md
    .planning/phases/09-claude-code-delivery-team-orchestration/09-PATTERNS.md
    scripts/instantiate_template_project.py
    scripts/check_template_conformance.py
    scripts/governance_common.py
    scripts/render_governance_status.py
    tests/test_instantiate_template_project.py
    tests/test_check_template_conformance.py
  </read_first>
  <files>tests/test_start_delivery_run.py, tests/test_delivery_events.py</files>
  <behavior>
    - Test 1: `start_delivery_run.py` blocks when any D-04 input is missing and creates `.hermes/delivery-run-manifest.json`, `.hermes/DELIVERY_SCOPE.md`, `.hermes/delivery-events.jsonl`, and `.hermes/DELIVERY_STATUS.md` when the bundle is complete.
    - Test 2: The initial manifest records one orchestrator, the five fixed stages, required input paths, workspace name, and default `scope_status=approved-brief-only` per D-08.
    - Test 3: `append_delivery_event.py` only accepts events with explicit `run_id`, `workspace_name`, `role`, `stage`, `action`, `artifact`, `timestamp`, and `outcome`, and `render_delivery_status.py` derives the latest markdown view from JSONL instead of direct manual edits.
  </behavior>
  <action>Create `tests/test_start_delivery_run.py` and `tests/test_delivery_events.py` first, following the temp-workspace fixture pattern already used in `tests/test_instantiate_template_project.py` and the event/status validation pattern mapped to `scripts/governance_common.py` plus `scripts/render_governance_status.py` in `09-PATTERNS.md`. Seed a fake approved workspace under a temp directory with `.hermes/project-metadata.json`, `.hermes/shared-backend-guardrails.json`, `.hermes/PROJECT_BRIEF_ENTRYPOINT.md`, and references to `docs/platform/standalone-saas-template-contract.md` plus the Phase 9 delivery contract docs from Plan 09-01. Assert that missing any one item fails fast with a blocking message, and that healthy runs create a manifest, a scope artifact, a workspace-local append-only JSONL stream, and a derived status markdown. Mirror the governance event discipline: explicit actor metadata, append-only semantics, and read-only latest view per D-10/D-11/D-12.</action>
  <acceptance_criteria>
    - `tests/test_start_delivery_run.py` covers both missing-input fail cases and successful run bootstrap.
    - `tests/test_delivery_events.py` covers schema validation, append-only writes, and latest-view rendering from JSONL.
    - The tests assert workspace-local `.hermes/` paths only; they do not redirect delivery state into `assets/shared/`.
    - The tests encode D-04, D-10, D-11, and D-12 behaviors explicitly.
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/42236/Desktop/dev/profit-corp-hermes && python -m unittest tests.test_start_delivery_run tests.test_delivery_events -v</automated>
  </verify>
  <done>The Phase 9 foundation is locked by failing tests before implementation: no delivery run may start without the approved bundle, and no status view may exist without an authority event stream.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement workspace-local run bootstrap, event append, and status rendering</name>
  <read_first>
    tests/test_start_delivery_run.py
    tests/test_delivery_events.py
    .planning/phases/09-claude-code-delivery-team-orchestration/09-PATTERNS.md
    scripts/instantiate_template_project.py
    scripts/check_template_conformance.py
    scripts/governance_common.py
    scripts/render_governance_status.py
    .planning/phases/08-shared-supabase-backend-guardrails/08-02-SUMMARY.md
  </read_first>
  <files>scripts/start_delivery_run.py, scripts/append_delivery_event.py, scripts/render_delivery_status.py</files>
  <behavior>
    - Test 1: `start_delivery_run.py` validates the required bundle, writes manifest/scope/status artifacts, and appends an initial `run_started` event attributed to `delivery-orchestrator` per D-01/D-04.
    - Test 2: `append_delivery_event.py` rejects malformed or roleless events and appends valid JSONL entries in timestamp order.
    - Test 3: `render_delivery_status.py` rebuilds `DELIVERY_STATUS.md` from `delivery-events.jsonl` and surfaces the latest stage, role, outcome, open gate status, and most recent artifact link for operators per D-11.
  </behavior>
  <action>Implement `scripts/start_delivery_run.py`, `scripts/append_delivery_event.py`, and `scripts/render_delivery_status.py` in Python stdlib style matching the existing governance helpers identified in `09-PATTERNS.md`. `start_delivery_run.py` should accept a workspace path, validate the D-04 input bundle, create `.hermes/delivery-run-manifest.json`, `.hermes/DELIVERY_SCOPE.md`, `.hermes/stage-handoffs/`, `.hermes/delivery-events.jsonl`, and `.hermes/DELIVERY_STATUS.md`, and seed the manifest with the five specialist roles, stable `workspace_name`, and their expected artifact paths per D-02/D-03 plus the resolved per-workspace event-stream choice from `09-RESEARCH.md`. `append_delivery_event.py` should follow the append/validate pattern from `scripts/governance_common.py`, validate allowed role/stage combinations from Plan 09-01 instead of accepting arbitrary labels, and require `workspace_name` alongside `run_id` so later aggregation can be added without changing the Phase 9 contract. `render_delivery_status.py` should follow the derived-latest-view pattern from `scripts/render_governance_status.py`, treat JSONL as the authority source, and render a markdown latest view showing run id, workspace name, current stage, last role/action, gate status, scope status, and cited artifact paths per D-10/D-11. Reuse the repo’s file-safe write helpers and JSONL pattern; do not introduce a database, daemon, or global shared event sink.</action>
  <acceptance_criteria>
    - A healthy temp workspace can be initialized into a delivery run with only workspace-local `.hermes` outputs.
    - The manifest, event stream, and latest view use stable field names that later plans can consume for replay and scope reopen.
    - Invalid events are rejected before file write, and valid events refresh the latest view deterministically.
    - The tests from Task 1 pass without loosening validation.
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/42236/Desktop/dev/profit-corp-hermes && python -m unittest tests.test_start_delivery_run tests.test_delivery_events -v && python scripts/start_delivery_run.py --help && python scripts/append_delivery_event.py --help && python scripts/render_delivery_status.py --help</automated>
  </verify>
  <done>Phase 9 now has executable workspace-local artifacts for delivery-run bootstrap, role-attributed event capture, and operator-readable latest status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved workspace bundle -> run bootstrap | Untrusted or incomplete workspace files must be blocked before a delivery run starts |
| delivery events -> status markdown | The human-readable latest view must never outrun or contradict the authority stream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | S | `scripts/append_delivery_event.py` | mitigate | Validate allowed role/stage combinations and require explicit role/action fields before appending any event. |
| T-09-06 | T | `scripts/render_delivery_status.py` | mitigate | Render `DELIVERY_STATUS.md` only from JSONL so manual status tampering cannot become the source of truth. |
| T-09-07 | R | `.hermes/delivery-events.jsonl` | mitigate | Append immutable role-attributed records with timestamps, artifacts, and outcomes so failed or blocked runs remain auditable. |
| T-09-08 | D | `scripts/start_delivery_run.py` | mitigate | Fail fast on missing required inputs rather than generating partial run state that later stages cannot trust. |
</threat_model>

<verification>
Use the new temp-workspace unit suite to prove that Phase 9 can initialize and observe delivery runs entirely through durable `.hermes` artifacts.
</verification>

<success_criteria>
- Start-run validation enforces the approved-project bundle before any work begins.
- Delivery events are append-only, role-attributed, and machine-readable.
- DELIVERY_STATUS.md is a derived latest view, not a manually maintained scratch file.
- TEAM-02, TEAM-03, and TEAM-05 are executable rather than doc-only.
</success_criteria>

<output>
After completion, create `.planning/phases/09-claude-code-delivery-team-orchestration/09-claude-code-delivery-team-orchestration-02-SUMMARY.md`
</output>
