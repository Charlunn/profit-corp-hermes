---
phase: 06-execution-handoff-and-team-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - tests/test_derived_packages.py
  - scripts/derive_execution_package.py
  - assets/shared/execution_packages/EXECUTION_PACKAGE.md
  - assets/shared/execution_packages/history/2026-04-25-execution-package.md
autonomous: true
requirements:
  - DECI-02
must_haves:
  truths:
    - "The execution package is a concise handoff pack with the locked Core 9 sections, not a checklist or backlog."
    - "The execution package exposes lightweight ownership metadata with a valid readiness status."
    - "Every surfaced execution risk has a directly paired acceptance gate so the next operator knows what ready means."
    - "The execution package stays solo-operator-first while becoming structurally safe for later team handoff."
  artifacts:
    - path: "scripts/derive_execution_package.py"
      provides: "Execution-package derivation logic with readiness metadata and risk/gate pairing"
      contains: "render_execution_package"
    - path: "assets/shared/execution_packages/EXECUTION_PACKAGE.md"
      provides: "Latest execution handoff pack"
      contains: "## Goal"
  key_links:
    - from: "scripts/derive_execution_package.py"
      to: "assets/shared/decision_packages/OPERATING_DECISION_PACKAGE.md"
      via: "section extractors and labeled-value parsing"
      pattern: "OPERATING_DECISION_PACKAGE_PATH"
    - from: "scripts/derive_execution_package.py"
      to: "assets/shared/governance/GOVERNANCE_STATUS.md"
      via: "readiness-status derivation"
      pattern: "GOVERNANCE_STATUS"
---

<objective>
Upgrade the execution package into the locked Phase 6 handoff contract.

Purpose: Deliver D-01 through D-12, D-16, and D-17 by turning the existing kickoff summary into a stable, machine-readable yet concise handoff pack that future workflows and humans can consume directly.
Output: Updated execution-package generator and regenerated latest/history execution handoff artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-execution-handoff-and-team-readiness/06-CONTEXT.md
@.planning/phases/06-execution-handoff-and-team-readiness/06-RESEARCH.md
@.planning/phases/06-execution-handoff-and-team-readiness/06-PATTERNS.md
@.planning/phases/06-execution-handoff-and-team-readiness/06-01-PLAN.md
@scripts/derive_execution_package.py
@assets/shared/decision_packages/OPERATING_DECISION_PACKAGE.md
@assets/shared/governance/GOVERNANCE_STATUS.md
@assets/shared/trace/decision_package_trace.json

<interfaces>
From scripts/derive_execution_package.py:
```python
OPERATING_DECISION_PACKAGE_PATH = DECISION_PACKAGES_DIR / "OPERATING_DECISION_PACKAGE.md"
DECISION_TRACE_PATH = TRACE_DIR / "decision_package_trace.json"

def extract_value(content: str, label: str) -> str: ...
def extract_first_table_row(content: str) -> dict[str, str]: ...
def render_execution_package(date_value: str, operating_package: str, trace_content: str) -> str: ...
```

From assets/shared/decision_packages/OPERATING_DECISION_PACKAGE.md:
```markdown
## Operating Framing
- **Goal**: ...
- **Target User**: ...
- **MVP Framing**: ...

## 主要风险
### ...
- **Judgment ID**: ...
- **Summary**: ...

## 推荐下一步
- ... trace: `judgment_id=...`
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild the execution generator around the Core 9 contract</name>
  <read_first>
    tests/test_derived_packages.py
    scripts/derive_execution_package.py
    assets/shared/decision_packages/OPERATING_DECISION_PACKAGE.md
    .planning/phases/06-execution-handoff-and-team-readiness/06-CONTEXT.md
    .planning/phases/06-execution-handoff-and-team-readiness/06-RESEARCH.md
  </read_first>
  <files>tests/test_derived_packages.py, scripts/derive_execution_package.py</files>
  <behavior>
    - Test 1: Dry-run output renders the ordered sections `## Goal`, `## Scope Boundary`, `## Target User`, `## MVP Framing`, `## Dependencies`, `## Key Risks`, `## Acceptance Gate`, `## Recommended First Actions`, `## Handoff Target`.
    - Test 2: The metadata block contains exactly `Owner`, `Primary Role`, `Handoff Target`, and `Readiness Status`, and readiness is only `ready`, `blocked`, or `needs-input`.
    - Test 3: Each surfaced risk has one directly corresponding acceptance-gate line in the same order.
  </behavior>
  <action>Modify `tests/test_derived_packages.py` and `scripts/derive_execution_package.py` together so the new execution-package contract is implemented and immediately verified in the same task. Use the script skeleton, allowlisted writes, and render-function ownership pattern mapped in `.planning/phases/06-execution-handoff-and-team-readiness/06-PATTERNS.md` from `scripts/generate_decision_package.py`, the existing `scripts/derive_execution_package.py`, and the supporting-view compatibility rules documented there for `scripts/generate_operating_visibility.py`. Activate exact execution assertions in `tests/test_derived_packages.py` for ordered headings `## Goal`, `## Scope Boundary`, `## Target User`, `## MVP Framing`, `## Dependencies`, `## Key Risks`, `## Acceptance Gate`, `## Recommended First Actions`, `## Handoff Target`; exact metadata labels `Owner`, `Primary Role`, `Handoff Target`, and `Readiness Status`; the enum `ready|blocked|needs-input`; per-risk acceptance-gate pairing; and banned task-board/workflow vocabulary. In `scripts/derive_execution_package.py`, keep reading the operating decision package and `decision_package_trace.json`, but now also read `assets/shared/governance/GOVERNANCE_STATUS.md` as a lightweight overlay for readiness, matching the resolved metadata-placement guidance in `06-RESEARCH.md`. Render a concise provenance header, then a top metadata block with exact labels `Owner: ceo`, `Primary Role: ceo`, `Handoff Target: founder/operator`, and `Readiness Status: <enum>`. Derive `Readiness Status` as `blocked` when `## Active Blocks` contains any non-`None` item, `needs-input` when there are pending approvals but no active block, else `ready`. Replace `## Kickoff Focus` with the exact Core 9 section set from D-04. Keep every section to 1-3 bullet items per D-05. Set `## Scope Boundary` bullets to concrete anti-bloat limits per D-06: no task board, no approval workflow detail, no backlog dump, no deep implementation decomposition. Build `## Dependencies` from the operating package source/trace references plus any governance dependency needed for readiness. In `## Key Risks`, surface the top 1-3 risk summaries from `## 主要风险`; in `## Acceptance Gate`, emit the same number of bullets, each explicitly paired to the matching risk using stable prefixes like `Risk 1 gate:` / `Risk 2 gate:`. Derive `## Recommended First Actions` from the first 1-3 operating-package next-step bullets, and keep `## Handoff Target` as the concise receiving-reader section for the founder/operator handoff per D-02 and D-08. Implement D-16 and D-17 explicitly by keeping the current handoff addressed to the solo founder/operator while making later team expansion possible only through the stable ownership fields and fixed section contract; do not add multi-user workflow sections, approval-routing state machines, assignment matrices, or queue semantics.</action>
  <acceptance_criteria>
    - `scripts/derive_execution_package.py` contains the exact strings `## Scope Boundary`, `## Acceptance Gate`, and `Readiness Status`.
    - `scripts/derive_execution_package.py` no longer contains the heading `## Kickoff Focus`.
    - `scripts/derive_execution_package.py` reads `assets/shared/governance/GOVERNANCE_STATUS.md` or an equivalent constant for readiness derivation.
    - `scripts/derive_execution_package.py` contains explicit enum values `ready`, `blocked`, and `needs-input`.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest discover -s C:/Users/42236/Desktop/dev/profit-corp-hermes/tests -p test_derived_packages.py -v</automated>
  </verify>
  <done>The execution generator produces the locked Core 9 handoff structure, embedded ownership metadata, per-risk acceptance gates, and solo-first future-team-safe contract without introducing task-board or schema-heavy behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the execution latest/history artifacts from the hardened generator</name>
  <read_first>
    scripts/derive_execution_package.py
    assets/shared/execution_packages/EXECUTION_PACKAGE.md
    assets/shared/execution_packages/history/2026-04-25-execution-package.md
    tests/test_derived_packages.py
  </read_first>
  <files>assets/shared/execution_packages/EXECUTION_PACKAGE.md, assets/shared/execution_packages/history/2026-04-25-execution-package.md</files>
  <action>Run `python scripts/derive_execution_package.py --date 2026-04-25` so both the latest artifact and `assets/shared/execution_packages/history/2026-04-25-execution-package.md` are regenerated with the new contract. Ensure the rendered artifact includes the exact metadata labels, all nine section headings, 1-3 bullets per section, and no legacy `Kickoff Focus` block. Preserve the solo-first reader stance per D-16/D-17: the artifact should read like a concise founder/operator handoff today while exposing only the stable ownership metadata needed for future team intake later.</action>
  <acceptance_criteria>
    - `assets/shared/execution_packages/EXECUTION_PACKAGE.md` contains the exact headings `## Goal` and `## Handoff Target`.
    - `assets/shared/execution_packages/EXECUTION_PACKAGE.md` contains `Readiness Status` and one of `ready`, `blocked`, or `needs-input`.
    - `assets/shared/execution_packages/EXECUTION_PACKAGE.md` does not contain `## Kickoff Focus`, `Backlog`, or `Task Board`.
    - `assets/shared/execution_packages/history/2026-04-25-execution-package.md` exists and contains the same new headings.
  </acceptance_criteria>
  <verify>
    <automated>python C:/Users/42236/Desktop/dev/profit-corp-hermes/scripts/derive_execution_package.py --date 2026-04-25 && python -m unittest discover -s C:/Users/42236/Desktop/dev/profit-corp-hermes/tests -p test_derived_packages.py -v</automated>
  </verify>
  <done>The latest and dated execution artifacts reflect the hardened handoff contract and satisfy the new unit contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operating decision package -> execution generator | Upstream markdown content is transformed into a downstream handoff contract |
| governance status -> readiness metadata | Read-only governance overlay affects surfaced readiness state |
| generator -> shared artifact outputs | Script writes must stay inside allowed execution-package directories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | T | `scripts/derive_execution_package.py` write path | mitigate | Preserve allowlisted write directories and write only latest/history execution artifact outputs. |
| T-06-05 | I | execution package readiness metadata | mitigate | Derive readiness only from explicit governance headings and emit only the locked enum values, not ad-hoc free text. |
| T-06-06 | R | execution-package provenance | mitigate | Keep `Derived From` and `Source Trace` headers bound to `OPERATING_DECISION_PACKAGE.md` and `decision_package_trace.json`. |
</threat_model>

<verification>
Run the strengthened derived-package unit suite after script changes and after artifact regeneration.
</verification>

<success_criteria>
- `scripts/derive_execution_package.py` renders the Phase 6 Core 9 contract.
- Latest and history execution artifacts are regenerated from the script, not manually edited.
- New output includes valid ownership metadata and per-risk acceptance gates.
- The execution artifact remains solo-operator-first while structurally ready for later team handoff.
</success_criteria>

<output>
After completion, create `.planning/phases/06-execution-handoff-and-team-readiness/06-execution-handoff-and-team-readiness-02-SUMMARY.md`
</output>
