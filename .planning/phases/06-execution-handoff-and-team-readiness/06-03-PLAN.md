---
phase: 06-execution-handoff-and-team-readiness
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - tests/test_derived_packages.py
  - scripts/derive_board_briefing.py
  - scripts/generate_operating_visibility.py
  - tests/test_generate_operating_visibility.py
  - assets/shared/board_briefings/BOARD_BRIEFING.md
  - assets/shared/board_briefings/history/2026-04-25-board-briefing.md
autonomous: true
requirements:
  - DECI-03
must_haves:
  truths:
    - "The board briefing remains a one-screen derivative of the operating package with exactly one governance, one risk, one finance, and one required-attention signal."
    - "The board briefing stays trace-linked to the same operating decision foundation instead of becoming a separate analysis artifact."
    - "Board output remains cron-regenerable through the existing script and history snapshot flow."
    - "The refined board brief preserves the solo-first operating loop while staying safe for future collaboration growth."
  artifacts:
    - path: "scripts/derive_board_briefing.py"
      provides: "Board-brief derivation logic with exact single-signal sections"
      contains: "render_board_briefing"
    - path: "assets/shared/board_briefings/BOARD_BRIEFING.md"
      provides: "Latest one-screen board briefing"
      contains: "## Governance Signal"
  key_links:
    - from: "scripts/derive_board_briefing.py"
      to: "assets/shared/decision_packages/OPERATING_DECISION_PACKAGE.md"
      via: "conclusion, risk, and attention extraction"
      pattern: "OPERATING_DECISION_PACKAGE_PATH"
    - from: "scripts/derive_board_briefing.py"
      to: "assets/shared/governance/GOVERNANCE_STATUS.md"
      via: "single governance signal selection"
      pattern: "GOVERNANCE_STATUS"
    - from: "scripts/derive_board_briefing.py"
      to: "assets/shared/LEDGER.json"
      via: "single finance signal selection"
      pattern: "LEDGER"
---

<objective>
Refine the board briefing into the locked one-screen Phase 6 executive contract.

Purpose: Deliver D-13 through D-17 while preserving the existing derivative workflow, provenance chain, and latest/history render pattern.
Output: Updated board-brief generator and regenerated latest/history board briefing artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-execution-handoff-and-team-readiness/06-CONTEXT.md
@.planning/phases/06-execution-handoff-and-team-readiness/06-RESEARCH.md
@.planning/phases/06-execution-handoff-and-team-readiness/06-PATTERNS.md
@.planning/phases/06-execution-handoff-and-team-readiness/06-01-PLAN.md
@scripts/derive_board_briefing.py
@assets/shared/decision_packages/OPERATING_DECISION_PACKAGE.md
@assets/shared/governance/GOVERNANCE_STATUS.md
@assets/shared/LEDGER.json
@assets/shared/trace/decision_package_trace.json

<interfaces>
From scripts/derive_board_briefing.py:
```python
OPERATING_DECISION_PACKAGE_PATH = DECISION_PACKAGES_DIR / "OPERATING_DECISION_PACKAGE.md"
DECISION_TRACE_PATH = TRACE_DIR / "decision_package_trace.json"

def extract_line_after_heading(content: str, heading: str) -> str: ...
def extract_bullets(content: str, heading: str) -> list[str]: ...
def render_board_briefing(date_value: str, operating_package: str, trace_content: str) -> str: ...
```

From assets/shared/governance/GOVERNANCE_STATUS.md:
```markdown
## Pending Approvals
- ...

## Active Blocks
- ...
```

From assets/shared/LEDGER.json:
```json
{"treasury": 500, "maturity_level": "Bootstrapping", "status": "growth"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rework board-brief rendering to exact single-signal sections</name>
  <read_first>
    scripts/derive_board_briefing.py
    tests/test_derived_packages.py
    assets/shared/decision_packages/OPERATING_DECISION_PACKAGE.md
    assets/shared/governance/GOVERNANCE_STATUS.md
    assets/shared/LEDGER.json
    .planning/phases/06-execution-handoff-and-team-readiness/06-CONTEXT.md
  </read_first>
  <files>tests/test_derived_packages.py, scripts/derive_board_briefing.py</files>
  <behavior>
    - Test 1: Dry-run output renders `## Governance Signal`, `## Risk Signal`, `## Finance Signal`, and `## Required Attention` exactly once each.
    - Test 2: Legacy sections `## Top 3` and `## Key Numbers / Signals` are gone.
    - Test 3: The finance line comes from `LEDGER.json`, and the governance line comes from `GOVERNANCE_STATUS.md`, while the conclusion/risk/attention remain derived from the operating decision package.
  </behavior>
  <action>Modify `tests/test_derived_packages.py` and `scripts/derive_board_briefing.py` together so the new board-brief contract is implemented and immediately verified in the same task. Following the structurally parallel generator pattern mapped in `.planning/phases/06-execution-handoff-and-team-readiness/06-PATTERNS.md` from `scripts/derive_execution_package.py`, and the compatibility-first downstream stance already documented there for `scripts/generate_operating_visibility.py`, activate exact board assertions in `tests/test_derived_packages.py` that require exactly one item under `## Governance Signal`, `## Risk Signal`, `## Finance Signal`, and `## Required Attention`, forbid legacy headings `## Top 3` and `## Key Numbers / Signals`, and keep the artifact one-screen by enforcing no repeated category sections and no more than one bullet in each required section. Also activate the execution risk-to-acceptance pairing helper path only if that helper still needs board-side coverage. In `scripts/derive_board_briefing.py`, keep it a one-screen derivative but expand its read-only inputs to include `assets/shared/governance/GOVERNANCE_STATUS.md` for the highest-priority governance signal and `assets/shared/LEDGER.json` for one finance signal, matching the resolved finance-source and visibility-compatibility decisions in `06-RESEARCH.md`. Keep the operating package and `decision_package_trace.json` as the primary provenance pair. Render a compact header with `Derived From`, `Source Trace`, and `Conclusion`, then exactly these four sections: `## Governance Signal`, `## Risk Signal`, `## Finance Signal`, `## Required Attention`. Select only one item for each section: prefer an active block over a pending approval for governance; use the first risk summary from `## 主要风险` for risk; render finance as a single concise line containing treasury, maturity level, and status from `LEDGER.json`; render required attention from the first bullet in `## 推荐下一步`. Do not reintroduce ranking tables, multi-item signal lists, or any larger board packet content. Implement D-16 and D-17 explicitly by preserving the current solo-operator visibility loop and avoiding any new team workflow headings, routing states, or assignment mechanics; this phase should only make the board artifact structurally consistent with future collaboration growth, not change the operating loop into a team coordination surface.</action>
  <acceptance_criteria>
    - `scripts/derive_board_briefing.py` contains the exact strings `## Governance Signal`, `## Finance Signal`, and `## Required Attention`.
    - `scripts/derive_board_briefing.py` no longer contains `## Top 3` or `## Key Numbers / Signals`.
    - `scripts/derive_board_briefing.py` reads `assets/shared/governance/GOVERNANCE_STATUS.md` and `assets/shared/LEDGER.json` or equivalent constants.
    - `scripts/derive_board_briefing.py` still keeps `Derived From` and `Source Trace` headers tied to the operating package and trace file.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest discover -s C:/Users/42236/Desktop/dev/profit-corp-hermes/tests -p test_derived_packages.py -v</automated>
  </verify>
  <done>The board generator emits a one-screen output with exactly one item per locked signal category and preserved provenance.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate and verify the board briefing through the existing cron-friendly path</name>
  <read_first>
    scripts/derive_board_briefing.py
    assets/shared/board_briefings/BOARD_BRIEFING.md
    assets/shared/board_briefings/history/2026-04-25-board-briefing.md
    scripts/smoke_test_pipeline.sh
    orchestration/cron/commands.sh
  </read_first>
  <files>assets/shared/board_briefings/BOARD_BRIEFING.md, assets/shared/board_briefings/history/2026-04-25-board-briefing.md</files>
  <action>Run `python scripts/derive_board_briefing.py --date 2026-04-25` so both the latest artifact and `assets/shared/board_briefings/history/2026-04-25-board-briefing.md` are regenerated with the new contract. Keep the existing cron-first flow untouched per the wiring pattern mapped in `.planning/phases/06-execution-handoff-and-team-readiness/06-PATTERNS.md` from `orchestration/cron/commands.sh`, and reserve the full repo unittest suite plus `bash scripts/smoke_test_pipeline.sh` for the plan-level or phase-level gate instead of this task-level verify. The task-level verify should stay fast while still proving the refined board output regenerates cleanly through the existing script-owned path. If the broader gate later reveals downstream visibility contract drift, treat that as a separate follow-up planning decision rather than silently expanding this task's edit scope.</action>
  <acceptance_criteria>
    - `assets/shared/board_briefings/BOARD_BRIEFING.md` contains the exact headings `## Governance Signal`, `## Risk Signal`, `## Finance Signal`, and `## Required Attention`.
    - `assets/shared/board_briefings/BOARD_BRIEFING.md` does not contain `## Top 3` or `## Key Numbers / Signals`.
    - `assets/shared/board_briefings/history/2026-04-25-board-briefing.md` exists and contains the same new headings.
    - The artifact regenerates without changing `orchestration/cron/commands.sh`.
  </acceptance_criteria>
  <verify>
    <automated>python C:/Users/42236/Desktop/dev/profit-corp-hermes/scripts/derive_board_briefing.py --date 2026-04-25 && python -m unittest discover -s C:/Users/42236/Desktop/dev/profit-corp-hermes/tests -p test_derived_packages.py -v</automated>
  </verify>
  <done>The board briefing latest/history artifacts are regenerated through the existing script path, and the fast task-level contract test remains green. Full repo tests plus smoke are deferred to the wave/phase gate.</done>
</task>

<task type="auto">
  <name>Task 3: Run the downstream compatibility gate and apply compatibility-only fixes if needed</name>
  <read_first>
    scripts/generate_operating_visibility.py
    tests/test_generate_operating_visibility.py
    scripts/smoke_test_pipeline.sh
    orchestration/cron/commands.sh
    .planning/phases/06-execution-handoff-and-team-readiness/06-RESEARCH.md
    .planning/phases/06-execution-handoff-and-team-readiness/06-PATTERNS.md
  </read_first>
  <files>scripts/generate_operating_visibility.py, tests/test_generate_operating_visibility.py</files>
  <action>After Task 2 regenerates the board briefing, run the explicit downstream compatibility gate required by `06-RESEARCH.md`, `06-PATTERNS.md`, and `06-VALIDATION.md`: `python -m unittest discover -s C:/Users/42236/Desktop/dev/profit-corp-hermes/tests -p "test_*.py" -v` followed by `bash C:/Users/42236/Desktop/dev/profit-corp-hermes/scripts/smoke_test_pipeline.sh`. If those broader checks reveal that the new execution/board contracts broke the supporting-view consumer path, make compatibility-only edits to `scripts/generate_operating_visibility.py` and/or `tests/test_generate_operating_visibility.py` so visibility keeps reading execution and board artifacts correctly without expanding Phase 6 into new visibility features or collaboration workflow scope. Follow the supporting-view compatibility pattern mapped in `.planning/phases/06-execution-handoff-and-team-readiness/06-PATTERNS.md`: preserve visibility as a downstream consumer, keep its trusted-artifact/provenance model intact, and only adjust headings, section parsing, or compatibility assertions needed to restore the existing cron/smoke path.</action>
  <acceptance_criteria>
    - `python -m unittest discover -s C:/Users/42236/Desktop/dev/profit-corp-hermes/tests -p "test_*.py" -v` exits 0 after board/execution contract changes.
    - `bash C:/Users/42236/Desktop/dev/profit-corp-hermes/scripts/smoke_test_pipeline.sh` exits 0 after board/execution contract changes.
    - Any edits to `scripts/generate_operating_visibility.py` remain compatibility-only and do not add new visibility sections, new collaboration workflow behavior, or new primary data sources.
    - `tests/test_generate_operating_visibility.py` reflects only the compatibility assertions needed for the updated downstream contract, if changes were required.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest discover -s C:/Users/42236/Desktop/dev/profit-corp-hermes/tests -p "test_*.py" -v && bash C:/Users/42236/Desktop/dev/profit-corp-hermes/scripts/smoke_test_pipeline.sh</automated>
  </verify>
  <done>The broader compatibility gate has been executed explicitly, and any downstream visibility drift caused by Phase 6 contract changes has been repaired without scope expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operating package + trace -> board generator | Primary decision conclusions are transformed into a compact executive derivative |
| governance status + ledger -> board signal overlays | Read-only operational overlays influence surfaced governance and finance signals |
| generator -> board briefing outputs | Writes must remain inside allowed latest/history board directories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | T | `scripts/derive_board_briefing.py` write path | mitigate | Preserve allowlisted write directories and keep regeneration script-owned. |
| T-06-08 | R | board-brief provenance | mitigate | Keep `Derived From` and `Source Trace` headers anchored to the operating package and trace sidecar. |
| T-06-09 | I | governance/finance board signals | mitigate | Read only the latest governance status and ledger artifacts, surface one concise item each, and validate exact single-item section counts in tests. |
</threat_model>

<verification>
Run the strengthened Phase 6 derived-package tests after script changes and artifact regeneration. Then run the explicit downstream compatibility gate: full unittest discovery plus `bash C:/Users/42236/Desktop/dev/profit-corp-hermes/scripts/smoke_test_pipeline.sh`, with compatibility-only fixes in `scripts/generate_operating_visibility.py` and `tests/test_generate_operating_visibility.py` if downstream drift is detected.
</verification>

<success_criteria>
- `scripts/derive_board_briefing.py` emits exactly one governance, one risk, one finance, and one required-attention signal.
- Latest/history board artifacts are regenerated from the script.
- Fast task-level verification stays within the phase validation latency target.
- The explicit full-suite + smoke compatibility gate passes, and any required fixes to `scripts/generate_operating_visibility.py` / `tests/test_generate_operating_visibility.py` remain compatibility-only without changing cron wiring or phase scope.
</success_criteria>

<output>
After completion, create `.planning/phases/06-execution-handoff-and-team-readiness/06-execution-handoff-and-team-readiness-03-SUMMARY.md`
</output>
