---
phase: 06-execution-handoff-and-team-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_derived_packages.py
autonomous: true
requirements:
  - DECI-02
  - DECI-03
must_haves:
  truths:
    - "Phase 6 execution-package contract checks exist and catch drift before generator changes ship."
    - "Phase 6 board-brief contract checks exist and catch drift before regeneration changes ship."
    - "Phase 6 solo-first collaboration boundaries are enforced by exact reusable checks before downstream artifact contracts are tightened."
  artifacts:
    - path: "tests/test_derived_packages.py"
      provides: "Exact contract assertions for execution and board derived artifacts"
      contains: "CORE_9_SECTION_ORDER, board signal count checks, readiness enum checks"
  key_links:
    - from: "tests/test_derived_packages.py"
      to: "scripts/derive_execution_package.py"
      via: "subprocess dry-run and write-mode assertions"
      pattern: "run_script\(EXECUTION_SCRIPT"
    - from: "tests/test_derived_packages.py"
      to: "scripts/derive_board_briefing.py"
      via: "subprocess dry-run and write-mode assertions"
      pattern: "run_script\(BOARD_SCRIPT"
---

<objective>
Prepare non-breaking test scaffolding for Phase 6 derived-artifact contract hardening.

Purpose: Establish reusable section/order extraction helpers, provenance/latest-history checks, and solo-first boundary scaffolding in `tests/test_derived_packages.py` without asserting generator behavior that does not exist yet.
Output: A strengthened test harness that stays green on the current generators and is ready for Plan 06-02 and Plan 06-03 to add the new passing contract assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-execution-handoff-and-team-readiness/06-CONTEXT.md
@.planning/phases/06-execution-handoff-and-team-readiness/06-RESEARCH.md
@.planning/phases/06-execution-handoff-and-team-readiness/06-PATTERNS.md
@tests/test_derived_packages.py
@tests/test_generate_operating_visibility.py
@scripts/derive_execution_package.py
@scripts/derive_board_briefing.py

<interfaces>
From tests/test_derived_packages.py:
```python
class DerivedPackagesTests(unittest.TestCase):
    def run_script(self, script: Path, *args: str) -> subprocess.CompletedProcess[str]:
        ...
```

From scripts/derive_execution_package.py:
```python
def render_execution_package(date_value: str, operating_package: str, trace_content: str) -> str:
    ...
```

From scripts/derive_board_briefing.py:
```python
def render_board_briefing(date_value: str, operating_package: str, trace_content: str) -> str:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode the execution-package contract as exact test assertions</name>
  <read_first>
    tests/test_derived_packages.py
    scripts/derive_execution_package.py
    .planning/phases/06-execution-handoff-and-team-readiness/06-CONTEXT.md
    .planning/phases/06-execution-handoff-and-team-readiness/06-RESEARCH.md
  </read_first>
  <files>tests/test_derived_packages.py</files>
  <action>Refactor `tests/test_derived_packages.py` into reusable scaffolding only, following the ordered-section helper and `extract_section(...)` style mapped in `.planning/phases/06-execution-handoff-and-team-readiness/06-PATTERNS.md` from `tests/test_generate_operating_visibility.py`, plus the subprocess exact-string contract style mapped from the current `tests/test_derived_packages.py`. Add helper functions/constants for section-order checks, per-section bullet extraction, metadata-block extraction, latest/history parity checks, and banned-vocabulary scanning, but keep the concrete assertions aligned with the current generators so the suite remains green in Wave 1. Do not yet require `## Goal`, `## Scope Boundary`, `## Acceptance Gate`, `## Governance Signal`, or the `ready|blocked|needs-input` enum. Instead, prepare named helper paths that Plans 06-02 and 06-03 will use to activate those exact assertions after generator changes land. Keep solo-first boundary scaffolding limited to reusable banned-heading/banned-vocabulary helpers; do not introduce failing assertions for future collaboration fields in this wave.</action>
  <acceptance_criteria>
    - `tests/test_derived_packages.py` contains reusable helper names or constants for section-order checks and latest/history parity checks.
    - `tests/test_derived_packages.py` still passes against the current generators with `python -m unittest discover -s C:/Users/42236/Desktop/dev/profit-corp-hermes/tests -p test_derived_packages.py -v`.
    - `tests/test_derived_packages.py` does not yet require the exact future headings `## Scope Boundary`, `## Acceptance Gate`, `## Governance Signal`, or the enum values `ready|blocked|needs-input`.
    - `tests/test_derived_packages.py` contains reusable banned-vocabulary or banned-heading helpers that later plans can apply to solo-first collaboration boundary checks.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest discover -s C:/Users/42236/Desktop/dev/profit-corp-hermes/tests -p test_derived_packages.py -v</automated>
  </verify>
  <done>Wave 1 leaves `tests/test_derived_packages.py` greener and more reusable, so later execution and board contract assertions can be added in the implementation plans without blocking on future generator behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Encode board-brief and pairing rules before implementation</name>
  <read_first>
    tests/test_derived_packages.py
    scripts/derive_board_briefing.py
    .planning/phases/06-execution-handoff-and-team-readiness/06-CONTEXT.md
    .planning/phases/06-execution-handoff-and-team-readiness/06-RESEARCH.md
  </read_first>
  <files>tests/test_derived_packages.py</files>
  <action>Extend the same non-breaking scaffolding in `tests/test_derived_packages.py` so it is ready for later activation of execution risk-to-acceptance pairing checks and board single-signal checks. Add helper functions that can compare bullet counts/order between two sections, assert one-item-per-section limits, and verify latest/history file parity, but keep any concrete heading/value expectations pinned to the current outputs until Plans 06-02 and 06-03 update the generators. Follow the exact-contract testing approach mapped in `.planning/phases/06-execution-handoff-and-team-readiness/06-PATTERNS.md`, but in this wave restrict the work to scaffolding plus any current-contract assertions that remain green.</action>
  <acceptance_criteria>
    - `tests/test_derived_packages.py` contains helper logic that can later compare paired section counts/order and one-item-per-section limits.
    - `tests/test_derived_packages.py` still verifies latest and dated history files exist after write mode.
    - `tests/test_derived_packages.py` remains green with the current generators.
    - No new assertion in this task requires future board headings or future execution risk/gate sections before Plans 06-02 and 06-03 implement them.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest discover -s C:/Users/42236/Desktop/dev/profit-corp-hermes/tests -p test_derived_packages.py -v</automated>
  </verify>
  <done>Wave 1 test scaffolding now supports later execution risk/gate and board single-signal assertions without forcing future-contract failures before implementation lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operating package -> derived-artifact tests | Untrusted contract drift in generators must be caught before release |
| test suite -> cron regeneration path | Weak assertions could allow invalid artifacts to ship through the existing automation path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | `tests/test_derived_packages.py` | mitigate | Add exact heading, enum, anti-bloat, and collaboration-boundary assertions so contract tampering is detected immediately. |
| T-06-02 | R | derived artifact provenance rules | mitigate | Keep tests asserting `OPERATING_DECISION_PACKAGE.md` and trace references remain present in both outputs. |
| T-06-03 | E | board/execution artifact scope | mitigate | Add negative assertions forbidding task-board, legacy multi-signal output, and multi-user workflow expansion so downstream artifacts cannot silently expand scope. |
</threat_model>

<verification>
Run the Phase 6 derived-package unit suite and confirm it is green before editing generator scripts.
</verification>

<success_criteria>
- `tests/test_derived_packages.py` contains reusable helper scaffolding for section order, bullet extraction, banned-vocabulary checks, and latest/history parity.
- Wave 1 test changes remain green against the current generators.
- Later implementation plans can activate stricter execution and board contract assertions without rebuilding the test harness.
- The scaffolding includes reusable solo-first collaboration-boundary checks without requiring future-only headings in Wave 1.
</success_criteria>

<output>
After completion, create `.planning/phases/06-execution-handoff-and-team-readiness/06-execution-handoff-and-team-readiness-01-SUMMARY.md`
</output>
