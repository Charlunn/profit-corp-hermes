---
phase: 11-github-and-vercel-automation
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - scripts/start_approved_project_delivery.py
  - scripts/github_delivery_common.py
  - scripts/render_approved_delivery_status.py
  - scripts/validate_approved_delivery_pipeline.py
  - tests/test_phase11_github_sync.py
  - tests/test_project_delivery_pipeline_bootstrap.py
autonomous: true
requirements:
  - SHIP-01
  - SHIP-02
  - SHIP-03
  - SHIP-08
must_haves:
  truths:
    - "Hermes can create or attach a target GitHub repository from the approved-project authority bundle."
    - "Hermes syncs the generated workspace to one canonical default branch and records repository identity plus delivery-run linkage."
    - "Any GitHub tool, auth, repo, or sync failure blocks Vercel execution with durable evidence and resume state."
  artifacts:
    - path: "scripts/github_delivery_common.py"
      provides: "Constrained GitHub helper surface for repo prepare and code sync only."
    - path: "scripts/start_approved_project_delivery.py"
      provides: "Implemented `github_repository` and `github_sync` stages wired into the approved-delivery pipeline."
    - path: "scripts/validate_approved_delivery_pipeline.py"
      provides: "Cross-link validation for repo URL/name, default branch, sync evidence, and blocked GitHub states."
  key_links:
    - from: "scripts/start_approved_project_delivery.py"
      to: "scripts/github_delivery_common.py"
      via: "stage helper calls for repo preparation and canonical sync"
      pattern: "prepare_github_repository|sync_workspace_to_github"
    - from: "scripts/render_approved_delivery_status.py"
      to: "APPROVED_PROJECT.json"
      via: "latest shipping metadata rendering"
      pattern: "repository|branch|sync"
---

<objective>
Implement the GitHub half of Phase 11 so approved SaaS workspaces can be repository-prepared and canonically synced before any deployment work begins.

Purpose: Deliver SHIP-01/02/03/08 and D-01 through D-06 by extending the authority-first pipeline with deterministic repository bootstrap semantics and hard-blocked downstream behavior.
Output: A constrained GitHub helper module plus integrated repository/sync stages, status rendering, and validator coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-github-and-vercel-automation/11-CONTEXT.md
@.planning/phases/11-github-and-vercel-automation/11-RESEARCH.md
@.planning/phases/11-github-and-vercel-automation/11-01-PLAN.md
@scripts/start_approved_project_delivery.py
@scripts/render_approved_delivery_status.py
@scripts/validate_approved_delivery_pipeline.py
@tests/test_phase11_github_sync.py

<interfaces>
New contract from 11-01 should include explicit authority fields for:
```json
{
  "shipping": {
    "github": {
      "repository_mode": "create|attach",
      "repository_owner": "...",
      "repository_name": "...",
      "repository_url": "...",
      "default_branch": "...",
      "remote_name": "origin",
      "synced_commit": "...",
      "sync_evidence_path": "..."
    }
  }
}
```
Executor must use this authority metadata directly rather than inferring from `.git/config` alone.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build constrained GitHub repository preparation and sync helpers</name>
  <read_first>
    - .planning/phases/11-github-and-vercel-automation/11-CONTEXT.md
    - .planning/phases/11-github-and-vercel-automation/11-RESEARCH.md
    - tests/test_phase11_github_sync.py
  </read_first>
  <files>scripts/github_delivery_common.py, tests/test_phase11_github_sync.py</files>
  <behavior>
    - Test 1: `prepare_github_repository` supports create and attach modes per D-02.
    - Test 2: missing `gh` and missing GitHub credentials produce distinct blocked results per D-03 and D-11.
    - Test 3: canonical branch detection persists the actual default branch instead of assuming `main` per D-04.
    - Test 4: full-project bootstrap sync records remote URL, commit, branch, and evidence per D-05.
  </behavior>
  <action>Create `scripts/github_delivery_common.py` as the only GitHub helper surface for this phase. Implement small functions that validate repo identifiers, check `gh` availability, verify token/auth presence, prepare a repo in either create or attach mode, and sync the workspace to one canonical branch. Use subprocess argument arrays only; validate owner/repo/branch inputs before invocation; and return structured dict results with `ok`, `block_reason`, `evidence_path`, and metadata instead of throwing raw CLI text upward. Honor D-03 by keeping the helper surface limited to repository bootstrap and sync actions only; do not add generic GitHub passthrough commands.</action>
  <acceptance_criteria>
    - The helper module exposes narrowly scoped functions for repo prep and sync only.
    - Create versus attach mode is explicit in both return data and persisted metadata.
    - The test file passes for helper-level behavior, including missing-tool and missing-auth blocks.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_phase11_github_sync -v</automated>
  </verify>
  <done>GitHub-specific behavior is implemented behind one constrained helper layer and its tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire GitHub stages into the approved-delivery pipeline and operator surfaces</name>
  <read_first>
    - scripts/start_approved_project_delivery.py
    - scripts/render_approved_delivery_status.py
    - scripts/validate_approved_delivery_pipeline.py
    - tests/test_project_delivery_pipeline_bootstrap.py
    - tests/test_phase11_github_sync.py
  </read_first>
  <files>scripts/start_approved_project_delivery.py, scripts/render_approved_delivery_status.py, scripts/validate_approved_delivery_pipeline.py, tests/test_project_delivery_pipeline_bootstrap.py, tests/test_phase11_github_sync.py</files>
  <behavior>
    - Test 1: `delivery_run_bootstrap` now resumes into `github_repository`, then `github_sync`, before any Vercel stage.
    - Test 2: sync failure blocks `vercel_linkage` and persists evidence/resume state per D-06.
    - Test 3: status and validator surfaces show repo mode, repo URL/name, canonical branch, synced commit, and GitHub block reasons per D-04 and D-11.
  </behavior>
  <action>Update the controller so `github_repository` and `github_sync` run immediately after `delivery_run_bootstrap` and before any Vercel work. Consume approved-project authority inputs rather than ad-hoc workspace commands per D-01. Persist repo mode, repo identity, canonical branch, synced commit, and sync evidence into the authority record and append-only events per D-04. When any GitHub prerequisite or sync step fails, write a blocked state that stops the stage graph before `vercel_linkage` per D-06. Extend the latest-status renderer and pipeline validator so operators can see repository linkage and GitHub evidence without opening local git state.</action>
  <acceptance_criteria>
    - Starting or resuming the pipeline produces GitHub stage transitions in the authority event stream before Vercel stages.
    - GitHub failures produce durable block reasons and prevent downstream stage advancement.
    - Status rendering and validation prove repository identity and canonical branch linkage from authority artifacts alone.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_phase11_github_sync tests.test_project_delivery_pipeline_bootstrap -v</automated>
  </verify>
  <done>The approved-delivery pipeline can repository-prepare and sync a workspace with durable GitHub state, and GitHub failures reliably stop downstream deployment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved-project authority → GitHub helper | Repository owner/name/branch and mode selections cross into credentialed CLI execution |
| workspace filesystem → git/gh subprocesses | Local source state is promoted to a remote repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | E | scripts/github_delivery_common.py | mitigate | Expose only repo-prepare and sync functions, validate identifiers, and refuse arbitrary GitHub passthrough commands per D-03. |
| T-11-05 | T | scripts/start_approved_project_delivery.py | mitigate | Require authority-driven repo mode and explicit branch persistence so local workspace state cannot silently diverge from recorded repository state. |
| T-11-06 | R | scripts/render_approved_delivery_status.py | mitigate | Render repo URL/name, branch, commit, and sync evidence from append-only authority events so operators can audit failures and resumes. |
| T-11-07 | I | scripts/github_delivery_common.py | mitigate | Capture evidence paths and sanitized stderr/stdout summaries only; never persist raw token values or secret-bearing command strings. |
</threat_model>

<verification>
- `python -m unittest tests.test_phase11_github_sync -v`
- `python -m unittest tests.test_project_delivery_pipeline_bootstrap -v`
</verification>

<success_criteria>
- Hermes can prepare or attach a GitHub repository from the approved authority bundle.
- Hermes syncs one deterministic workspace snapshot to the recorded canonical branch.
- Repo identity, branch, commit, and GitHub failure evidence are visible in the authority record, status view, and validator.
</success_criteria>

<output>
After completion, create `.planning/phases/11-github-and-vercel-automation/11-02-SUMMARY.md`
</output>
