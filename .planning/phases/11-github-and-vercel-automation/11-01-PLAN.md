---
phase: 11-github-and-vercel-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase11_github_sync.py
  - tests/test_phase11_vercel_flow.py
  - tests/test_project_delivery_pipeline_bootstrap.py
  - tests/test_approved_delivery_pipeline_cli.py
  - scripts/start_approved_project_delivery.py
  - scripts/append_approved_delivery_event.py
autonomous: true
requirements:
  - SHIP-01
  - SHIP-02
  - SHIP-03
  - SHIP-04
  - SHIP-05
  - SHIP-06
  - SHIP-07
  - SHIP-08
must_haves:
  truths:
    - "The approved-delivery pipeline recognizes GitHub and Vercel as first-class post-bootstrap stages."
    - "Blocked GitHub/Vercel prerequisites produce durable stage-specific evidence instead of silent skips."
    - "The new shipping behaviors are locked by executable tests before implementation changes land."
  artifacts:
    - path: "tests/test_phase11_github_sync.py"
      provides: "Red/green coverage for repository preparation, canonical branch sync, and persisted repo linkage."
    - path: "tests/test_phase11_vercel_flow.py"
      provides: "Red/green coverage for Vercel linkage, environment contract enforcement, deploy gating, and outcome reporting."
    - path: "scripts/start_approved_project_delivery.py"
      provides: "Extended stage contracts, allowed block reasons, and shipping metadata placeholders for downstream implementation."
  key_links:
    - from: "tests/test_phase11_github_sync.py"
      to: "scripts/start_approved_project_delivery.py"
      via: "imported pipeline helpers and stage execution assertions"
      pattern: "github_repository|github_sync"
    - from: "tests/test_phase11_vercel_flow.py"
      to: "scripts/start_approved_project_delivery.py"
      via: "imported pipeline helpers and blocked/deploy assertions"
      pattern: "vercel_linkage|vercel_deploy"
---

<objective>
Lock the Phase 11 GitHub/Vercel stage contracts before implementation so the executor extends the Phase 10 authority-first pipeline in one deterministic direction.

Purpose: Prevent ad-hoc shipping logic and ensure D-01 through D-12 are represented as executable pipeline behavior, blocked states, and persisted metadata.
Output: New Phase 11 test files plus updated stage/event contract scaffolding in the approved-delivery controller.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-github-and-vercel-automation/11-CONTEXT.md
@.planning/phases/11-github-and-vercel-automation/11-RESEARCH.md
@.planning/phases/10-approved-project-delivery-pipeline/10-approved-project-delivery-pipeline-02-SUMMARY.md
@.planning/phases/10-approved-project-delivery-pipeline/10-approved-project-delivery-pipeline-03-SUMMARY.md
@scripts/start_approved_project_delivery.py
@tests/test_project_delivery_pipeline_bootstrap.py
@tests/test_approved_delivery_pipeline_cli.py

<interfaces>
From scripts/start_approved_project_delivery.py:
```python
PIPELINE_STAGES = [
    "approval",
    "brief_generation",
    "workspace_instantiation",
    "conformance",
    "delivery_run_bootstrap",
    "handoff",
]

def run_pipeline_from_stage(authority_path: Path, record: dict[str, Any], *, start_stage: str, workspace_root: Path | None) -> dict[str, Any]: ...
def start_approved_project_delivery(authority_record_path: Path | str, *, workspace_root: Path | None = None) -> dict[str, Any]: ...
def resume_approved_project_delivery(authority_record_path: Path | str, *, workspace_root: Path | None = None) -> dict[str, Any]: ...
def finalize_delivery_handoff(authority_record_path: Path | str) -> dict[str, Any]: ...
def block_pipeline(... ) -> dict[str, Any]: ...
def update_pipeline_state(... ) -> None: ...
def make_event(... ) -> dict[str, str]: ...
```

From scripts/append_approved_delivery_event.py:
```python
REQUIRED_EVENT_FIELDS = (
    "project_slug", "stage", "status", "action", "timestamp", "outcome",
    "authority_record_path", "brief_path", "workspace_path", "delivery_run_id",
    "artifact", "block_reason", "evidence_path", "resume_from_stage", "final_handoff_path",
)
ALLOWED_STAGES = {"approval", "brief_generation", "workspace_instantiation", "conformance", "delivery_run_bootstrap", "handoff"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 11 red tests for GitHub/Vercel pipeline stages</name>
  <read_first>
    - .planning/phases/11-github-and-vercel-automation/11-CONTEXT.md
    - .planning/phases/11-github-and-vercel-automation/11-RESEARCH.md
    - tests/test_project_delivery_pipeline_bootstrap.py
    - tests/test_approved_delivery_pipeline_cli.py
  </read_first>
  <files>tests/test_phase11_github_sync.py, tests/test_phase11_vercel_flow.py, tests/test_project_delivery_pipeline_bootstrap.py, tests/test_approved_delivery_pipeline_cli.py</files>
  <behavior>
    - Test 1: repository preparation supports both create and attach modes and persists repo URL/name/default branch/run linkage per D-02 and D-04.
    - Test 2: sync failure blocks `vercel_linkage` and `vercel_deploy` with explicit evidence and resume stage per D-06 and D-10.
    - Test 3: Vercel linkage requires one project, applies a required env contract, and distinguishes platform-level versus identity-derived env values per D-07 to D-09.
    - Test 4: deploy success/failure updates both authority-layer status and workspace-local final handoff references per D-12.
    - Test 5: missing `gh`, missing GitHub auth, missing Vercel auth, missing env values, and missing linkage each map to separate blocked states per D-11.
  </behavior>
  <action>Write failing `unittest` coverage for the four new stages `github_repository`, `github_sync`, `vercel_linkage`, and `vercel_deploy`. Extend existing bootstrap and CLI suites only where Phase 10 surfaces must now mention the new stages, wrappers, and operator-visible blocked reasons. Reference D-01 in assertions that all behavior starts from `APPROVED_PROJECT.json`; reference D-03 when locking the constrained CLI path; reference D-05 when asserting full-project bootstrap semantics on one canonical branch rather than PR-first flows. Do not implement the production logic in this task beyond the minimum constants/types needed to let imports resolve.</action>
  <acceptance_criteria>
    - `tests/test_phase11_github_sync.py` exists and contains failing coverage for create/attach, canonical branch persistence, sync evidence, and downstream hard-blocking.
    - `tests/test_phase11_vercel_flow.py` exists and contains failing coverage for project linkage, env contract enforcement, deploy gating, blocked states, and handoff reporting.
    - Existing bootstrap/CLI tests explicitly mention the new Phase 11 stages or wrappers where operator surfaces must change.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_phase11_github_sync tests.test_phase11_vercel_flow -v</automated>
  </verify>
  <done>The repo has executable failing tests that fully describe the required Phase 11 shipping behaviors before implementation starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend core stage and event contracts for Phase 11 without implementing external side effects yet</name>
  <read_first>
    - scripts/start_approved_project_delivery.py
    - scripts/append_approved_delivery_event.py
    - tests/test_phase11_github_sync.py
    - tests/test_phase11_vercel_flow.py
  </read_first>
  <files>scripts/start_approved_project_delivery.py, scripts/append_approved_delivery_event.py</files>
  <behavior>
    - Test 1: stage enums accept `github_repository`, `github_sync`, `vercel_linkage`, and `vercel_deploy` in order after `delivery_run_bootstrap`.
    - Test 2: pipeline record can persist shipping metadata sections for repo, branch, Vercel project, env evidence, deploy URL/status, and resume stage.
    - Test 3: blocked-state enums include distinct missing-tool, missing-credential, missing-linkage, sync-failed, and env-contract-failed reasons.
  </behavior>
  <action>Update shared constants, helper payload builders, and event/state scaffolding so downstream implementation can satisfy the new tests without inventing schema mid-flight. Add Phase 11 stage names after `delivery_run_bootstrap` and before `handoff`; add shipping metadata placeholders under the approved-project authority record per D-01, D-04, D-07, and D-12; extend allowed block reasons to cover missing `gh`, missing GitHub auth, sync failure, missing Vercel auth, missing Vercel project linkage, missing env contract, and deploy failure per D-06 and D-11. Keep subprocess execution out of this plan; this task is only for contracts that make the later plans deterministic.</action>
  <acceptance_criteria>
    - The controller and event validator accept the new stage names and block reasons without broad `any`-style fallback logic.
    - The authority record schema now has explicit fields for repository path used, repository identity, canonical branch, delivery-run linkage, Vercel linkage, env-contract evidence, and deploy result placeholders.
    - The new contract scaffolding turns the Phase 11 tests from import/setup failures into behavior failures awaiting implementation.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_phase11_github_sync tests.test_phase11_vercel_flow tests.test_project_delivery_pipeline_bootstrap tests.test_approved_delivery_pipeline_cli -v</automated>
  </verify>
  <done>Phase 11 contracts exist in code and tests now fail only because the GitHub/Vercel stage logic is not yet implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved-project authority → pipeline controller | Untrusted persisted metadata crosses into stage selection and external command planning |
| test fixtures → controller helpers | Synthetic repo/project/env identifiers exercise validation logic before real credentialed execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | T | scripts/start_approved_project_delivery.py | mitigate | Lock explicit stage enums, block-reason enums, and shipping metadata keys in tests so later implementation cannot silently skip validation paths. |
| T-11-02 | R | tests/test_phase11_github_sync.py | mitigate | Require append-only event and evidence assertions for sync and deploy failures so later runtime behavior remains auditable. |
| T-11-03 | E | scripts/start_approved_project_delivery.py | mitigate | Model constrained GitHub/Vercel execution paths in tests per D-03 and D-10 so later code cannot expand into arbitrary shell actions without breaking tests. |
</threat_model>

<verification>
- `python -m unittest tests.test_phase11_github_sync tests.test_phase11_vercel_flow -v`
- `python -m unittest tests.test_project_delivery_pipeline_bootstrap tests.test_approved_delivery_pipeline_cli -v`
</verification>

<success_criteria>
- Phase 11 has red/green contract tests for all SHIP requirements.
- The approved-delivery controller recognizes the full Phase 11 stage graph and shipping metadata shape.
- Later plans can implement GitHub and Vercel side effects without guessing schema, stage names, or blocked-state semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/11-github-and-vercel-automation/11-01-SUMMARY.md`
</output>
