---
phase: 11-github-and-vercel-automation
plan: 03
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - scripts/start_approved_project_delivery.py
  - scripts/vercel_delivery_common.py
  - scripts/render_approved_delivery_status.py
  - scripts/validate_approved_delivery_pipeline.py
  - orchestration/cron/commands.sh
  - docs/OPERATIONS.md
  - tests/test_phase11_vercel_flow.py
  - tests/test_approved_delivery_pipeline_cli.py
autonomous: true
requirements:
  - SHIP-04
  - SHIP-05
  - SHIP-06
  - SHIP-07
  - SHIP-08
user_setup:
  - service: github
    why: "Phase 11 requires platform-managed repository automation credentials on the execution host."
    env_vars:
      - name: GH_TOKEN
        source: "GitHub platform-managed token for repo bootstrap/sync"
  - service: vercel
    why: "Phase 11 requires non-interactive project linkage, environment sync, and deploy execution."
    env_vars:
      - name: VERCEL_TOKEN
        source: "Vercel platform-managed token"
      - name: VERCEL_TEAM
        source: "Vercel team/org identifier used by automation"
      - name: VERCEL_PROJECT
        source: "Approved target project identifier or name when linkage is expected"
must_haves:
  truths:
    - "Hermes links an approved SaaS workspace to one deployable Vercel project and records the linkage in authority artifacts."
    - "Hermes applies a declared env contract before deploy and blocks if required values are missing."
    - "Hermes triggers deployment only after GitHub sync, Vercel linkage, and env checks pass, then reports the outcome in both authority and workspace handoff surfaces."
  artifacts:
    - path: "scripts/vercel_delivery_common.py"
      provides: "Constrained Vercel helper surface for project link, env contract sync, and deploy only."
    - path: "scripts/start_approved_project_delivery.py"
      provides: "Implemented `vercel_linkage`, env-contract enforcement, `vercel_deploy`, and handoff reporting logic."
    - path: "docs/OPERATIONS.md"
      provides: "Operator-safe instructions for blocked credential/linkage/env/deploy cases and resume flow."
  key_links:
    - from: "scripts/start_approved_project_delivery.py"
      to: "scripts/vercel_delivery_common.py"
      via: "link, env sync, and deploy stage helpers"
      pattern: "link_vercel_project|apply_env_contract|deploy_to_vercel"
    - from: "scripts/start_approved_project_delivery.py"
      to: "workspace .hermes/FINAL_DELIVERY.md"
      via: "deploy outcome persistence before final handoff"
      pattern: "deploy_url|deploy_status|final_handoff"
---

<objective>
Finish Phase 11 by wiring Vercel linkage, env-contract enforcement, deployment execution, and outcome reporting into the approved-delivery pipeline.

Purpose: Deliver SHIP-04 through SHIP-08 and D-07 through D-12 while preserving the existing authority-first, blocked/resume, operator-readable workflow.
Output: A constrained Vercel helper module, integrated pipeline stages, updated wrappers/docs, and validator/status coverage for deploy outcomes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-github-and-vercel-automation/11-CONTEXT.md
@.planning/phases/11-github-and-vercel-automation/11-RESEARCH.md
@.planning/phases/11-github-and-vercel-automation/11-02-PLAN.md
@docs/OPERATIONS.md
@orchestration/cron/commands.sh
@scripts/start_approved_project_delivery.py
@scripts/render_approved_delivery_status.py
@scripts/validate_approved_delivery_pipeline.py
@tests/test_phase11_vercel_flow.py

<interfaces>
The Phase 11 authority record should expose a declared env contract similar to:
```json
{
  "shipping": {
    "vercel": {
      "project_id": "...",
      "project_name": "...",
      "team_scope": "...",
      "linked": true,
      "env_contract": {
        "platform_managed": ["..."],
        "identity_derived": {
          "APP_KEY": "...",
          "APP_NAME": "...",
          "APP_URL": "...",
          "PAYPAL_BRAND_NAME": "..."
        },
        "evidence_path": "..."
      },
      "deploy_status": "pending|ready|blocked|success|failed",
      "deploy_url": "...",
      "deploy_evidence_path": "..."
    }
  }
}
```
Keep the field names stable once implemented because renderer, validator, and handoff reporting all depend on them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build constrained Vercel linkage, env-contract, and deploy helpers</name>
  <read_first>
    - .planning/phases/11-github-and-vercel-automation/11-CONTEXT.md
    - .planning/phases/11-github-and-vercel-automation/11-RESEARCH.md
    - docs/platform/standalone-saas-template-contract.md
    - tests/test_phase11_vercel_flow.py
  </read_first>
  <files>scripts/vercel_delivery_common.py, tests/test_phase11_vercel_flow.py</files>
  <behavior>
    - Test 1: missing Vercel CLI or auth produces separate blocked states with evidence per D-11.
    - Test 2: project linkage records exactly one linked Vercel project per approved SaaS per D-07.
    - Test 3: env-contract generation separates reusable platform-managed values from identity-derived values per D-08 and D-09.
    - Test 4: deployment only runs after GitHub sync and env-contract checks have passed per D-10.
  </behavior>
  <action>Create `scripts/vercel_delivery_common.py` as the only Vercel helper surface for this phase, using `vercel` if installed or `npx vercel@latest` fallback when needed. Implement functions that validate project/team identifiers, verify token/auth availability, link one project, generate/apply a declared env contract, and trigger deploy while returning structured metadata and evidence paths. Derive identity values from the approved bundle and template contract per D-09; require platform-managed secrets to arrive via declared inputs rather than dashboard assumptions per D-08. Keep the helper surface limited to link/env/deploy actions only.</action>
  <acceptance_criteria>
    - The helper module distinguishes missing CLI, missing auth, missing project linkage, and missing env contract as separate failure modes.
    - Env-contract output explicitly separates platform-managed variables from approved-project identity values.
    - Helper-level Vercel tests pass without depending on manual dashboard steps.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_phase11_vercel_flow -v</automated>
  </verify>
  <done>Vercel-specific behaviors are implemented behind one constrained helper surface and validated by passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Vercel stages, deploy outcome reporting, wrappers, and runbooks</name>
  <read_first>
    - scripts/start_approved_project_delivery.py
    - scripts/render_approved_delivery_status.py
    - scripts/validate_approved_delivery_pipeline.py
    - orchestration/cron/commands.sh
    - docs/OPERATIONS.md
    - tests/test_approved_delivery_pipeline_cli.py
    - tests/test_phase11_vercel_flow.py
  </read_first>
  <files>scripts/start_approved_project_delivery.py, scripts/render_approved_delivery_status.py, scripts/validate_approved_delivery_pipeline.py, orchestration/cron/commands.sh, docs/OPERATIONS.md, tests/test_approved_delivery_pipeline_cli.py, tests/test_phase11_vercel_flow.py</files>
  <behavior>
    - Test 1: `vercel_linkage` and `vercel_deploy` run only after successful GitHub sync per D-10.
    - Test 2: status and validator surfaces show Vercel project linkage, env-contract evidence, deploy URL/status, and blocked reasons per D-11.
    - Test 3: deployment outcomes are written into both authority-layer artifacts and workspace-local handoff artifacts per D-12.
  </behavior>
  <action>Integrate `vercel_linkage` and `vercel_deploy` into the approved-delivery stage graph after `github_sync`. Persist Vercel project identity, env-contract evidence, deploy URL/status, and deploy evidence into the authority record, append-only events, rendered status view, validator checks, and final handoff references. Update `orchestration/cron/commands.sh` and `docs/OPERATIONS.md` only where operator-visible start/status/validate/resume behavior or blocked prerequisite wording must reflect the new GitHub/Vercel flow. Honor D-10 by refusing deployment when GitHub sync, Vercel linkage, or env requirements are incomplete, and honor D-12 by updating both the authority surface and workspace-local handoff artifacts with deploy outcomes.</action>
  <acceptance_criteria>
    - The stage graph now completes `github_repository -> github_sync -> vercel_linkage -> vercel_deploy -> handoff` with durable resume points.
    - Operators can inspect blocked credential, linkage, env, and deploy failures from the standard status and validation surfaces without reading hidden CLI state.
    - Successful deploys persist deploy URL/status into both top-level approved-project artifacts and workspace-local handoff output.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_phase11_vercel_flow tests.test_approved_delivery_pipeline_cli -v</automated>
  </verify>
  <done>Phase 11 Vercel automation, deploy gating, and outcome reporting are implemented end to end and visible through the existing operator workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved-project authority → Vercel helper | Project/team/env identifiers and deployment state cross into credentialed platform commands |
| platform-managed secrets → persisted artifacts | Sensitive env values must be used without leaking into authority or workspace evidence |
| authority pipeline → workspace handoff artifacts | Deploy outcomes must remain consistent across both visibility surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | I | scripts/vercel_delivery_common.py | mitigate | Persist env names and evidence paths only; never write raw secret values into authority events, status views, or handoff markdown. |
| T-11-09 | T | scripts/start_approved_project_delivery.py | mitigate | Gate `vercel_deploy` on successful GitHub sync, explicit project linkage, and env-contract completion before any deploy command is issued. |
| T-11-10 | R | scripts/validate_approved_delivery_pipeline.py | mitigate | Cross-check authority, event stream, rendered status, and final handoff for Vercel project/deploy fields so failures and successes remain auditable. |
| T-11-11 | E | orchestration/cron/commands.sh | mitigate | Keep operator entrypoints on the existing wrapper surface and do not add generic Vercel shell passthrough commands. |
</threat_model>

<verification>
- `python -m unittest tests.test_phase11_vercel_flow -v`
- `python -m unittest tests.test_approved_delivery_pipeline_cli -v`
- `python -m unittest discover -s tests -p 'test_*.py' -v`
</verification>

<success_criteria>
- Hermes can link one Vercel project, apply a declared env contract, and trigger deploy only when prerequisites pass.
- Missing credentials, linkage, or env values produce durable blocked states with evidence and resume points.
- Deployment outcomes appear in both authority-layer status artifacts and workspace-local handoff artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/11-github-and-vercel-automation/11-03-SUMMARY.md`
</output>
