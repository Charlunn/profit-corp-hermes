---
phase: 15-github-sync-reliability-on-real-workspaces
plan: 03
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
files_modified:
  - tests/test_phase11_github_sync.py
  - tests/test_project_delivery_pipeline_bootstrap.py
  - tests/test_project_delivery_pipeline_resume.py
  - tests/test_phase12_credential_governance.py
autonomous: true
requirements:
  - GHSYNC-05
must_haves:
  truths:
    - "Regression coverage proves GitHub sync reliability on the Windows + pnpm failure shapes that broke the live operator path."
    - "Bootstrap and resume flows preserve the refined GitHub sync evidence and authority metadata without breaking governed-action audit/event contracts."
    - "Governance artifacts still distinguish sync failure boundaries so operators can see the true blocked step."
  artifacts:
    - path: "tests/test_phase11_github_sync.py"
      provides: "End-to-end helper regression coverage for filtered staging, remote mismatch, transport behavior, and step-specific failure evidence."
    - path: "tests/test_project_delivery_pipeline_bootstrap.py"
      provides: "Fresh-run authority/event assertions for the new sync metadata and failure-boundary evidence."
    - path: "tests/test_project_delivery_pipeline_resume.py"
      provides: "Resume-path assertions proving sync metadata continuity after retry/recovery."
    - path: "tests/test_phase12_credential_governance.py"
      provides: "Governed-action audit/event contract checks for refined sync failure outputs."
  key_links:
    - from: "tests/test_project_delivery_pipeline_bootstrap.py"
      to: "scripts/start_approved_project_delivery.py"
      via: "authority persistence assertions for shipping.github sync metadata"
      pattern: "shipping\['github'\]|sync_evidence_path|last_sync_status"
    - from: "tests/test_project_delivery_pipeline_resume.py"
      to: "scripts/start_approved_project_delivery.py"
      via: "resume continuity assertions across github_sync stage"
      pattern: "resume_from_stage|github_sync|shipping\['github'\]"
    - from: "tests/test_phase12_credential_governance.py"
      to: "scripts/approved_delivery_governance.py"
      via: "audit/event schema assertions for github_sync failures"
      pattern: "github_sync|audit|event|block_reason"
---

<objective>
Lock Phase 15 reliability fixes behind regression validation that exercises helper, controller, resume, and governance seams against the real failure signatures.

Purpose: Prevent regressions once snapshot filtering, remote convergence, and transport hardening land by proving the operator-path evidence and persistence contracts stay intact.
Output: Expanded automated regression coverage across helper, bootstrap, resume, and governance suites aligned to the Phase 15 validation strategy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-github-sync-reliability-on-real-workspaces/15-RESEARCH.md
@.planning/phases/15-github-sync-reliability-on-real-workspaces/15-VALIDATION.md
@tests/test_phase11_github_sync.py
@tests/test_project_delivery_pipeline_bootstrap.py
@tests/test_project_delivery_pipeline_resume.py
@tests/test_phase12_credential_governance.py
@scripts/start_approved_project_delivery.py
@scripts/approved_delivery_governance.py

<interfaces>
From scripts/start_approved_project_delivery.py:
```python
def run_pipeline_from_stage(authority_path: Path, record: dict[str, Any], *, start_stage: str, workspace_root: Path | None) -> dict[str, Any]: ...
def block_pipeline(...) -> dict[str, Any]: ...
def run_github_sync(workspace_path: Path | str, github_record: dict[str, Any]) -> dict[str, Any]: ...
```

From scripts/approved_delivery_governance.py:
```python
def run_governed_action(*, action, authority_record_path, stage, helper: Helper, **kwargs): ...
def _build_audit_payload(... ) -> dict[str, Any]: ...
def _append_event(... ) -> None: ...
```

Validation contract from 15-VALIDATION.md:
```text
Quick run: python -m unittest tests.test_phase11_github_sync
Full suite: python -m unittest tests.test_phase11_github_sync tests.test_project_delivery_pipeline_bootstrap tests.test_project_delivery_pipeline_resume tests.test_phase12_credential_governance
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand helper regression coverage to mirror live Windows and remote failure signatures</name>
  <read_first>
    - .planning/phases/15-github-sync-reliability-on-real-workspaces/15-VALIDATION.md
    - tests/test_phase11_github_sync.py
    - scripts/github_delivery_common.py
  </read_first>
  <files>tests/test_phase11_github_sync.py</files>
  <behavior>
    - Test 1: helper passes the combined filter + remote + transport scenarios expected after Plans 15-01 and 15-02.
    - Test 2: helper fails with step-specific evidence when remote convergence, commit, or push boundaries break.
    - Test 3: long-path-prone generated trees remain excluded in the fully hardened sync path.
  </behavior>
  <action>Consolidate Phase 15 helper regression coverage into scenario-oriented tests that model the real operator workspace shape and failure boundaries from research/validation. Keep the tests fast and deterministic by staying at the command-stub seam. Require assertions that the final helper evidence shape contains the fields added in Plans 15-01 and 15-02 so future regressions cannot collapse back to a generic sync failure.</action>
  <acceptance_criteria>
    - `tests/test_phase11_github_sync.py` covers the combined hardened sync path, not only isolated sub-behaviors.
    - Tests assert granular evidence for failure boundaries and retained snapshot exclusions.
    - The quick validation command remains the default smoke test after each task commit.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_phase11_github_sync -v</automated>
  </verify>
  <done>Helper-level regressions now encode the real Phase 15 sync contract end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify bootstrap and resume authority continuity for refined sync metadata</name>
  <read_first>
    - tests/test_project_delivery_pipeline_bootstrap.py
    - tests/test_project_delivery_pipeline_resume.py
    - scripts/start_approved_project_delivery.py
    - .planning/phases/15-github-sync-reliability-on-real-workspaces/15-VALIDATION.md
  </read_first>
  <files>tests/test_project_delivery_pipeline_bootstrap.py, tests/test_project_delivery_pipeline_resume.py</files>
  <behavior>
    - Test 1: bootstrap flow persists the new GitHub sync metadata/evidence fields into `shipping.github` without breaking stage progression.
    - Test 2: resume flow preserves or converges those fields correctly after a blocked `github_sync` retry.
    - Test 3: downstream stage gating still reflects true sync success/failure boundaries rather than generic blocked state.
  </behavior>
  <action>Add controller-level regression assertions for fresh-run and resume-run flows that consume the refined helper outputs from Plans 15-01 and 15-02. Validate only the persistence/event continuity needed for Phase 15: authority metadata, resume stage, and downstream blocking semantics. Do not redesign status rendering truth precedence; keep validation focused on metadata continuity and stage correctness.</action>
  <acceptance_criteria>
    - Bootstrap tests assert persisted sync metadata/evidence fields in `shipping.github`.
    - Resume tests prove retry continuity after `github_sync` metadata changes.
    - Existing stage ordering and downstream blocking expectations remain intact.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_project_delivery_pipeline_bootstrap tests.test_project_delivery_pipeline_resume -v</automated>
  </verify>
  <done>Pipeline integration coverage proves the new sync behavior survives fresh runs and resumes without metadata drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify governed audit/event compatibility for refined sync failures</name>
  <read_first>
    - tests/test_phase12_credential_governance.py
    - scripts/approved_delivery_governance.py
    - .planning/phases/15-github-sync-reliability-on-real-workspaces/15-VALIDATION.md
  </read_first>
  <files>tests/test_phase12_credential_governance.py</files>
  <behavior>
    - Test 1: governed `github_sync` audits preserve explicit reasons and evidence when helper failures are step-specific.
    - Test 2: append-only events keep the true sync failure boundary visible to operators.
    - Test 3: the targeted full-suite validation remains green with Phase 15 evidence refinements.
  </behavior>
  <action>Extend governance-contract tests so refined sync outputs from Phase 15 continue to fit the existing audit/event envelope. Assert that step-specific `github_sync` failure reasons and evidence propagate through audit payloads and append-only events without flattening back to a generic message. Keep the governance layer pattern intact; this task validates compatibility rather than inventing a new schema.</action>
  <acceptance_criteria>
    - Governance tests explicitly cover step-specific `github_sync` failure reasons and evidence paths.
    - Audit/event assertions remain aligned with the current governed-action contract.
    - The targeted full suite validates Phase 15 without requiring manual interpretation.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_phase11_github_sync tests.test_project_delivery_pipeline_bootstrap tests.test_project_delivery_pipeline_resume tests.test_phase12_credential_governance -v</automated>
  </verify>
  <done>Phase 15 regression validation now proves helper, controller, and governance seams all preserve the true GitHub sync failure boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper result payloads → governance audit/event artifacts | Structured sync evidence crosses into operator-visible governance records |
| controller persistence → authority/resume state | Refined sync metadata becomes the source of truth for retries and downstream stage gating |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | R | tests/test_phase12_credential_governance.py + governance artifacts | mitigate | Lock audit/event assertions to the refined sync failure reasons and evidence so high-severity ambiguity is blocked by regression tests. |
| T-15-08 | T | tests/test_project_delivery_pipeline_bootstrap.py and resume suite | mitigate | Assert authority metadata continuity across bootstrap and resume so sync evidence cannot be silently dropped or overwritten incorrectly. |
| T-15-09 | D | validation contract for real failure signatures | mitigate | Require the targeted full suite from `15-VALIDATION.md` to pass before Phase 15 is considered complete, blocking regressions that reintroduce operator-path sync failures. |
</threat_model>

<verification>
- `python -m unittest tests.test_phase11_github_sync -v`
- `python -m unittest tests.test_project_delivery_pipeline_bootstrap tests.test_project_delivery_pipeline_resume -v`
- `python -m unittest tests.test_phase11_github_sync tests.test_project_delivery_pipeline_bootstrap tests.test_project_delivery_pipeline_resume tests.test_phase12_credential_governance -v`
</verification>

<success_criteria>
- Phase 15 has automated regression coverage for the real sync failure shapes called out in roadmap, research, and validation.
- Authority persistence and resume semantics remain stable after the sync helper becomes more explicit.
- Governed audit/event artifacts preserve the true sync failure boundary for operators.
</success_criteria>

<output>
After completion, create `.planning/phases/15-github-sync-reliability-on-real-workspaces/15-03-SUMMARY.md`
</output>
