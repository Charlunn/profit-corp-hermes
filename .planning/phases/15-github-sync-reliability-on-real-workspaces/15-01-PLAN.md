---
phase: 15-github-sync-reliability-on-real-workspaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase11_github_sync.py
  - scripts/github_delivery_common.py
autonomous: true
requirements:
  - GHSYNC-01
  - GHSYNC-02
must_haves:
  truths:
    - "GitHub sync excludes generated dependency/build trees from the canonical snapshot instead of staging the full workspace."
    - "GitHub sync can stage the real Windows + pnpm workspace without tripping on dependency-tree long paths."
    - "Successful sync evidence records which snapshot filters were applied so the operator can audit what was intentionally excluded."
  artifacts:
    - path: "tests/test_phase11_github_sync.py"
      provides: "Red and green regression coverage for canonical snapshot filtering, excluded generated paths, and Windows path-heavy staging behavior."
    - path: "scripts/github_delivery_common.py"
      provides: "Filtered snapshot staging helpers and sync evidence fields for canonical workspace commits."
  key_links:
    - from: "tests/test_phase11_github_sync.py"
      to: "scripts/github_delivery_common.py"
      via: "helper-level assertions for filtered staging and evidence payloads"
      pattern: "sync_workspace_to_github|github-sync.json|git add"
    - from: "scripts/github_delivery_common.py"
      to: ".hermes/github-sync.json"
      via: "sync success/failure evidence write"
      pattern: "snapshot|excluded|evidence"
---

<objective>
Define and enforce the canonical GitHub sync snapshot for real generated workspaces so delivery stages only source artifacts and avoids Windows long-path failures caused by staging dependency/build trees.

Purpose: Fix the highest-probability live sync failure boundary first by replacing whole-workspace staging with governed canonical snapshot behavior.
Output: Tested helper changes that filter generated paths, preserve the existing governed-action/evidence flow, and make snapshot behavior explicit in helper evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-github-sync-reliability-on-real-workspaces/15-RESEARCH.md
@.planning/phases/15-github-sync-reliability-on-real-workspaces/15-VALIDATION.md
@scripts/github_delivery_common.py
@tests/test_phase11_github_sync.py

<interfaces>
From scripts/github_delivery_common.py:
```python
def sync_workspace_to_github(
    *,
    workspace_path: Path | str,
    repository_url: str,
    default_branch: str,
    remote_name: str = "origin",
    runner: Runner | None = None,
) -> dict[str, Any]: ...

def _blocked(workspace: Path, evidence_name: str, block_reason: str, message: str, **extra: Any) -> dict[str, Any]: ...
```

Current staging seam in scripts/github_delivery_common.py:
```python
add_result = run_git("git", "add", "-A")
if int(getattr(add_result, "returncode", 1)) != 0:
    return _blocked(
        workspace,
        "github-sync.json",
        "github_sync_failed",
        "Failed to stage workspace snapshot.",
        stderr_summary=_safe_summary(getattr(add_result, "stderr", "")),
    )
```

From tests/test_phase11_github_sync.py:
```python
class Phase11GithubSyncTests(unittest.TestCase):
    def test_sync_workspace_to_github_persists_detected_default_branch_instead_of_assuming_main(self) -> None: ...
    def test_full_project_bootstrap_sync_records_remote_url_commit_branch_and_evidence(self) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red tests for canonical workspace snapshot filtering</name>
  <read_first>
    - .planning/phases/15-github-sync-reliability-on-real-workspaces/15-RESEARCH.md
    - .planning/phases/15-github-sync-reliability-on-real-workspaces/15-VALIDATION.md
    - tests/test_phase11_github_sync.py
    - scripts/github_delivery_common.py
  </read_first>
  <files>tests/test_phase11_github_sync.py</files>
  <behavior>
    - Test 1: `node_modules`, `.next`, `dist`, and equivalent generated directories are excluded from the canonical sync snapshot per GHSYNC-01.
    - Test 2: a path-heavy Windows + pnpm workspace stages only included source paths and does not attempt broad `git add -A` per GHSYNC-02.
    - Test 3: helper evidence records the applied snapshot filter contract and excluded path categories.
  </behavior>
  <action>Write failing helper-level regression tests that codify the canonical snapshot contract per GHSYNC-01 and GHSYNC-02. Reproduce the live failure shape by seeding generated dependency/build trees and asserting sync stages explicit included paths or pathspecs rather than the whole workspace. Require evidence assertions for snapshot filter details in `.hermes/github-sync.json`. Stay inside the helper seam; do not change repository targeting, Vercel behavior, or Phase 17 status semantics.</action>
  <acceptance_criteria>
    - `tests/test_phase11_github_sync.py` contains failing coverage for excluded generated trees and Windows path-heavy workspaces.
    - Tests explicitly reject a raw whole-workspace `git add -A` canonical path.
    - Tests require helper evidence fields describing the snapshot policy or exclusions.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_phase11_github_sync -v</automated>
  </verify>
  <done>Red tests prove that the current sync helper still stages too broadly and lacks snapshot evidence, establishing the Phase 15-01 contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement filtered snapshot staging and evidence in the sync helper</name>
  <read_first>
    - scripts/github_delivery_common.py
    - tests/test_phase11_github_sync.py
    - .planning/phases/15-github-sync-reliability-on-real-workspaces/15-RESEARCH.md
  </read_first>
  <files>scripts/github_delivery_common.py, tests/test_phase11_github_sync.py</files>
  <behavior>
    - Test 1: helper derives a deterministic canonical source snapshot and excludes generated directories per GHSYNC-01.
    - Test 2: helper avoids staging dependency/build trees that trigger long-path failure in the real Windows + pnpm workspace shape per GHSYNC-02.
    - Test 3: success and failure evidence both surface snapshot-filter details without changing the governed result envelope.
  </behavior>
  <action>Refactor `sync_workspace_to_github()` so staging is performed through explicit snapshot-selection helpers instead of `git add -A`. Encode a deterministic exclude contract that covers the generated trees called out in requirements/research, and keep the implementation in `scripts/github_delivery_common.py` so governed action, blocked-result, and audit/event patterns remain unchanged. Record snapshot-related evidence fields such as included/excluded categories, staging mode, or snapshot policy summary in `github-sync.json`. Do not drift into remote convergence, transport fallback, or authority/status redesign in this plan.</action>
  <acceptance_criteria>
    - Helper tests pass for canonical snapshot exclusions and Windows path-heavy staging.
    - `scripts/github_delivery_common.py` no longer relies on whole-workspace staging as the canonical sync path.
    - Evidence written by the helper makes snapshot filtering operator-auditable while preserving the current blocked-result structure.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_phase11_github_sync -v</automated>
  </verify>
  <done>GitHub sync now commits only the governed source snapshot needed for delivery and avoids the live generated-tree staging failure mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated workspace filesystem → git staging helper | Untrusted/generated files cross into the canonical commit boundary and can leak machine-local artifacts or trigger path failures |
| sync helper → helper evidence JSON | Operators rely on helper evidence to understand what was staged or excluded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | I | scripts/github_delivery_common.py snapshot staging | mitigate | Block on over-broad staging by replacing `git add -A` with tested canonical filters that exclude generated dependency/build trees before commit creation. |
| T-15-02 | D | scripts/github_delivery_common.py staging path | mitigate | Use deterministic filtered staging so Windows long-path dependency trees are not traversed as part of the canonical snapshot path. |
| T-15-03 | R | .hermes/github-sync.json | mitigate | Persist snapshot policy/exclusion evidence so operators can verify why files were omitted and high-severity ambiguity is eliminated before execution continues. |
</threat_model>

<verification>
- `python -m unittest tests.test_phase11_github_sync -v`
- Confirm the helper evidence file contains snapshot-filter fields after a passing sync test fixture.
- Use the Phase 15 validation sampling rule: rerun `python -m unittest tests.test_phase11_github_sync` after each task commit.
</verification>

<success_criteria>
- Generated dependency/build directories are no longer part of the canonical delivery commit.
- Windows + pnpm path-heavy workspaces no longer fail solely because generated trees are staged.
- Helper evidence explains the snapshot contract without changing the governed action envelope.
</success_criteria>

<output>
After completion, create `.planning/phases/15-github-sync-reliability-on-real-workspaces/15-01-SUMMARY.md`
</output>
