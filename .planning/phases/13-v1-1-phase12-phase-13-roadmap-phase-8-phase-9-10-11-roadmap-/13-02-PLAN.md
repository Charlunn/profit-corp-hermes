---
phase: 13-v1-1-phase12-phase-13-roadmap-phase-8-phase-9-10-11-roadmap-
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - .planning/phases/11-github-and-vercel-automation/11-HUMAN-UAT.md
  - .planning/phases/11-github-and-vercel-automation/11-VERIFICATION.md
autonomous: false
requirements:
  - SHIP-01
  - SHIP-02
  - SHIP-03
  - SHIP-04
  - SHIP-05
  - SHIP-06
  - SHIP-07
  - SHIP-08
user_setup:
  - service: github
    why: "真实 GitHub bootstrap/sync UAT 需要有效 CLI 与平台托管凭证"
    env_vars:
      - name: GH_TOKEN
        source: "GitHub 平台托管凭证或执行主机已登录的 gh auth 上下文"
    dashboard_config:
      - task: "准备一个允许创建/连接仓库的目标组织或仓库命名空间"
        location: "GitHub organization/user settings"
  - service: vercel
    why: "真实 Vercel link/env/deploy UAT 需要有效 CLI 与项目/团队访问权"
    env_vars:
      - name: VERCEL_TOKEN
        source: "Vercel 平台托管凭证或执行主机已登录的 vercel auth 上下文"
      - name: VERCEL_TEAM
        source: "Vercel dashboard team slug / scope"
      - name: VERCEL_PROJECT
        source: "目标 Vercel project 名称或可创建目标"
    dashboard_config:
      - task: "确认目标 team/project 可被 link、写 env、触发 deploy"
        location: "Vercel Dashboard"
must_haves:
  truths:
    - "`11-HUMAN-UAT.md` 中三个 live checks 不再是 pending，而是带证据路径的 passed 或 blocked。"
    - "`11-VERIFICATION.md` 不再停留在 `human_needed`；它会反映真实 live UAT 结论。"
    - "如果 GitHub/Vercel CLI、认证或目标资源不可用，阻塞原因会被明确记录，而不会被误报为通过。"
  artifacts:
    - path: ".planning/phases/11-github-and-vercel-automation/11-HUMAN-UAT.md"
      provides: "Real GitHub/Vercel/operator live UAT result log with evidence paths"
      contains: "Real GitHub bootstrap/sync"
    - path: ".planning/phases/11-github-and-vercel-automation/11-VERIFICATION.md"
      provides: "Final Phase 11 pass-or-blocked verification verdict"
      contains: "## Goal Verdict"
  key_links:
    - from: ".planning/phases/11-github-and-vercel-automation/11-HUMAN-UAT.md"
      to: ".planning/phases/11-github-and-vercel-automation/11-VERIFICATION.md"
      via: "live UAT verdict promotion"
      pattern: "passed|blocked"
    - from: "gh / git / vercel execution evidence"
      to: ".planning/phases/11-github-and-vercel-automation/11-HUMAN-UAT.md"
      via: "recorded command outputs, repo URL, deploy URL, artifact paths"
      pattern: "github|vercel|deploy"
---

<objective>
完成 Phase 11 剩余的真实外部系统验收：执行 GitHub bootstrap/sync、Vercel link/env/deploy、以及 operator artifact usability 三项 live checks，并把结果写回正式闭环工件。

Purpose: 让 SHIP-01..SHIP-08 从“代码已实现、真人验收待做”推进到真正通过，或在凭证/工具/目标资源缺失时形成显式 blocked 证据。
Output: 已更新的 `11-HUMAN-UAT.md` 与 `11-VERIFICATION.md`，其中包含 preflight 结果、live evidence 路径、以及最终 pass/blocked 判定。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-v1-1-phase12-phase-13-roadmap-phase-8-phase-9-10-11-roadmap-/13-RESEARCH.md
@.planning/phases/10-approved-project-delivery-pipeline/10-VERIFICATION.md
@.planning/phases/11-github-and-vercel-automation/11-HUMAN-UAT.md
@.planning/phases/11-github-and-vercel-automation/11-VERIFICATION.md
@.planning/phases/12-credential-governance-and-operator-handoff/12-VERIFICATION.md
@orchestration/cron/commands.sh
@docs/OPERATIONS.md

<interfaces>
Execution prerequisites confirmed by research:
```text
gh CLI: missing on this machine; must exist before real GitHub UAT
vercel CLI: use `npx vercel@latest` fallback when global CLI is absent
git: available
automated preflight suite:
python -m unittest tests.test_phase11_github_sync tests.test_phase11_vercel_flow tests.test_project_delivery_pipeline_bootstrap tests.test_approved_delivery_pipeline_cli -v
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 执行 Phase 11 live UAT preflight 并刷新自动化基线</name>
  <files>.planning/phases/11-github-and-vercel-automation/11-HUMAN-UAT.md, .planning/phases/11-github-and-vercel-automation/11-VERIFICATION.md</files>
  <action>先做严格 preflight：检查 `gh --version`、`git --version`、`npx vercel@latest --version`，并确认当前执行主机对 GitHub / Vercel 的 auth 状态可用；随后重跑 Phase 11 已记录的自动化 suite：`python -m unittest tests.test_phase11_github_sync tests.test_phase11_vercel_flow tests.test_project_delivery_pipeline_bootstrap tests.test_approved_delivery_pipeline_cli -v`。把 preflight 结果写入 `11-HUMAN-UAT.md` 顶部或每个 live check 的 prerequisite/blocker 区域；如果任一前提缺失，只能记为 blocked，不得继续冒充 live pass。</action>
  <verify>
    <automated>cd /c/Users/42236/Desktop/dev/profit-corp-hermes && gh --version && git --version && npx vercel@latest --version && python -m unittest tests.test_phase11_github_sync tests.test_phase11_vercel_flow tests.test_project_delivery_pipeline_bootstrap tests.test_approved_delivery_pipeline_cli -v</automated>
  </verify>
  <done>执行者在真正 live UAT 前已确认工具链、认证、自动化回归是否满足；所有缺失前提都已在工件中显式记录为 blocker。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: 完成真实外部系统所需的人类认证或目标资源授权</name>
  <action>暂停自动执行，等待用户补齐 live UAT 无法由 Claude 直接创建的外部前提：GitHub CLI 安装与登录、Vercel 登录或 token/scope、以及本次真实 repo/project 目标访问权。恢复后继续沿用 Task 1 已记录的 blocker 清单，不新增绕过治理边界的手工替代路径。</action>
  <verify>
    <automated>gh --version && gh auth status && npx vercel@latest whoami</automated>
  </verify>
  <done>执行主机已具备 GitHub/Vercel live UAT 所需的 CLI 与认证上下文，或阻塞原因已被用户明确确认并记录。</done>
  <what-built>Claude 已完成 preflight、识别缺失的 GitHub/Vercel CLI 或 auth 前提，并把 blocker 写入 `11-HUMAN-UAT.md` 草稿位置。</what-built>
  <how-to-verify>
    1. 若 `gh` 未安装，先在执行主机安装 GitHub CLI，并确认 `gh --version` 可用。
    2. 完成 `gh auth login` 或提供可用的 `GH_TOKEN`，确保能访问目标 repo/organization。
    3. 完成 Vercel 登录或提供 `VERCEL_TOKEN`、`VERCEL_TEAM`、`VERCEL_PROJECT`，确保目标 project 可 link / 写 env / deploy。
    4. 准备本次 live UAT 使用的真实 repo / branch / Vercel project 目标，并确认这些目标符合平台托管凭证边界。
  </how-to-verify>
  <resume-signal>回复“已完成 live UAT 前提”或提供仍然存在的 blocker。</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 执行三项 live checks 并把 Phase 11 更新为最终通过或显式阻塞</name>
  <files>.planning/phases/11-github-and-vercel-automation/11-HUMAN-UAT.md, .planning/phases/11-github-and-vercel-automation/11-VERIFICATION.md</files>
  <action>按 `11-HUMAN-UAT.md` 现有三项 checklist 原样执行真实 GitHub bootstrap/sync、真实 Vercel link/env/deploy、以及 operator artifact usability review。对每一项都补全：执行时间、实际命令或调用入口、repo URL / branch / commit / deploy URL / status artifact 路径等证据路径，以及 `passed` 或 `blocked` 结果。然后更新 `11-VERIFICATION.md`：若三项都通过，把 `Goal Verdict` 从 `human_needed` 改为 `passed`，并把 `Deferred Human Verification` 替换为已完成 live evidence；若任一项因真实前提缺失或真实外部失败未通过，则把文件改为明确 `blocked` 或 `partial` 状态，逐条列出 blocker 和后续恢复入口。不要保留模糊的 `pending` 文案。</action>
  <verify>
    <automated>cd /c/Users/42236/Desktop/dev/profit-corp-hermes && python - <<'PY'
from pathlib import Path
for rel in [
    '.planning/phases/11-github-and-vercel-automation/11-HUMAN-UAT.md',
    '.planning/phases/11-github-and-vercel-automation/11-VERIFICATION.md',
]:
    text = Path(rel).read_text(encoding='utf-8')
    assert 'result: pending' not in text, rel
print('phase11 live UAT pending markers cleared')
PY</automated>
  </verify>
  <done>`11-HUMAN-UAT.md` 三项 live checks 都有真实结论和证据路径；`11-VERIFICATION.md` 已从 human_needed 收口为最终 pass 或显式 blocked。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local execution host → GitHub/Vercel live systems | 真实凭证与真实部署目标在此边界被使用 |
| live command outputs → verification markdown | 外部系统结果被转录为闭环文档，必须避免遗漏或失真 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | S | GitHub/Vercel live auth | mitigate | 先做 CLI/auth preflight；任何未认证状态一律写成 blocked，不允许跳过后直接标记通过。 |
| T-13-05 | I | `11-HUMAN-UAT.md` evidence | mitigate | 只记录 repo URL、branch、artifact path、deploy URL、status 等证据，不写入原始 token 或秘密值。 |
| T-13-06 | R | `11-VERIFICATION.md` verdict | mitigate | 最终 pass/blocked 必须直接引用三项 live check 结果，不能只沿用旧的 automated-only 结论。 |
| T-13-07 | E | live deploy execution path | mitigate | 仅使用已有 approved-delivery / governed command surface 和既有工件链路，避免绕过 Phase 12 治理边界手工改状态。 |
</threat_model>

<verification>
- `cd /c/Users/42236/Desktop/dev/profit-corp-hermes && gh --version && git --version && npx vercel@latest --version`
- `cd /c/Users/42236/Desktop/dev/profit-corp-hermes && python -m unittest tests.test_phase11_github_sync tests.test_phase11_vercel_flow tests.test_project_delivery_pipeline_bootstrap tests.test_approved_delivery_pipeline_cli -v`
- `cd /c/Users/42236/Desktop/dev/profit-corp-hermes && python - <<'PY'
from pathlib import Path
uat = Path('.planning/phases/11-github-and-vercel-automation/11-HUMAN-UAT.md').read_text(encoding='utf-8')
ver = Path('.planning/phases/11-github-and-vercel-automation/11-VERIFICATION.md').read_text(encoding='utf-8')
assert 'result: pending' not in uat
assert 'human_needed' not in ver
print('phase11 closure files finalized')
PY`
</verification>

<success_criteria>
- 三项 live UAT 均被真实执行并记录结果，或被真实 blocker 明确阻止并记录。
- `11-HUMAN-UAT.md` 不再存在 pending 条目。
- `11-VERIFICATION.md` 的最终状态与 live UAT 真实结果完全一致。
</success_criteria>

<output>
After completion, create `.planning/phases/13-v1-1-phase12-phase-13-roadmap-phase-8-phase-9-10-11-roadmap-/13-02-SUMMARY.md`
</output>
