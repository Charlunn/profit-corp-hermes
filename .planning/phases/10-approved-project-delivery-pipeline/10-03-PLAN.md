---
phase: 10-approved-project-delivery-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - scripts/validate_approved_delivery_pipeline.py
  - orchestration/cron/commands.sh
  - assets/workspaces/ceo/AGENTS.md
  - docs/OPERATIONS.md
  - tests/test_approved_delivery_pipeline_cli.py
autonomous: true
requirements:
  - PIPE-03
  - PIPE-05
  - PIPE-06
  - PIPE-07
must_haves:
  truths:
    - "Operators can trigger, inspect, validate, and resume the approved-project pipeline through the existing orchestrator-first command surface"
    - "Phase 10 can prove the approved-project authority, workspace linkage, Phase 9 handoff, final operator-facing handoff artifact, and their persisted links are cross-linked correctly"
    - "The operator has one concise, reliable workflow for blocked-state recovery instead of restarting from scratch"
  artifacts:
    - path: "scripts/validate_approved_delivery_pipeline.py"
      provides: "Phase 10 cross-link validator for authority record, brief, workspace, handoff, and status artifacts"
      exports: ["validate_approved_delivery_pipeline"]
    - path: "tests/test_approved_delivery_pipeline_cli.py"
      provides: "CLI and wrapper coverage for start/render/validate/resume commands"
    - path: "orchestration/cron/commands.sh"
      provides: "approved-delivery command wrappers"
    - path: "docs/OPERATIONS.md"
      provides: "operator runbook for Phase 10 start/block/resume flow"
  key_links:
    - from: "orchestration/cron/commands.sh"
      to: "scripts/start_approved_project_delivery.py"
      via: "start-approved-delivery/resume-approved-delivery wrappers"
      pattern: "start-approved-delivery|resume-approved-delivery"
    - from: "scripts/validate_approved_delivery_pipeline.py"
      to: "assets/shared/approved-projects/<project>/APPROVED_PROJECT.json"
      via: "cross-link validation"
      pattern: "APPROVED_PROJECT\.json"
    - from: "scripts/validate_approved_delivery_pipeline.py"
      to: ".hermes/FINAL_DELIVERY.md"
      via: "final handoff validation"
      pattern: "FINAL_DELIVERY\.md"
---

<objective>
Expose the Phase 10 pipeline through existing operator entrypoints and add the final validation layer that proves handoff completeness.

Purpose: Finish the approved-project pipeline with operator-safe command wrappers, CEO guidance, and a validator that confirms cross-links from approval through final delivery artifact.
Output: Approved-delivery CLI wrappers, operator/CEO instructions, and a Phase 10 validator with automated CLI coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-approved-project-delivery-pipeline/10-CONTEXT.md
@.planning/phases/10-approved-project-delivery-pipeline/10-VALIDATION.md
@.planning/phases/10-approved-project-delivery-pipeline/10-PATTERNS.md
@.planning/phases/10-approved-project-delivery-pipeline/10-02-PLAN.md
@docs/OPERATIONS.md
@orchestration/cron/commands.sh
@assets/workspaces/ceo/AGENTS.md
@scripts/validate_delivery_handoff.py

<interfaces>
From orchestration/cron/commands.sh:
```bash
start_delivery_run() {
  if [ "${1:-}" = "--help" ]; then
    python "$ROOT_DIR/scripts/start_delivery_run.py" --help
    return 0
  fi
  [ "$#" -ge 1 ] || { echo "Usage: bash orchestration/cron/commands.sh start-delivery-run <workspace-path>"; return 1; }
  python "$ROOT_DIR/scripts/start_delivery_run.py" --workspace-path "$1"
}
```

From scripts/validate_delivery_handoff.py pattern:
```python
def require_in_order(text: str, headings: list[str], label: str) -> None: ...
def validate_delivery_run(workspace: Path) -> dict[str, Any]: ...
```

From Phase 10 validation:
```text
Exit criteria require approved-project record, canonical brief, governed workspace,
passed conformance, started delivery run, and persisted latest-view status.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the Phase 10 validator and command surface with CLI tests</name>
  <read_first>
    - orchestration/cron/commands.sh
    - docs/OPERATIONS.md
    - assets/workspaces/ceo/AGENTS.md
    - scripts/validate_delivery_handoff.py
    - .planning/phases/10-approved-project-delivery-pipeline/10-VALIDATION.md
  </read_first>
  <files>tests/test_approved_delivery_pipeline_cli.py</files>
  <behavior>
    - Test 1: wrapper commands expose `start-approved-delivery`, `render-approved-delivery-status`, `validate-approved-delivery-pipeline`, and `resume-approved-delivery` with `--help` passthrough and arity guards per D-03.
    - Test 2: `validate_approved_delivery_pipeline.py` fails when any required artifact/link is missing: approved-project record, brief, event stream, status view, workspace path, conformance evidence, Phase 9 delivery run manifest, final handoff artifact, or persisted handoff path/link on the approved-project authority record per PIPE-05, PIPE-06, and PIPE-07.
    - Test 3: validation passes only when the top-level approved-project bundle and workspace-local Phase 9 artifacts are cross-linked consistently, the approved-project authority layer records the final handoff artifact path/link, the pipeline event stream includes a final `handoff` event, and the latest view renders that handoff linkage for operators.
    - Test 4: operator instructions and CEO delegation text reference start → inspect status → resolve block → resume, not restart, per D-15, including blocked downstream prerequisite handling when credential/deployment preconditions are missing.
  </behavior>
  <action>Create a red-first unittest module that exercises the new validator and the shell-wrapper/help surface through subprocess calls. Use temp approved-project fixtures plus temp workspace `.hermes` artifacts to assert both failing and passing validation states. Include fixture cases where `FINAL_DELIVERY.md` exists but the approved-project authority record does not yet store its path/link, where the latest view omits the handoff link, and where downstream prerequisite evidence is missing so validation must report a blocked-state linkage gap instead of only a missing file. Include string assertions on `docs/OPERATIONS.md` and `assets/workspaces/ceo/AGENTS.md` so the operator path is locked to the orchestrator-first pattern and resumable block handling rather than ad-hoc manual recovery. Treat the final handoff chain here as a validation contract over artifacts produced by Plan 10-02, not as the place that creates those links.</action>
  <verify>
    <automated>python -m unittest tests.test_approved_delivery_pipeline_cli -v</automated>
  </verify>
  <acceptance_criteria>CLI tests fail first, then define the exact wrapper, validator, final handoff linkage, and runbook behavior operators need to manage Phase 10 safely.</acceptance_criteria>
  <done>Executable tests lock the final command-entry, blocked-state reporting, and handoff-proof contract for Phase 10.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the validator plus approved-delivery command wrappers and runbooks</name>
  <read_first>
    - .planning/phases/10-approved-project-delivery-pipeline/10-02-PLAN.md
    - scripts/validate_delivery_handoff.py
    - orchestration/cron/commands.sh
    - docs/OPERATIONS.md
    - assets/workspaces/ceo/AGENTS.md
  </read_first>
  <files>scripts/validate_approved_delivery_pipeline.py, orchestration/cron/commands.sh, docs/OPERATIONS.md, assets/workspaces/ceo/AGENTS.md</files>
  <behavior>
    - Test 1: the validator reuses ordered-section and replay cross-check patterns from `validate_delivery_handoff.py` instead of inventing a separate proof model.
    - Test 2: command wrappers call the Phase 10 scripts directly and preserve the existing help/usage/dispatch style per D-03.
    - Test 3: operations/CEO docs describe the approved-project authority layer above the workspace layer and tell operators to inspect persisted block reasons before resume per D-10 through D-15.
    - Test 4: validation requires the approved-project authority layer to record the final handoff artifact path/link, requires `approved-delivery-events.jsonl` to contain the terminal `handoff` event, and requires `DELIVERY_PIPELINE_STATUS.md` to render that same handoff linkage so PIPE-07 is produced and wired rather than merely observed.
  </behavior>
  <action>Create `scripts/validate_approved_delivery_pipeline.py` to assert all required Phase 10 cross-links: `APPROVED_PROJECT.json`, `PROJECT_BRIEF.md`, `approved-delivery-events.jsonl`, `DELIVERY_PIPELINE_STATUS.md`, linked workspace path, conformance evidence, Phase 9 `delivery-run-manifest.json`, final handoff artifact, and the authority-record field that stores the final handoff artifact path/link. Reuse the ordered-heading/token checks and replay-artifact validation style from `validate_delivery_handoff.py`. The validator must also confirm that the latest `handoff` pipeline event references the same final handoff artifact path and that `DELIVERY_PIPELINE_STATUS.md` renders the final handoff link for operators, closing the PIPE-07 chain explicitly. Treat persisted blocked downstream prerequisite states from missing credential/deployment preconditions as required validation surfaces for PIPE-06: validation should fail when the authority record, event stream, or latest view drops that block reason/evidence link, but do not add a new safety system beyond the existing Phase 10/Phase 9 artifacts. Extend `orchestration/cron/commands.sh` with wrappers for `start-approved-delivery`, `render-approved-delivery-status`, `validate-approved-delivery-pipeline`, and `resume-approved-delivery`, keeping the existing usage/help/case-dispatch pattern. Update `docs/OPERATIONS.md` with the operator runbook and `assets/workspaces/ceo/AGENTS.md` with a CEO delegation example that starts from the approved-project bundle, enforces hard blocks, and resumes from persisted state instead of restarting. Keep this plan responsible for proving the chain, while Plan 10-02 remains responsible for writing the handoff record/event/status links in production code.</action>
  <verify>
    <automated>python -m unittest tests.test_approved_delivery_pipeline_cli -v && python scripts/validate_approved_delivery_pipeline.py --help && bash orchestration/cron/commands.sh start-approved-delivery --help && bash orchestration/cron/commands.sh render-approved-delivery-status --help && bash orchestration/cron/commands.sh validate-approved-delivery-pipeline --help && bash orchestration/cron/commands.sh resume-approved-delivery --help</automated>
  </verify>
  <acceptance_criteria>Operators and the CEO profile can trigger, inspect, validate, and resume approved-project delivery entirely through the existing wrapper/runbook style, and Phase 10 explicitly proves final handoff linkage plus downstream prerequisite block propagation.</acceptance_criteria>
  <done>Phase 10 ends with a validated operator-facing control surface and final handoff proof path that covers PIPE-07 and PIPE-06 without introducing any new service or UI plane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell wrapper → Phase 10 scripts | command-line inputs invoke state-mutating pipeline operations |
| Phase 10 validator → approved-project/workspace artifacts | validator decides whether handoff is trustworthy based on file evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | S | command wrappers | mitigate | keep strict arity checks and explicit subcommand mapping so wrappers cannot silently target the wrong script |
| T-10-14 | T | `DELIVERY_PIPELINE_STATUS.md` latest view | mitigate | validate against the JSONL authority stream and other artifacts instead of trusting markdown alone |
| T-10-15 | R | operator handoff completeness | mitigate | require cross-linked approved-project, workspace, conformance, final delivery artifacts, and persisted handoff path/link before validation passes |
| T-10-16 | I | operator runbook | accept | docs expose operational paths and artifact locations but no secrets; low sensitivity |
| T-10-17 | D | failed resume attempts | mitigate | document and expose resume command that consumes persisted state instead of forcing a destructive restart |
| T-10-18 | E | bypassing orchestrator-first control plane | mitigate | expose only approved wrapper commands and CEO delegation guidance, not a second service/UI path, per D-03 |
</threat_model>

<verification>
- `python -m unittest tests.test_approved_delivery_pipeline_cli -v`
- `python scripts/validate_approved_delivery_pipeline.py --help`
- `bash orchestration/cron/commands.sh start-approved-delivery --help`
- `bash orchestration/cron/commands.sh render-approved-delivery-status --help`
- `bash orchestration/cron/commands.sh validate-approved-delivery-pipeline --help`
- `bash orchestration/cron/commands.sh resume-approved-delivery --help`
</verification>

<success_criteria>
- Operators can start, inspect, validate, and resume the approved-delivery pipeline through existing repo entrypoints.
- Validation proves the approval-to-handoff chain is complete, cross-linked, and includes the persisted final handoff path/link on the approved-project authority layer.
- Final operator-facing handoff requirements and downstream prerequisite blocked-state visibility are covered without introducing Phase 11/12 scope.
</success_criteria>

<output>
After completion, create `.planning/phases/10-approved-project-delivery-pipeline/10-approved-project-delivery-pipeline-03-SUMMARY.md`
</output>
