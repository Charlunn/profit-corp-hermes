---
phase: 08-shared-supabase-backend-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/shared/templates/standalone-saas-template.json
  - docs/platform/standalone-saas-template-contract.md
  - scripts/instantiate_template_project.py
  - tests/test_template_registry.py
  - tests/test_template_contract.py
  - tests/test_instantiate_template_project.py
autonomous: true
requirements:
  - BACK-01
  - BACK-04
  - BACK-05
must_haves:
  truths:
    - "Hermes 权威契约明确声明默认复用单一共享 Supabase，而不是为每个 SaaS 生成独立后端。"
    - "生成工作区在交付前自带可读的共享后端护栏元数据，而不是只靠口头约定。"
    - "共享全局表边界与受保护 auth/payment/entitlement 路径可被后续校验器直接读取。"
  artifacts:
    - path: "docs/platform/standalone-saas-template-contract.md"
      provides: "共享 Supabase、共享表边界、受保护流程复用规则"
      contains: "single shared Supabase"
    - path: "assets/shared/templates/standalone-saas-template.json"
      provides: "模板注册表中的共享后端治理字段"
      contains: "shared backend"
    - path: "scripts/instantiate_template_project.py"
      provides: "实例化时写入 .hermes 护栏元数据"
      exports: ["parse_args", "instantiate_workspace"]
    - path: "tests/test_instantiate_template_project.py"
      provides: "实例化与护栏元数据测试锁定"
      contains: "shared"
  key_links:
    - from: "assets/shared/templates/standalone-saas-template.json"
      to: "docs/platform/standalone-saas-template-contract.md"
      via: "canonical_contract / governance.contract_path"
      pattern: "standalone-saas-template-contract\.md"
    - from: "scripts/instantiate_template_project.py"
      to: ".hermes/shared-backend-guardrails.json"
      via: "workspace bootstrap write"
      pattern: "shared-backend-guardrails"
    - from: "tests/test_instantiate_template_project.py"
      to: "scripts/instantiate_template_project.py"
      via: "CLI fixture assertions"
      pattern: "run_script"
---

<objective>
把共享 Supabase 平台规则先固化成权威契约、注册表字段和实例化产物，让后续校验器有明确输入而不是推测行为。

Purpose: 先把“什么算合规的共享后端工作区”写成 Hermes 可消费的真相源，避免 Phase 8 只加扫描规则却没有稳定 authority artifact。
Output: 更新后的平台契约与注册表、实例化阶段生成的共享后端护栏元数据、锁定这些规则的 unittest。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-shared-supabase-backend-guardrails/08-RESEARCH.md
@.planning/phases/08-shared-supabase-backend-guardrails/08-PATTERNS.md
@.planning/phases/07-template-assetization-and-platform-contract/07-01-SUMMARY.md
@.planning/phases/07-template-assetization-and-platform-contract/07-02-SUMMARY.md
@assets/shared/templates/standalone-saas-template.json
@docs/platform/standalone-saas-template-contract.md
@scripts/instantiate_template_project.py
@tests/test_template_registry.py
@tests/test_template_contract.py
@tests/test_instantiate_template_project.py

<interfaces>
From scripts/instantiate_template_project.py:
```python
DEFAULT_ASSET_ID = "standalone-saas-template"
DEFAULT_CONTRACT_PATH = ROOT_DIR / "docs" / "platform" / "standalone-saas-template-contract.md"

def build_metadata(asset: dict, workspace_name: str, identity: dict[str, str], template_source_path: Path) -> dict[str, str]:
    ...

def write_hermes_handoff(workspace: Path, metadata: dict[str, str]) -> None:
    ...

def instantiate_workspace(template_source: Path, workspace_root: Path, workspace: Path, identity: dict[str, str], metadata: dict[str, str]) -> None:
    ...
```

From assets/shared/templates/standalone-saas-template.json:
```json
{
  "asset_id": "standalone-saas-template",
  "canonical_contract": "docs/platform/standalone-saas-template-contract.md",
  "source": { "repo_path": "C:/Users/42236/Desktop/standalone-saas-template" },
  "governance": { "status": "canonical", "owner": "Hermes platform" }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展共享后端权威契约与注册表真相源</name>
  <files>assets/shared/templates/standalone-saas-template.json, docs/platform/standalone-saas-template-contract.md, tests/test_template_registry.py, tests/test_template_contract.py</files>
  <behavior>
    - Test 1: 契约明确声明默认单一共享 Supabase 模型，并禁止默认新建独立后端（BACK-01）。
    - Test 2: 契约把共享全局表边界锁定为 users/orders/payments/subscriptions，且产品业务表必须使用 APP_KEY_ 前缀（BACK-04）。
    - Test 3: 契约和注册表都明确要求复用共享 auth/payment/entitlement 流程与受保护路径（BACK-05）。
  </behavior>
  <action>先补测试，再更新 `tests/test_template_registry.py` 与 `tests/test_template_contract.py`，锁定新增共享后端治理文本和字段；随后修改 `assets/shared/templates/standalone-saas-template.json` 与 `docs/platform/standalone-saas-template-contract.md`，把“单一共享 Supabase、四张共享表边界、产品表 APP_KEY_ 约束、共享 auth/payment/entitlement 复用规则、客户端不得直接写共享支付/权益状态”的 authority 规则写成可测试文本。不要新建平行 contract 文件，复用现有 `standalone-saas-template-contract.md`，符合 Phase 7 模式。</action>
  <verify>
    <automated>cd /c/Users/42236/Desktop/dev/profit-corp-hermes && python -m unittest tests.test_template_registry tests.test_template_contract</automated>
  </verify>
  <done>注册表与权威契约都可直接表达 BACK-01/BACK-04/BACK-05 的治理边界，相关 unittest 用精确断言锁定新增字段、章节和关键语句。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在实例化阶段写入共享后端护栏元数据</name>
  <files>scripts/instantiate_template_project.py, tests/test_instantiate_template_project.py</files>
  <behavior>
    - Test 1: 新工作区生成后包含 `.hermes/shared-backend-guardrails.json` 护栏元数据文件，记录 shared backend mode、app_key、allowed shared tables、protected paths（BACK-01/BACK-04/BACK-05）。
    - Test 2: dry-run 不写入任何工作区文件，但仍能展示即将应用的 shared backend 护栏摘要。
    - Test 3: 护栏元数据与 `.hermes/project-metadata.json` 中的 app_key / canonical contract path 保持一致，避免后续校验器读到冲突来源。
  </behavior>
  <action>先扩展 `tests/test_instantiate_template_project.py`，再修改 `scripts/instantiate_template_project.py`：在现有 `build_metadata()` / `write_hermes_handoff()` / `instantiate_workspace()` 流程内追加一个显式的共享后端护栏写入步骤，输出到 `.hermes/` 目录；内容至少包含 `backend_model=shared-supabase`、`app_key`、允许的共享全局表、受保护 Supabase/auth/payment/entitlement 路径，以及指向 canonical contract 的引用。保持现有 CLI 形状、路径安全保护和 newline normalization，不要引入交互式参数，也不要改成新脚本。</action>
  <verify>
    <automated>cd /c/Users/42236/Desktop/dev/profit-corp-hermes && python -m unittest tests.test_instantiate_template_project</automated>
  </verify>
  <done>模板实例化后，工作区自带稳定的 `.hermes` 共享后端护栏元数据，后续 conformance gate 可直接消费；dry-run 和安全路径行为仍保持 Phase 7 约束。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hermes authority artifacts → generated workspace | 受治理规则从仓库文档/注册表进入新工作区，若写错会把错误规则扩散到所有 SaaS |
| Generated workspace `.hermes` metadata → later validators | 后续校验器信任这些元数据作为 shared-backend 输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T | `docs/platform/standalone-saas-template-contract.md` | mitigate | 用精确 unittest 锁定单一共享 Supabase、共享表边界与受保护流程文本，防止契约静默漂移。 |
| T-08-02 | T | `assets/shared/templates/standalone-saas-template.json` | mitigate | 在注册表测试中断言 canonical contract 与 shared backend 字段，避免错误 registry 让后续脚本读取到错误后端模式。 |
| T-08-03 | R | `scripts/instantiate_template_project.py` | mitigate | 把 shared-backend guardrails 持久化到 `.hermes` 文件，而不是只在 stdout 输出，确保后续可审计。 |
| T-08-04 | E | generated workspace bootstrap | mitigate | 继续复用 `ensure_allowed_workspace_path()`，避免实例化阶段把护栏文件写到受控目录之外。 |
</threat_model>

<verification>
- `cd /c/Users/42236/Desktop/dev/profit-corp-hermes && python -m unittest tests.test_template_registry tests.test_template_contract`
- `cd /c/Users/42236/Desktop/dev/profit-corp-hermes && python -m unittest tests.test_instantiate_template_project`
</verification>

<success_criteria>
- 权威契约、注册表、实例化产物三者对共享 Supabase 模型表达一致。
- 新工作区在未运行 conformance gate 前，就已经具备机器可读的 shared-backend 护栏元数据。
- BACK-01、BACK-04、BACK-05 的 authority inputs 已可被测试锁定。
</success_criteria>

<output>
After completion, create `.planning/phases/08-shared-supabase-backend-guardrails/08-01-SUMMARY.md`
</output>
