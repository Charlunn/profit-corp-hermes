---
phase: 04-governance-and-control-layer
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - scripts/governance_common.py
  - scripts/enforce_governed_action.py
  - docs/STATE_CONTRACT.md
  - docs/OPERATIONS.md
  - scripts/smoke_test_pipeline.sh
  - assets/shared/governance/GOVERNANCE_STATUS.md
  - assets/shared/governance/governance_events.jsonl
autonomous: true
requirements:
  - GOV-01
  - GOV-02
must_haves:
  truths:
    - "写权限矩阵会变成可执行检查，而不是只停留在文档里。"
    - "CEO fallback takeover 会留下结构化治理事件，且原主写者、原因、目标资产都可追溯。"
    - "治理层不会取代 manage_finance.py，而是先校验、再调用 authoritative writer。"
  artifacts:
    - path: "scripts/governance_common.py"
      provides: "写权限矩阵与 fallback/override 执行规则"
    - path: "assets/shared/governance/governance_events.jsonl"
      provides: "state/finance/fallback 治理留痕"
    - path: "scripts/smoke_test_pipeline.sh"
      provides: "Phase 4 governance smoke coverage"
  key_links:
    - from: "scripts/enforce_governed_action.py"
      to: "assets/shared/manage_finance.py"
      via: "approved-only subprocess path"
      pattern: "manage_finance.py"
    - from: "scripts/governance_common.py"
      to: "docs/STATE_CONTRACT.md"
      via: "encoded role ownership + approval gates"
      pattern: "LEDGER.json|PAIN_POINTS.md|MARKET_PLAN.md|TECH_SPEC.md"
---

<objective>
把治理层真正接到现有状态与财务约束上：写权限矩阵执行化、fallback 接管留痕化、smoke 验证自动化。

Purpose: 兑现 Phase 4 的最后一段价值——不仅能写治理事件，而且能把治理事件和当前 repo 的状态契约、财务入口、失败恢复流程真正连起来。
Output: 可执行写权限规则、fallback/override 约束、finance/state 接入、Phase 4 smoke coverage。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-governance-and-control-layer/04-CONTEXT.md
@.planning/phases/04-governance-and-control-layer/04-RESEARCH.md
@.planning/phases/04-governance-and-control-layer/04-PATTERNS.md
@.planning/phases/04-governance-and-control-layer/04-01-PLAN.md
@.planning/phases/04-governance-and-control-layer/04-02-PLAN.md
@docs/STATE_CONTRACT.md
@docs/OPERATIONS.md
@assets/shared/manage_finance.py
@skills/common/run_accountant_audit.md
@scripts/smoke_test_pipeline.sh

<interfaces>
Write permission matrix to encode:
```text
LEDGER.json -> manage_finance.py only
PAIN_POINTS.md -> scout (ceo fallback)
MARKET_PLAN.md -> cmo (ceo fallback)
TECH_SPEC.md -> arch (ceo fallback)
external_intelligence artifacts -> collector workflow (ceo fallback for summaries only)
```

Failure/stop semantics to preserve:
```text
若任一步失败：
- 不推进到下一步
- 记录失败原因
- 由 CEO 决定重试或降级执行
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 把写权限矩阵与 fallback 规则编码成可执行校验</name>
  <read_first>.planning/phases/04-governance-and-control-layer/04-RESEARCH.md, .planning/phases/04-governance-and-control-layer/04-PATTERNS.md, docs/STATE_CONTRACT.md, docs/OPERATIONS.md</read_first>
  <files>scripts/governance_common.py, scripts/enforce_governed_action.py, assets/shared/governance/governance_events.jsonl</files>
  <action>在 `scripts/governance_common.py` 中固化 write-permission matrix 与 fallback 规则表：每个 target asset 绑定 primary writer、allowed fallback actor、是否需要 governance approval、以及是否属于 finance/state-transition/high-impact action。`scripts/enforce_governed_action.py` 在执行前必须根据 target path + actor 验证权限；若 CEO 以 fallback 身份接管 scout/cmo/arch 的主写资产，则必须要求 `fallback.takeover.*` 类型治理动作已被 approve 或 override，并在 event 中记录 `primary_writer`、`approved_by`、`override_reason`/`reason`。对 `LEDGER.json` 或 finance action，包装器只能调用 `manage_finance.py`，不得出现任何 direct JSON write fallback，也不应把治理逻辑内联进 `manage_finance.py` 本身。</action>
  <acceptance_criteria>`scripts/governance_common.py` 可 grep 到 role/asset rule table；`scripts/enforce_governed_action.py` 可 grep 到 primary writer / fallback 校验逻辑；fallback event 可记录 `primary_writer` 与 `reason`；finance path 仍只引用 `manage_finance.py` 而不直接写 ledger。</acceptance_criteria>
  <verify>
    <automated>grep -n 'PAIN_POINTS.md\|MARKET_PLAN.md\|TECH_SPEC.md\|LEDGER.json\|primary_writer\|fallback' scripts/governance_common.py scripts/enforce_governed_action.py && ! grep -n 'json.dump(.*LEDGER\|write_text(.*LEDGER' scripts/enforce_governed_action.py</automated>
  </verify>
  <done>写权限矩阵已经脱离文档，变成真正会阻断违规 actor/target 组合的执行规则。</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 smoke test 与运维文档，验证治理阻断和审计链</name>
  <read_first>.planning/phases/04-governance-and-control-layer/04-RESEARCH.md, docs/OPERATIONS.md, scripts/smoke_test_pipeline.sh, skills/common/run_accountant_audit.md, assets/shared/manage_finance.py</read_first>
  <files>scripts/smoke_test_pipeline.sh, docs/OPERATIONS.md, docs/STATE_CONTRACT.md</files>
  <action>扩展 `scripts/smoke_test_pipeline.sh`，加入 Phase 4 的最低自动门禁：治理目录存在、治理公共脚本可 `py_compile`、`render_governance_status.py --dry-run` 可运行、一个未批准高影响 action 会被阻断、一个合法 approval path 能写事件并刷新 latest 视图。若 smoke 不适合真的执行财务写入，就使用 `--dry-run` 或假 target 来验证包装器逻辑，但必须证明“未审批不执行”的语义。同步更新 `docs/OPERATIONS.md` 与 `docs/STATE_CONTRACT.md`，把已实现的治理入口、block/override 行为、以及 ‘先治理后写入’ 运维路径写清楚，保持代码与文档一致。</action>
  <acceptance_criteria>`scripts/smoke_test_pipeline.sh` 可 grep 到 governance 相关检查；运行 smoke 时能覆盖至少一个 blocked path；`docs/OPERATIONS.md` 与 `docs/STATE_CONTRACT.md` 出现 governance 执行入口与 override/block 说明，且不与已实现逻辑冲突。</acceptance_criteria>
  <verify>
    <automated>grep -n 'governance\|render_governance_status\|enforce_governed_action\|blocked' scripts/smoke_test_pipeline.sh && grep -n 'governance\|override\|blocked\|manage_finance.py' docs/OPERATIONS.md docs/STATE_CONTRACT.md</automated>
  </verify>
  <done>Phase 4 已进入自动质量门禁，operator 文档也能准确描述真实治理路径，而不是停留在旧政策文字。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| actor/target request → permission matrix | 角色权限与目标资产边界在执行前被检查 |
| governance wrapper → finance/state writer | 只有审批通过的动作能触发 authoritative writer |
| smoke / docs → operator behavior | 验证面和文档共同决定未来是否会绕过治理路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | E | fallback takeover path | mitigate | 只允许 CEO fallback，且必须有结构化治理事件和原因字段。 |
| T-04-10 | T | write-permission enforcement | mitigate | target asset → primary writer 映射固化在规则表中，非法 actor/target 组合直接阻断。 |
| T-04-11 | R | finance/state action auditability | mitigate | 高影响写入前后都保留治理事件，且引用 decision package 与 target asset。 |
| T-04-12 | I | smoke/document drift | mitigate | 把 blocked path 纳入 smoke，并同步更新 docs，避免“文档说有治理、实际无门禁”。 |
</threat_model>

<verification>
- `python3 -m py_compile scripts/governance_common.py scripts/request_governance_approval.py scripts/enforce_governed_action.py scripts/render_governance_status.py`
- `bash scripts/smoke_test_pipeline.sh`
- grep docs，确认治理入口、block、override、manage_finance authoritative path 都有准确说明。
</verification>

<success_criteria>
- GOV-01：高影响 finance/state/fallback 动作已被治理门禁包裹。
- GOV-02：审计链可从 operating decision package 追到 governance event，再追到 target action outcome。
- 严格主写者边界与 CEO-only override 已经从政策变成执行规则。
</success_criteria>

<output>
After completion, create `.planning/phases/04-governance-and-control-layer/04-03-SUMMARY.md`
</output>
