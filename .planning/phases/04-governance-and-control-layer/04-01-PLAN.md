---
phase: 04-governance-and-control-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/shared/governance/governance_events.jsonl
  - assets/shared/governance/GOVERNANCE_STATUS.md
  - scripts/governance_common.py
  - scripts/render_governance_status.py
autonomous: true
requirements:
  - GOV-01
  - GOV-02
must_haves:
  truths:
    - "治理事件会落到独立的 append-only JSONL 日志，而不是写进 LEDGER.json 或分散在 markdown 里。"
    - "系统会同时维护一个按治理动作分组的 operator latest 视图。"
    - "每条治理事件都强绑定当前 OPERATING_DECISION_PACKAGE.md，并保留完整上下文字段。"
  artifacts:
    - path: "assets/shared/governance/governance_events.jsonl"
      provides: "机器可校验的治理事件流"
    - path: "assets/shared/governance/GOVERNANCE_STATUS.md"
      provides: "按治理动作分组的 latest 视图"
    - path: "scripts/governance_common.py"
      provides: "治理 schema、规则与事件追加帮助函数"
    - path: "scripts/render_governance_status.py"
      provides: "从 JSONL 重建 latest 视图的渲染器"
  key_links:
    - from: "assets/shared/governance/governance_events.jsonl"
      to: "assets/shared/decision_packages/OPERATING_DECISION_PACKAGE.md"
      via: "related_decision_package"
      pattern: "OPERATING_DECISION_PACKAGE.md"
    - from: "assets/shared/governance/governance_events.jsonl"
      to: "assets/shared/trace/decision_package_trace.json"
      via: "related_trace.trace_path"
      pattern: "decision_package_trace.json"
---

<objective>
建立 Phase 4 的治理基础层：固定治理事件 schema、JSONL 落盘位置、operator latest 视图，以及最小可复用的治理辅助库。

Purpose: 先把“治理记录长什么样、存哪里、如何回放为 operator 视图”这层打稳，避免后续审批/阻断实现时继续把治理信息散落到各脚本和 markdown 中。
Output: 独立治理目录、append-only 事件流、latest 视图、标准 schema/渲染工具。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-governance-and-control-layer/04-CONTEXT.md
@.planning/phases/04-governance-and-control-layer/04-RESEARCH.md
@.planning/phases/04-governance-and-control-layer/04-PATTERNS.md
@docs/STATE_CONTRACT.md
@docs/OPERATIONS.md
@assets/shared/decision_packages/OPERATING_DECISION_PACKAGE.md
@assets/shared/trace/decision_package_trace.json

<interfaces>
Required governance event fields from locked decisions:
```text
action_id
event_type
actor
target_artifact
related_decision_package
status_before
status_after
approved_by
timestamp
```

Phase 3 trace anchor:
```json
{
  "operating_package_path": "assets/shared/decision_packages/OPERATING_DECISION_PACKAGE.md",
  "judgment_links": [...]
}
```
治理事件层必须扩展这条审计链，而不是另起炉灶。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 固定治理目录、JSONL schema 与 operator latest 视图</name>
  <read_first>.planning/phases/04-governance-and-control-layer/04-CONTEXT.md, .planning/phases/04-governance-and-control-layer/04-RESEARCH.md, .planning/phases/04-governance-and-control-layer/04-PATTERNS.md, assets/shared/decision_packages/OPERATING_DECISION_PACKAGE.md, assets/shared/trace/decision_package_trace.json</read_first>
  <files>assets/shared/governance/governance_events.jsonl, assets/shared/governance/GOVERNANCE_STATUS.md</files>
  <action>创建 `assets/shared/governance/` 目录，固定两类核心产物。`governance_events.jsonl` 作为唯一权威治理事件流，按一行一个 JSON object 记录，必须包含 D-03 指定字段，并补上 `action_type`、`reason`、`result_code`、`related_trace.trace_path`、`related_trace.judgment_ids`、`primary_writer`、`override_reason` 等最小扩展字段。`GOVERNANCE_STATUS.md` 作为 latest 视图，按 `Pending Approvals`、`Active Blocks`、`Recent Approvals`、`Recent Rejections`、`Recent Overrides` 五个分组呈现，禁止退化成原始时间线。每条 latest 记录都要显示 action_id、action_type、target、current status、decision package backlink。初始化时可写入空结构或示例占位，但格式必须已经可被后续脚本稳定重建。</action>
  <acceptance_criteria>`assets/shared/governance/governance_events.jsonl` 存在且首条示例/初始化事件可 grep 到 `action_id`、`event_type`、`related_decision_package`、`timestamp`；`assets/shared/governance/GOVERNANCE_STATUS.md` 可 grep 到 `Pending Approvals`、`Active Blocks`、`Recent Overrides` 与 `OPERATING_DECISION_PACKAGE.md`。</acceptance_criteria>
  <verify>
    <automated>grep -n '"action_id"\|"event_type"\|"related_decision_package"\|"timestamp"' assets/shared/governance/governance_events.jsonl && grep -n 'Pending Approvals\|Active Blocks\|Recent Overrides\|OPERATING_DECISION_PACKAGE.md' assets/shared/governance/GOVERNANCE_STATUS.md</automated>
  </verify>
  <done>治理产物层已从其他共享状态中分离，后续审批与阻断逻辑有了稳定落盘契约。</done>
</task>

<task type="auto">
  <name>Task 2: 实现治理公共库与 latest 重建器</name>
  <read_first>.planning/phases/04-governance-and-control-layer/04-RESEARCH.md, .planning/phases/04-governance-and-control-layer/04-PATTERNS.md, docs/STATE_CONTRACT.md, assets/shared/manage_finance.py</read_first>
  <files>scripts/governance_common.py, scripts/render_governance_status.py</files>
  <action>新增 stdlib-only Python 公共库 `scripts/governance_common.py`，负责：治理路径常量、event schema 校验、action_id 生成、append-only 写入、按 action_id 聚合最新状态、以及对 `related_decision_package` / `related_trace` 的最小校验。公共库不允许包含任何对 `LEDGER.json` 或角色业务 markdown 的直接写入逻辑。再新增 `scripts/render_governance_status.py`，只读取 `governance_events.jsonl` 并重建 `GOVERNANCE_STATUS.md`，按 action 分组展示 pending/blocked/approved/rejected/override 状态，保持 operator 可读性。实现风格需复用仓库现有 Python CLI 模式：显式常量、guard clauses、`--dry-run` 支持、单一异常出口。</action>
  <acceptance_criteria>`scripts/governance_common.py` 可 grep 到 `GOVERNANCE_EVENTS_PATH`、`append_event`、`validate_event`、`generate_action_id`；`scripts/render_governance_status.py --dry-run` 可输出 markdown 预览；两个脚本都 grep 不到 `LEDGER.json` 写入逻辑或 role artifact 直接改写代码。</acceptance_criteria>
  <verify>
    <automated>python3 scripts/render_governance_status.py --dry-run && grep -n 'GOVERNANCE_EVENTS_PATH\|append_event\|validate_event\|generate_action_id' scripts/governance_common.py && ! grep -n 'open(.*LEDGER.json.*w\|write_text(.*LEDGER.json' scripts/governance_common.py scripts/render_governance_status.py</automated>
  </verify>
  <done>治理层已经具备可复用的事件追加与 latest 重建能力，不需要后续脚本各自手搓 schema 和 markdown 汇总。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decision foundation → governance event | Phase 3 决策结论进入高影响治理动作的审计链 |
| governance JSONL → operator latest view | 机器权威事件流被重建为人类可读状态 |
| governance helpers → shared artifacts | 辅助库可以写治理目录，但不得越界到受保护状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `governance_events.jsonl` schema | mitigate | 所有事件经 `validate_event()` 校验，缺少 locked fields 即拒绝落盘。 |
| T-04-02 | R | governance event history | mitigate | 采用 append-only JSONL，不做静默覆盖；latest 由重建器派生，不直接手改。 |
| T-04-03 | I | `GOVERNANCE_STATUS.md` | mitigate | latest 只从 JSONL 重建，避免 operator 视图与权威事件流漂移。 |
| T-04-04 | E | `governance_common.py` | mitigate | 写路径固定在 `assets/shared/governance/`，不得新增任何 protected-state 直写逻辑。 |
</threat_model>

<verification>
- `python3 scripts/render_governance_status.py --dry-run`
- grep `governance_events.jsonl`，确认必填字段存在。
- grep `GOVERNANCE_STATUS.md`，确认按 action-state 分组而不是时间线平铺。
</verification>

<success_criteria>
- GOV-01：审批/阻断结果已经有统一事件结构可记录。
- GOV-02：治理事件能稳定回链到 `OPERATING_DECISION_PACKAGE.md` 与 trace sidecar。
- JSONL 是权威源，latest markdown 是派生视图，二者职责清晰。
</success_criteria>

<output>
After completion, create `.planning/phases/04-governance-and-control-layer/04-01-SUMMARY.md`
</output>
