---
phase: 12-credential-governance-and-operator-handoff
plan: 03
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
files_modified:
  - scripts/render_approved_delivery_status.py
  - scripts/validate_approved_delivery_pipeline.py
  - scripts/start_approved_project_delivery.py
  - orchestration/cron/commands.sh
  - docs/OPERATIONS.md
  - tests/test_approved_delivery_pipeline_cli.py
  - tests/test_project_delivery_pipeline_bootstrap.py
autonomous: true
requirements:
  - GOV-06
must_haves:
  truths:
    - "Operators can read one final review artifact showing approval state, blocked prerequisites, credentialed action outcomes, protected-change justification status, deployment outcome, and handoff state."
    - "Blocked deliveries, approval failures, deployment failures, and protected-platform modification attempts are more visible than success-only history."
    - "Validation proves the review artifact and authority state stay cross-linked for governance evidence."
  artifacts:
    - path: "scripts/render_approved_delivery_status.py"
      provides: "Final operator review markdown with exception-first governance, shipping, and handoff visibility."
    - path: "scripts/validate_approved_delivery_pipeline.py"
      provides: "Cross-link validation for credential audit artifacts, protected-change blocks, justification status, and final review completeness."
    - path: "docs/OPERATIONS.md"
      provides: "Operator workflow guidance for review, blocked-state inspection, and resume after governed approval."
  key_links:
    - from: "scripts/render_approved_delivery_status.py"
      to: "scripts/start_approved_project_delivery.py"
      via: "latest event and authority shipping/governance metadata"
      pattern: "block_reason|evidence_path|shipping|justification"
    - from: "scripts/validate_approved_delivery_pipeline.py"
      to: "scripts/render_approved_delivery_status.py"
      via: "rendered markdown assertions for governance visibility"
      pattern: "status_text"
---

<objective>
Finish Phase 12 with an operator-first review artifact and validator coverage that make governed delivery outcomes auditable, resumable, and easy to inspect from one authority-layer surface.

Purpose: Implement D-10 through D-12 by surfacing blocked, failed, approved, deployed, and handed-off states in one file-backed review flow.
Output: Updated status renderer, validator, wrapper/docs guidance, and tests covering the final review artifact contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-credential-governance-and-operator-handoff/12-CONTEXT.md
@.planning/phases/12-credential-governance-and-operator-handoff/12-RESEARCH.md
@.planning/phases/12-credential-governance-and-operator-handoff/12-VALIDATION.md
@scripts/render_approved_delivery_status.py
@scripts/validate_approved_delivery_pipeline.py
@scripts/start_approved_project_delivery.py
@orchestration/cron/commands.sh
@docs/OPERATIONS.md
@tests/test_approved_delivery_pipeline_cli.py
@tests/test_project_delivery_pipeline_bootstrap.py

<interfaces>
From scripts/render_approved_delivery_status.py:
```python
def build_status_markdown(events: list[dict[str, Any]]) -> str: ...
```

From scripts/validate_approved_delivery_pipeline.py:
```python
def validate_blocked_prerequisite_visibility(record, events, status_text) -> None: ...
def validate_github_linkage(record: dict[str, Any], status_text: str) -> None: ...
def validate_vercel_linkage(record: dict[str, Any], status_text: str) -> None: ...
def validate_approved_delivery_pipeline(approved_project_path: Path) -> dict[str, Any]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 12-03-01: Render and validate the final operator review artifact</name>
  <read_first>
    - scripts/render_approved_delivery_status.py
    - scripts/validate_approved_delivery_pipeline.py
    - docs/OPERATIONS.md
    - tests/test_approved_delivery_pipeline_cli.py
    - tests/test_project_delivery_pipeline_bootstrap.py
    - .planning/phases/12-credential-governance-and-operator-handoff/12-VALIDATION.md
  </read_first>
  <files>scripts/render_approved_delivery_status.py, scripts/validate_approved_delivery_pipeline.py, scripts/start_approved_project_delivery.py, orchestration/cron/commands.sh, docs/OPERATIONS.md, tests/test_approved_delivery_pipeline_cli.py, tests/test_project_delivery_pipeline_bootstrap.py</files>
  <acceptance_criteria>
    - The rendered authority-layer markdown review shows approval summary, blocked prerequisite evidence, credentialed GitHub/Vercel action outcomes, protected-change classification, platform-justification state, final handoff status, and action-required guidance in one place per D-10.
    - Blocked deliveries, approval failures, deployment failures, and protected-platform modification attempts are rendered with exception-first prominence instead of being buried in history per D-11.
    - The validator fails if audit artifacts, governance blocks, justification linkage, or final review visibility are missing, and operator wrapper/docs paths explain how to inspect and resume per D-12.
  </acceptance_criteria>
  <action>Extend `scripts/render_approved_delivery_status.py` into the final Phase 12 operator review artifact, keeping it markdown-first and authority-layer scoped per D-10 and D-12, and preserve the existing latest-view plus event-history structure already mapped in `12-PATTERNS.md`. Reuse the current blocked-visibility validation style in `scripts/validate_approved_delivery_pipeline.py`, but add explicit sections for credential action audits, protected-change classification, justification approval status, blocked prerequisite evidence, deployment outcome, and final handoff. Update `scripts/validate_approved_delivery_pipeline.py` so it proves the review artifact mentions required audit/justification evidence and that blocked/failed states remain visible, not merely persisted in JSON. If the pipeline needs a stable path for the final review artifact, persist that path from `scripts/start_approved_project_delivery.py`. Update `orchestration/cron/commands.sh` only by following its existing shell wrapper help/arity/dispatch pattern, and update `docs/OPERATIONS.md` and CLI tests only enough to keep the operator entry surface coherent; do not invent a second dashboard, service UI, or parallel status document.</action>
  <verify>
    <automated>python -m unittest tests.test_approved_delivery_pipeline_cli tests.test_project_delivery_pipeline_bootstrap -v</automated>
  </verify>
  <done>Operators can validate governed delivery state from one authority-layer review artifact, and missing governance visibility now fails automated validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authority artifacts/event stream → operator review markdown | Machine-readable governance state is transformed into a human decision surface |
| operator wrapper commands → validation/review scripts | Human-triggered inspection and resume commands cross into authoritative governance evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | I | scripts/render_approved_delivery_status.py | mitigate | Render only operator-relevant summaries and linked evidence paths while reusing existing helper redaction behavior for sensitive credential context. |
| T-12-08 | R | scripts/validate_approved_delivery_pipeline.py | mitigate | Fail validation when audit artifacts, blocked reasons, or justification linkage are absent from the rendered review so the operator surface cannot omit critical governance evidence. |
| T-12-09 | D | orchestration/cron/commands.sh | accept | Wrapper changes are limited to existing review/validate entrypoints; transient wrapper misuse is low-risk because validation remains read-only and resumable. |
</threat_model>

<verification>
- `python -m unittest tests.test_approved_delivery_pipeline_cli tests.test_project_delivery_pipeline_bootstrap -v`
- `python -m unittest tests.test_approved_delivery_pipeline_cli tests.test_project_delivery_pipeline_bootstrap tests.test_template_contract tests.test_check_template_conformance`
</verification>

<success_criteria>
- GOV-06 is satisfied by one operator-visible authority-layer review artifact.
- The review artifact highlights blocked, failed, approval, and handoff states with linked evidence.
- Validation now proves governance visibility, not just underlying JSON persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/12-credential-governance-and-operator-handoff/12-03-SUMMARY.md`
</output>
