---
phase: 12-credential-governance-and-operator-handoff
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - scripts/approved_delivery_governance.py
  - scripts/request_platform_justification.py
  - scripts/start_approved_project_delivery.py
  - tests/test_phase12_credential_governance.py
  - tests/test_project_delivery_pipeline_bootstrap.py
autonomous: true
requirements:
  - GOV-04
  - GOV-05
must_haves:
  truths:
    - "Hermes deterministically classifies touched changes as product-level or protected-platform before sync/deploy proceeds."
    - "Protected platform or shared-backend changes block the pipeline until a platform-justification artifact exists and governed approval is recorded."
    - "Blocked protected-change attempts persist resume-from-stage state and evidence instead of continuing silently."
  artifacts:
    - path: "scripts/approved_delivery_governance.py"
      provides: "Protected-surface classifier and justification validation helpers driven by platform contract surfaces."
    - path: "scripts/request_platform_justification.py"
      provides: "Authority-layer entrypoint that creates or validates the justification artifact and approval request linkage."
    - path: "scripts/start_approved_project_delivery.py"
      provides: "Pre-sync and pre-deploy gating that blocks protected changes until governed justification exists."
  key_links:
    - from: "scripts/start_approved_project_delivery.py"
      to: "scripts/approved_delivery_governance.py"
      via: "pre-github-sync and pre-vercel-deploy classification gate"
      pattern: "classify_.*protected|requires_platform_justification"
    - from: "scripts/approved_delivery_governance.py"
      to: "scripts/request_platform_justification.py"
      via: "justification artifact creation and approval linkage"
      pattern: "platform_justification"
---

<objective>
Add deterministic protected-surface classification and governed platform-justification blocking before any credentialed sync or deploy step can alter protected template or shared-backend primitives.

Purpose: Implement D-07 through D-09 by making platform-level changes visible, governable, and resumable before code leaves Hermes.
Output: Protected-path classifier logic, justification artifact/request wiring, and blocked/resume pipeline behavior covered by tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-credential-governance-and-operator-handoff/12-CONTEXT.md
@.planning/phases/12-credential-governance-and-operator-handoff/12-RESEARCH.md
@.planning/phases/12-credential-governance-and-operator-handoff/12-PATTERNS.md
@docs/platform/standalone-saas-template-contract.md
@docs/STATE_CONTRACT.md
@docs/OPERATIONS.md
@scripts/start_approved_project_delivery.py
@scripts/approved_delivery_governance.py
@tests/test_project_delivery_pipeline_bootstrap.py
@tests/test_phase12_credential_governance.py

<interfaces>
From scripts/start_approved_project_delivery.py:
```python
def run_pipeline_from_stage(authority_path: Path, record: dict[str, Any], *, start_stage: str, workspace_root: Path | None) -> dict[str, Any]: ...
def block_pipeline(...) -> dict[str, Any]: ...
```

Protected surfaces to classify from docs/platform/standalone-saas-template-contract.md include:
```text
src/lib/auth.ts
src/lib/supabase-browser.ts
src/lib/supabase-server.ts
src/lib/paypal.ts
src/lib/entitlement.ts
src/lib/db-guards.ts
supabase/migrations/20260423112500_create_shared_public_tables.sql
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 12-02-01: Implement protected-surface classification and governed justification blocking</name>
  <read_first>
    - docs/platform/standalone-saas-template-contract.md
    - .planning/phases/12-credential-governance-and-operator-handoff/12-CONTEXT.md
    - scripts/start_approved_project_delivery.py
    - scripts/approved_delivery_governance.py
    - tests/test_phase12_credential_governance.py
    - tests/test_project_delivery_pipeline_bootstrap.py
  </read_first>
  <files>scripts/approved_delivery_governance.py, scripts/request_platform_justification.py, scripts/start_approved_project_delivery.py, tests/test_phase12_credential_governance.py, tests/test_project_delivery_pipeline_bootstrap.py</files>
  <acceptance_criteria>
    - Tests prove touched-path classification runs before `github_sync` and again before `vercel_deploy`, per D-07.
    - Protected template/shared-backend touches generate an explicit platform-justification artifact plus approval linkage and block continuation until governed approval exists, per D-08.
    - The justification path reuses existing governance/conformance language and blocked/resume semantics rather than inventing a second approval model, per D-09.
  </acceptance_criteria>
  <action>Extend the governance helper from 12-01 with deterministic touched-path classification driven by the protected surfaces already defined in the template contract, per D-07, and mirror the deterministic path-matching style already present in `scripts/check_template_conformance.py`. The classifier should return product-only, protected-platform, or blocked-for-missing-information outcomes without broad fuzzy matching. Add `scripts/request_platform_justification.py` as the authority-layer artifact entrypoint that creates or validates the platform-justification record and links it to the approved-project governance trail, following the request/approval flow shape already used in `scripts/request_governance_approval.py`. Update `scripts/start_approved_project_delivery.py` so both the pre-`github_sync` path and the pre-`vercel_deploy` path run this gate: if protected surfaces are touched and no approved justification exists, persist blocked pipeline state, evidence path, and `resume_from_stage` exactly through the existing blocked/resume pattern per D-08 and D-09. Avoid late detection after sync or deploy already ran, and do not create a separate dashboard/state store.</action>
  <verify>
    <automated>python -m unittest tests.test_phase12_credential_governance tests.test_project_delivery_pipeline_bootstrap -v</automated>
  </verify>
  <done>Protected platform changes are classified deterministically and cannot pass through GitHub/Vercel shipping stages without an approved justification artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace changed-path inventory → classifier | Untrusted file-path input determines whether protected platform governance is required |
| justification artifact → pipeline resume gate | Human-approved governance metadata crosses into automated continuation decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | T | scripts/approved_delivery_governance.py | mitigate | Match touched paths deterministically against the documented protected-surface allow/block list before sync or deploy, per D-07. |
| T-12-05 | E | scripts/start_approved_project_delivery.py | mitigate | Block protected changes until an approved justification artifact is linked and validated, preserving `resume_from_stage` semantics per D-08. |
| T-12-06 | R | scripts/request_platform_justification.py | mitigate | Persist machine-readable justification artifacts and approval linkage in the existing governance trail so protected changes remain auditable and reviewable, per D-09. |
</threat_model>

<verification>
- `python -m unittest tests.test_phase12_credential_governance tests.test_project_delivery_pipeline_bootstrap -v`
- `python -m unittest tests.test_project_delivery_pipeline_bootstrap`
</verification>

<success_criteria>
- GOV-04 is satisfied by deterministic product-vs-platform classification before sync/deploy.
- GOV-05 is satisfied by an explicit platform-justification artifact and governed approval block/resume flow.
- Protected changes cannot silently ship through GitHub or Vercel stages without prior approval.
</success_criteria>

<output>
After completion, create `.planning/phases/12-credential-governance-and-operator-handoff/12-02-SUMMARY.md`
</output>
