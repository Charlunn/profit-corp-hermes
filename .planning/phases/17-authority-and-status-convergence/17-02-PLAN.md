---
phase: 17-authority-and-status-convergence
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - scripts/render_approved_delivery_status.py
  - scripts/validate_approved_delivery_pipeline.py
  - tests/test_approved_delivery_pipeline_cli.py
autonomous: true
requirements:
  - STAT-03
  - STAT-04
must_haves:
  truths:
    - "Operator-facing status surfaces show the current final recovered state instead of continuing to present a stale blocked summary after success."
    - "Blocked prerequisites and other prior failures remain visible as historical evidence even after the pipeline has recovered."
    - "Validator acceptance depends on agreement among authority record, rendered status, final operator review, and historical evidence links."
  artifacts:
    - path: "scripts/render_approved_delivery_status.py"
      provides: "Current-truth-first renderer with explicit audit-history section"
      contains: "build_status_markdown"
    - path: "scripts/validate_approved_delivery_pipeline.py"
      provides: "Cross-surface convergence validation for recovered completed runs"
      contains: "validate_approved_delivery_pipeline"
    - path: "tests/test_approved_delivery_pipeline_cli.py"
      provides: "CLI-level regression coverage for authority/status/final-review agreement"
      contains: "validator pass/fail scenarios"
  key_links:
    - from: "scripts/render_approved_delivery_status.py"
      to: "APPROVED_PROJECT.json"
      via: "record/shipping fallback precedence"
      pattern: "if not shipping and isinstance\(record.get\('shipping'\), dict\)"
    - from: "scripts/render_approved_delivery_status.py"
      to: "approved-delivery-events.jsonl"
      via: "Event History section"
      pattern: "history_lines"
    - from: "scripts/validate_approved_delivery_pipeline.py"
      to: "DELIVERY_PIPELINE_STATUS.md and FINAL_OPERATOR_REVIEW.md"
      via: "combined linkage validation"
      pattern: "status_text \+ \"\\n\" \+ final_review_text"
---

<objective>
Rework the approved-delivery status renderer and validator so current authority truth overrides stale blocked state while historical failures stay visible and linkable.

Purpose: Make operator-facing surfaces read as completed after recovery without losing auditability.
Output: Updated renderer/validator behavior plus CLI coverage that proves cross-surface convergence for recovered runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-authority-and-status-convergence/17-PATTERNS.md
@.planning/phases/16-vercel-auth-and-deploy-reliability/16-02-SUMMARY.md
@scripts/render_approved_delivery_status.py
@scripts/validate_approved_delivery_pipeline.py
@tests/test_approved_delivery_pipeline_cli.py

<interfaces>
From scripts/render_approved_delivery_status.py:
```python
def summarize_action_required(latest: dict[str, Any], record: dict[str, Any], github: dict[str, Any], vercel: dict[str, Any], protected_change: dict[str, Any], justification: dict[str, Any]) -> list[str]

def build_status_markdown(events: list[dict[str, Any]], record: dict[str, Any], project_dir: Path) -> str

def render_approved_delivery_status(project_dir: Path) -> str
```

From scripts/validate_approved_delivery_pipeline.py:
```python
def validate_blocked_prerequisite_visibility(record: dict[str, Any], events: list[dict[str, Any]], status_text: str) -> None

def validate_github_linkage(record: dict[str, Any], status_text: str) -> None

def validate_vercel_linkage(record: dict[str, Any], status_text: str) -> None

def validate_status_markdown(status_text: str, workspace: Path, final_handoff: Path) -> None

def validate_approved_delivery_pipeline(approved_project_path: Path) -> dict[str, Any]
```

Pattern constraints from 17-PATTERNS.md:
- Latest-view summary and explicit event-history section must remain separate.
- Validator should treat current snapshot claims as authoritative only when matching evidence and operator-visible linkage exist.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI regressions for recovered completed-state rendering with preserved blocked history</name>
  <files>tests/test_approved_delivery_pipeline_cli.py</files>
  <behavior>
    - Test 1: when authority state has recovered GitHub/Vercel success and events still contain an earlier blocked stage, rendered status/final review show completed current truth plus explicit blocked-history evidence per STAT-03 and STAT-04.
    - Test 2: validator fails if the rendered current-state summary still prefers stale blocked event fields over authoritative recovered state.
    - Test 3: validator fails if blocked history is hidden or its evidence link disappears after recovery.
  </behavior>
  <action>Extend the existing CLI/validator fixture set with a once-blocked-then-recovered scenario that exercises both `DELIVERY_PIPELINE_STATUS.md` and `FINAL_OPERATOR_REVIEW.md`. Assert current-state sections show the final recovered GitHub/Vercel/handoff truth, while history sections continue to expose prior blocked reason and evidence. Keep this focused on renderer/validator agreement; do not duplicate controller persistence coverage from Plan 17-01.</action>
  <verify>
    <automated>python -m unittest discover -s "C:/Users/42236/Desktop/dev/profit-corp-hermes/tests" -p "test_approved_delivery_pipeline_cli.py" -v</automated>
  </verify>
  <done>CLI tests codify the recovered-view precedence and historical-evidence retention required for operator surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Render current authority truth first while preserving explicit blocked-history audit links</name>
  <files>scripts/render_approved_delivery_status.py, scripts/validate_approved_delivery_pipeline.py</files>
  <action>Update `build_status_markdown` so the top-level current-state summary, action-required guidance, deployment outcome, and handoff sections derive from the latest authoritative recovered state from the record/latest success-aligned event, not merely the last blocked event payload. Preserve the separate `## Event History`/blocked-prerequisite visibility so old failures remain visible per STAT-04. Then update `validate_approved_delivery_pipeline.py` so convergence checks accept recovered completed runs only when current-state sections reflect the authoritative success truth and blocked-history links remain present. Keep both rendered files (`DELIVERY_PIPELINE_STATUS.md`, `FINAL_OPERATOR_REVIEW.md`) generated from the same markdown body and avoid introducing a second renderer or markdown-as-source-of-truth logic.</action>
  <verify>
    <automated>python -m unittest discover -s "C:/Users/42236/Desktop/dev/profit-corp-hermes/tests" -p "test_approved_delivery_pipeline_cli.py" -v</automated>
  </verify>
  <done>The renderer shows completed recovered truth in current-state sections, the validator enforces that precedence, and blocked evidence stays visible in history-oriented sections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authority/events -> renderer | Canonical JSON and append-only history are transformed into operator-visible markdown |
| rendered markdown -> validator | Validator determines whether surfaces faithfully represent authoritative state |
| historical failure evidence -> current-state summary | Old blocked data must remain visible without overriding present truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | T | `scripts/render_approved_delivery_status.py` precedence rules | mitigate | Compute current-state sections from authoritative record / latest success-aligned state while keeping historical failure details in a separate history section. |
| T-17-06 | I | operator status surfaces | mitigate | Keep `DELIVERY_PIPELINE_STATUS.md` and `FINAL_OPERATOR_REVIEW.md` written from the same markdown body so current truth cannot diverge between surfaces. |
| T-17-07 | R | historical blocked evidence | mitigate | Require blocked reason/evidence links to remain visible in rendered history and validator checks whenever blocked events exist. |
| T-17-08 | E | validator acceptance of stale state | mitigate | Tighten `validate_approved_delivery_pipeline.py` so a recovered completed run passes only if success metadata and blocked-history evidence both appear in the appropriate sections. |
</threat_model>

<verification>
- `python -m unittest discover -s "C:/Users/42236/Desktop/dev/profit-corp-hermes/tests" -p "test_approved_delivery_pipeline_cli.py" -v`
</verification>

<success_criteria>
- `DELIVERY_PIPELINE_STATUS.md` reads as completed after recovery when authority data says the run succeeded.
- `FINAL_OPERATOR_REVIEW.md` matches the same recovered current truth.
- Blocked-history evidence remains visible and validator-enforced instead of being erased or promoted to current status.
</success_criteria>

<output>
After completion, create `.planning/phases/17-authority-and-status-convergence/17-authority-and-status-convergence-02-SUMMARY.md`
</output>
