---
phase: 17-authority-and-status-convergence
plan: 03
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
files_modified:
  - tests/test_project_delivery_pipeline_bootstrap.py
  - tests/test_project_delivery_pipeline_resume.py
  - tests/test_approved_delivery_pipeline_cli.py
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
  - STAT-04
must_haves:
  truths:
    - "A once-blocked live-style delivery run can recover through GitHub and Vercel success and end with completed authority, status, and review surfaces."
    - "Recovered completion keeps old blocked evidence visible without leaving the current pipeline state stuck as blocked."
    - "Bootstrap, resume, and CLI validation agree on the same final recovered end state."
  artifacts:
    - path: "tests/test_project_delivery_pipeline_bootstrap.py"
      provides: "Fresh-run recovery regression chain across authority/event/status surfaces"
      contains: "once-blocked then completed assertions"
    - path: "tests/test_project_delivery_pipeline_resume.py"
      provides: "Resume-path recovery regression chain across authority/event/status surfaces"
      contains: "recovered final-state assertions"
    - path: "tests/test_approved_delivery_pipeline_cli.py"
      provides: "Validator/command-level proof that recovered runs pass convergence checks"
      contains: "validator success scenario"
  key_links:
    - from: "tests/test_project_delivery_pipeline_bootstrap.py"
      to: "scripts/start_approved_project_delivery.py"
      via: "fresh-run recovery fixtures"
      pattern: "run_github_sync|vercel"
    - from: "tests/test_project_delivery_pipeline_resume.py"
      to: "APPROVED_PROJECT.json + events + status files"
      via: "resume assertions"
      pattern: "status|final_handoff|events"
    - from: "tests/test_approved_delivery_pipeline_cli.py"
      to: "scripts/validate_approved_delivery_pipeline.py"
      via: "validator pass/fail scenarios"
      pattern: "run_validator"
---

<objective>
Add the final end-to-end recovery validation proving a once-blocked approved delivery run can finish and render as completed across authority, events, and operator surfaces.

Purpose: Close the Phase 17 regression envelope with live-style recovered-run evidence instead of isolated unit seams.
Output: Coordinated bootstrap/resume/CLI tests that prove cross-surface convergence from blocked history to completed recovered truth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-authority-and-status-convergence/17-PATTERNS.md
@scripts/start_approved_project_delivery.py
@scripts/render_approved_delivery_status.py
@scripts/validate_approved_delivery_pipeline.py
@tests/test_project_delivery_pipeline_bootstrap.py
@tests/test_project_delivery_pipeline_resume.py
@tests/test_approved_delivery_pipeline_cli.py

<interfaces>
Cross-surface convergence contract to assert end to end:
```text
APPROVED_PROJECT.json
  -> current pipeline.stage/status/resume_from_stage/final_handoff
  -> shipping.github repository/sync fields
  -> shipping.vercel link/deploy fields
  -> latest_blocked_prerequisite historical evidence

approved-delivery-events.jsonl
  -> retains blocked event(s)
  -> appends later success event(s) without mutation

DELIVERY_PIPELINE_STATUS.md + FINAL_OPERATOR_REVIEW.md
  -> current sections show completed recovered truth
  -> history sections still expose blocked reason/evidence links
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an end-to-end recovered-run regression matrix across bootstrap and resume flows</name>
  <files>tests/test_project_delivery_pipeline_bootstrap.py, tests/test_project_delivery_pipeline_resume.py</files>
  <behavior>
    - Test 1: a fresh run that was previously blocked, then completes GitHub sync and Vercel deploy, ends with completed authority state plus preserved blocked history.
    - Test 2: a resumed run from blocked GitHub/Vercel stages converges to the same final completed truth without duplicate-event replay after handoff.
    - Test 3: both flows assert the same final handoff path, shipping metadata, and historical blocked evidence linkage across authority and events.
  </behavior>
  <action>Build one coordinated recovery matrix spanning bootstrap and resume tests using existing fixtures rather than introducing a new test file. The matrix should drive a blocked-to-success lifecycle across GitHub sync, Vercel link, Vercel deploy, and handoff, then assert that authority fields, appended events, and generated status artifacts converge on the same final outcome. Keep the scenario phase-local: do not reopen GitHub auth detection, workspace snapshot mechanics, or Vercel helper contracts already closed in Phases 15-16.</action>
  <verify>
    <automated>python -m unittest discover -s "C:/Users/42236/Desktop/dev/profit-corp-hermes/tests" -p "test_project_delivery_pipeline_resume.py" -v</automated>
  </verify>
  <done>Bootstrap/resume tests prove a recovered run reaches the same completed truth regardless of whether the recovery happens during the original run or after resume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove recovered-run convergence through the validator CLI envelope</name>
  <files>tests/test_approved_delivery_pipeline_cli.py</files>
  <behavior>
    - Test 1: validator passes only when authority record, event stream, rendered status, and final operator review all agree on the recovered completed state.
    - Test 2: validator still fails if any single surface regresses back to stale blocked-current-state wording or loses blocked-history evidence.
  </behavior>
  <action>Extend the CLI regression suite with a single authoritative recovered-run fixture that mirrors the end-to-end lifecycle from Task 1. Reuse the validator command path and assert success only when all four surfaces agree on the final recovered truth. Also add at least one targeted negative assertion showing the validator catches a stale-current-state regression even if the underlying authority record is correct. Avoid adding new production code in this plan unless a small bug is discovered while closing the regression envelope; if that happens, keep the fix tightly scoped to the failing cross-surface seam.</action>
  <verify>
    <automated>python -m unittest discover -s "C:/Users/42236/Desktop/dev/profit-corp-hermes/tests" -p "test_approved_delivery_pipeline_bootstrap.py" -v && python -m unittest discover -s "C:/Users/42236/Desktop/dev/profit-corp-hermes/tests" -p "test_project_delivery_pipeline_resume.py" -v && python -m unittest discover -s "C:/Users/42236/Desktop/dev/profit-corp-hermes/tests" -p "test_approved_delivery_pipeline_cli.py" -v</automated>
  </verify>
  <done>The targeted Phase 17 regression envelope proves the once-blocked recovery path finishes as completed across authority JSON, append-only events, status markdown, and final review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures -> production scripts | Recovery scenarios must accurately model real persisted authority/events/status interactions |
| authority/events/status/review -> validator outcome | Any mismatch between surfaces should be treated as a failure, not silently tolerated |
| historical blocked state -> recovered completion | Prior failures must remain auditable without controlling current-state truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | R | recovered-run regression coverage | mitigate | Assert append-only blocked-to-success history and final authority/status agreement across bootstrap, resume, and CLI scenarios. |
| T-17-10 | T | end-to-end fixtures | mitigate | Use existing project/workspace fixture patterns so tests exercise the same persisted artifact shapes used by the real pipeline. |
| T-17-11 | I | validator success envelope | mitigate | Include negative cases where one surface regresses to stale blocked-current-state wording, proving convergence is enforced rather than assumed. |
| T-17-12 | D | completed-handoff resume path | mitigate | Preserve and assert the no-duplicate-event behavior after completed handoff so recovery tests do not create replay-based false positives. |
</threat_model>

<verification>
- `python -m unittest discover -s "C:/Users/42236/Desktop/dev/profit-corp-hermes/tests" -p "test_project_delivery_pipeline_bootstrap.py" -v`
- `python -m unittest discover -s "C:/Users/42236/Desktop/dev/profit-corp-hermes/tests" -p "test_project_delivery_pipeline_resume.py" -v`
- `python -m unittest discover -s "C:/Users/42236/Desktop/dev/profit-corp-hermes/tests" -p "test_approved_delivery_pipeline_cli.py" -v`
</verification>

<success_criteria>
- A once-blocked approved delivery scenario can be exercised end to end and finishes as completed.
- Authority JSON, event history, status markdown, and final review all agree on the recovered final truth.
- The regression envelope catches both stale-current-state and hidden-history regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/17-authority-and-status-convergence/17-authority-and-status-convergence-03-SUMMARY.md`
</output>
