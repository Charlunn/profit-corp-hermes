---
phase: 17-authority-and-status-convergence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/start_approved_project_delivery.py
  - tests/test_project_delivery_pipeline_bootstrap.py
  - tests/test_project_delivery_pipeline_resume.py
autonomous: true
requirements:
  - STAT-01
  - STAT-02
must_haves:
  truths:
    - "After GitHub sync succeeds, the authority record shows the real repository metadata, sync commit, and next-stage resume state instead of stale blocked values."
    - "After Vercel link succeeds, the authority record shows the real linked Vercel project metadata and env-contract evidence only from the successful governed helper result."
    - "After Vercel deploy succeeds, the authority record shows the real deploy URL, deploy status, evidence paths, and a completed handoff-ready pipeline state."
  artifacts:
    - path: "scripts/start_approved_project_delivery.py"
      provides: "Authority-first convergence logic for post-success GitHub/Vercel stages"
      contains: "update_pipeline_state"
    - path: "tests/test_project_delivery_pipeline_bootstrap.py"
      provides: "Fresh-run regression coverage for post-success authority convergence"
      contains: "GitHub/Vercel success assertions"
    - path: "tests/test_project_delivery_pipeline_resume.py"
      provides: "Resume-time regression coverage for replacing stale current-state metadata with recovered success truth"
      contains: "resume convergence assertions"
  key_links:
    - from: "scripts/start_approved_project_delivery.py"
      to: "APPROVED_PROJECT.json"
      via: "persist_and_render"
      pattern: "write_json\(authority_path, record\)"
    - from: "scripts/start_approved_project_delivery.py"
      to: "approved-delivery-events.jsonl"
      via: "append_next_pipeline_event"
      pattern: "append_next_pipeline_event"
    - from: "tests/test_project_delivery_pipeline_resume.py"
      to: "scripts/start_approved_project_delivery.py"
      via: "resume_approved_project_delivery"
      pattern: "resume_approved_project_delivery"
---

<objective>
Normalize authority-state convergence after successful GitHub sync, Vercel link, and Vercel deploy so the canonical record always reflects the recovered current truth.

Purpose: Close the Phase 17 authority seam without reopening Phase 16 success-gating rules.
Output: Updated controller persistence logic plus focused bootstrap/resume regressions that prove current-run success overwrites stale blocked-state metadata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-authority-and-status-convergence/17-PATTERNS.md
@.planning/phases/16-vercel-auth-and-deploy-reliability/16-02-SUMMARY.md
@.planning/phases/16-vercel-auth-and-deploy-reliability/16-03-SUMMARY.md
@scripts/start_approved_project_delivery.py
@tests/test_project_delivery_pipeline_bootstrap.py
@tests/test_project_delivery_pipeline_resume.py

<interfaces>
From scripts/start_approved_project_delivery.py:
```python
def make_event(*, record: dict[str, Any], authority_path: Path, stage: str, status: str, action: str, outcome: str, artifact: str, timestamp: str, workspace_path: str = "", delivery_run_id: str = "", block_reason: str = "", evidence_path: str = "", resume_from_stage: str = "", final_handoff_path: str = "") -> dict[str, Any]

def update_pipeline_state(record: dict[str, Any], *, stage: str, status: str, block_reason: str | None = None, workspace_path: str | None = None, evidence_path: str | None = None, resume_from_stage: str | None = None, delivery_run_id: str | None = None, final_handoff_path: str | None = None) -> None

def persist_and_render(authority_path: Path, record: dict[str, Any]) -> None

def append_next_pipeline_event(project_dir: Path, event: dict[str, Any]) -> None

def resume_approved_project_delivery(authority_record_path: Path | str, *, workspace_root: Path | None = None) -> dict[str, Any]
```

Phase constraints from prior summaries:
- Keep Phase 16 success-gating intact: no GitHub/Vercel success metadata may be written before the governed helper returns authoritative success evidence.
- Preserve authority-first persistence: JSON authority state is canonical; rendered markdown remains derived.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression coverage for recovered current-state authority truth</name>
  <files>tests/test_project_delivery_pipeline_bootstrap.py, tests/test_project_delivery_pipeline_resume.py</files>
  <behavior>
    - Test 1: a once-blocked run that later succeeds at `github_sync` persists the real repository metadata, synced commit, evidence path, and next-stage resume state per STAT-01.
    - Test 2: a recovered `vercel_linkage` success replaces stale current-state Vercel metadata with the latest governed link result and preserves prior blocked evidence only in history per STAT-02.
    - Test 3: a recovered `vercel_deploy` success persists the real deploy URL/status/evidence and completed handoff-facing state without replaying duplicate events after completed handoff.
  </behavior>
  <action>Extend the existing bootstrap/resume fixtures to model the exact recovered path from blocked to successful shipping. Add assertions for the authority-record fields that must converge after success per STAT-01 and STAT-02, while explicitly preserving the Phase 16 rule that success metadata appears only after the successful helper result. Do not broaden coverage into renderer wording or validator formatting here; keep this plan focused on controller persistence seams.</action>
  <verify>
    <automated>python -m unittest discover -s "C:/Users/42236/Desktop/dev/profit-corp-hermes/tests" -p "test_project_delivery_pipeline_bootstrap.py" -v</automated>
  </verify>
  <done>Bootstrap and resume tests fail before controller changes and codify the recovered authority truth required for GitHub/Vercel success.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize post-success authority writes and emitted recovery events</name>
  <files>scripts/start_approved_project_delivery.py</files>
  <behavior>
    - Test-backed controller writes replace stale blocked current-state values with authoritative GitHub success fields after `github_sync` per STAT-01.
    - Test-backed controller writes replace stale blocked current-state values with authoritative Vercel link/deploy fields only after helper success per STAT-02.
    - Recovery keeps append-only history: old blocked events remain in `approved-delivery-events.jsonl`, while the current authority snapshot and latest event reflect the successful state.
  </behavior>
  <action>Update `scripts/start_approved_project_delivery.py` so successful `github_sync`, `vercel_linkage`, and `vercel_deploy` stages each rewrite the canonical current-state fields that downstream status surfaces consume: `pipeline.stage`, `pipeline.status`, `pipeline.resume_from_stage`, workspace/evidence paths, `shipping.github`, `shipping.vercel`, and `final_handoff`/artifact mirrors when handoff is reached. When old blocked context exists, preserve it as historical evidence rather than the current truth by storing it in the append-only event stream and any existing `latest_blocked_prerequisite`-style record fields, but do not let stale blocked fields remain authoritative after success. Reuse `update_pipeline_state`, `make_event`, `append_next_pipeline_event`, and `persist_and_render`; do not introduce side-channel writers or markdown-derived state.</action>
  <verify>
    <automated>python -m unittest discover -s "C:/Users/42236/Desktop/dev/profit-corp-hermes/tests" -p "test_project_delivery_pipeline_resume.py" -v</automated>
  </verify>
  <done>Recovered GitHub/Vercel success paths persist authoritative current truth into `APPROVED_PROJECT.json`, append a matching latest event, and keep resume semantics rooted in authority metadata rather than stale blocked state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| governed helper -> authority record | External GitHub/Vercel helper results cross into canonical persisted state |
| authority record -> operator surfaces | Machine-readable state is rendered into user-visible markdown |
| resume command -> pipeline controller | Persisted state drives subsequent automation decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | T | `scripts/start_approved_project_delivery.py` success persistence | mitigate | Persist GitHub/Vercel success fields only from governed helper return payloads; never synthesize success metadata from defaults or stale blocked state. |
| T-17-02 | R | append-only recovery events | mitigate | Reuse `append_next_pipeline_event` and `make_event` so recovery writes leave an auditable blocked-to-success trail. |
| T-17-03 | I | authority snapshot consumed by renderer/validator | mitigate | Rewrite current-state fields atomically through `update_pipeline_state` + `persist_and_render` so operator surfaces do not expose stale or contradictory metadata. |
| T-17-04 | E | resume path after recovery | mitigate | Keep `resume_approved_project_delivery` rooted in persisted authority fields and completed-handoff short-circuit logic instead of inferring state from markdown. |
</threat_model>

<verification>
- `python -m unittest discover -s "C:/Users/42236/Desktop/dev/profit-corp-hermes/tests" -p "test_project_delivery_pipeline_bootstrap.py" -v`
- `python -m unittest discover -s "C:/Users/42236/Desktop/dev/profit-corp-hermes/tests" -p "test_project_delivery_pipeline_resume.py" -v`
</verification>

<success_criteria>
- A recovered `github_sync` success leaves `shipping.github` and pipeline resume fields aligned to the true repo/commit state.
- A recovered `vercel_linkage` / `vercel_deploy` success leaves `shipping.vercel` and deploy evidence fields aligned to the true linked/deployed project.
- Historical blocked evidence remains recoverable from events, but the current authority snapshot no longer reads as blocked after success.
</success_criteria>

<output>
After completion, create `.planning/phases/17-authority-and-status-convergence/17-authority-and-status-convergence-01-SUMMARY.md`
</output>
