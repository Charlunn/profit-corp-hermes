---
phase: 16-vercel-auth-and-deploy-reliability
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - scripts/start_approved_project_delivery.py
  - tests/test_project_delivery_pipeline_bootstrap.py
  - tests/test_project_delivery_pipeline_resume.py
  - scripts/render_approved_delivery_status.py
  - scripts/validate_approved_delivery_pipeline.py
autonomous: true
requirements:
  - VERC-03
  - VERC-04
must_haves:
  truths:
    - "The authority record does not claim a Vercel project name, scope, or URL before real linkage succeeds."
    - "Successful Vercel linkage and deploy overwrite authority metadata with the actual linked project, scope, deploy URL, status, and evidence paths from the current run."
    - "Resume and operator-facing validation consume authoritative Vercel metadata instead of stale defaults inherited from earlier runs or environment guesses."
  artifacts:
    - path: "scripts/start_approved_project_delivery.py"
      provides: "Authority-first persistence rules for authoritative-only Vercel metadata and deployment evidence."
    - path: "tests/test_project_delivery_pipeline_bootstrap.py"
      provides: "Fresh-run controller assertions preventing stale Vercel metadata writes before linkage and requiring real metadata after success."
    - path: "tests/test_project_delivery_pipeline_resume.py"
      provides: "Resume-path assertions proving current-run Vercel metadata/evidence replace stale values after recovery."
    - path: "scripts/render_approved_delivery_status.py"
      provides: "Operator status rendering that displays persisted authoritative Vercel metadata/evidence fields once present."
    - path: "scripts/validate_approved_delivery_pipeline.py"
      provides: "Validation rules that require Vercel metadata/evidence linkage after successful link/deploy without pulling in Phase 17 truth-precedence redesign."
  key_links:
    - from: "scripts/start_approved_project_delivery.py"
      to: "record['shipping']['vercel']"
      via: "authoritative-only persistence after governed link/env/deploy results"
      pattern: "project_name|project_url|team_scope|deploy_url|evidence_path"
    - from: "scripts/render_approved_delivery_status.py"
      to: "shipping.vercel"
      via: "operator-visible Vercel linkage/deploy summary"
      pattern: "Vercel Team Scope|Vercel Project URL|Vercel Deploy Evidence Path"
    - from: "scripts/validate_approved_delivery_pipeline.py"
      to: "APPROVED_PROJECT.json + DELIVERY_PIPELINE_STATUS.md"
      via: "post-success Vercel metadata/evidence cross-link validation"
      pattern: "project_name|project_url|team_scope|deploy_url|deploy_evidence_path"
---

<objective>
Harden controller persistence and downstream metadata consumers so Phase 16 records only real Vercel linkage/deploy truth and stops pre-writing stale authority fields.

Purpose: Fix the stale-metadata seam exposed in the live run while keeping the approved-delivery controller, status renderer, and validator aligned around authoritative Vercel evidence only.
Output: Controller and consumer updates that persist true Vercel metadata/evidence from current-run results and prevent inherited defaults from being mistaken for real deployment state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-vercel-auth-and-deploy-reliability/16-RESEARCH.md
@scripts/start_approved_project_delivery.py
@scripts/render_approved_delivery_status.py
@scripts/validate_approved_delivery_pipeline.py
@tests/test_project_delivery_pipeline_bootstrap.py
@tests/test_project_delivery_pipeline_resume.py
@tests/test_phase11_vercel_flow.py

<interfaces>
From scripts/start_approved_project_delivery.py:
```python
def run_pipeline_from_stage(authority_path: Path, record: dict[str, Any], *, start_stage: str, workspace_root: Path | None) -> dict[str, Any]: ...
def link_vercel_project(authority_record_path: Path | str, workspace_path: Path | str) -> dict[str, Any]: ...
def run_vercel_deploy(authority_record_path: Path | str, workspace_path: Path | str) -> dict[str, Any]: ...
def update_pipeline_state(...) -> None: ...
```

Current stale-metadata seam:
```python
approved_vercel_project = str(os.environ.get("VERCEL_PROJECT", "")).strip() or f"{project_slug}-prod"
approved_vercel_team = str(os.environ.get("VERCEL_TEAM", "")).strip() or "profit-corp"
vercel_record["project_name"] = approved_vercel_project
vercel_record["project_url"] = f"https://vercel.com/{approved_vercel_team}/{approved_vercel_project}"
vercel_record["team_scope"] = approved_vercel_team
```

From scripts/validate_approved_delivery_pipeline.py:
```python
def validate_vercel_linkage(record: dict[str, Any], status_text: str) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red controller tests for authoritative-only Vercel metadata persistence and resume convergence</name>
  <files>tests/test_project_delivery_pipeline_bootstrap.py, tests/test_project_delivery_pipeline_resume.py</files>
  <behavior>
    - Test 1: after `github_sync`, the authority record advances to `vercel_linkage` without claiming successful `project_name`, `project_url`, or `team_scope` values before link success.
    - Test 2: after successful Vercel link/deploy, the authority record persists the real `project_id`, `project_name`, `project_url`, `team_scope`, `deploy_url`, `deploy_status`, and evidence paths from the current run.
    - Test 3: resume/recovery replaces stale or inherited Vercel metadata with current authoritative values instead of preserving outdated defaults.
  </behavior>
  <action>Extend bootstrap and resume tests to encode VERC-03 at the controller boundary. Add failing assertions that the controller no longer pre-populates authoritative Vercel fields during the `github_sync -> vercel_linkage` transition, and add current-run replacement assertions for resumed deliveries that previously carried stale Vercel metadata. Keep the test scope on controller persistence and resume continuity; do not pull Phase 17 historical-truth precedence into these tests.</action>
  <verify>
    <automated>python -m unittest tests.test_project_delivery_pipeline_bootstrap tests.test_project_delivery_pipeline_resume -v</automated>
  </verify>
  <done>Red tests pin down the no-stale-authority rule and the current-run replacement behavior required for VERC-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove prewritten Vercel authority defaults and persist current-run metadata/evidence only after success</name>
  <files>scripts/start_approved_project_delivery.py, tests/test_project_delivery_pipeline_bootstrap.py, tests/test_project_delivery_pipeline_resume.py</files>
  <behavior>
    - Test 1: the controller derives candidate Vercel project/scope inputs in-memory for helper calls but does not write them as authoritative truth before link success.
    - Test 2: successful link/env/deploy results overwrite `shipping.vercel` with current-run metadata, auth source, required env contract, deploy status, and evidence paths.
    - Test 3: blocked/failure paths preserve pipeline evidence and failure reason without synthesizing fake successful Vercel metadata.
  </behavior>
  <action>Refactor `scripts/start_approved_project_delivery.py` so Vercel candidate inputs stay transient until governed helper results return success. Remove the current `github_sync`-stage writes that synthesize `project_name`, `project_url`, and `team_scope`. At `vercel_linkage` success, persist only helper-returned authoritative values including `project_id`, `project_name`, `project_url`, `team_scope`, `auth_source`, `auth_source_details`, `env_contract`, `env_contract_path`, and audit/evidence paths. At deploy success, persist only helper-returned `deploy_url`, `deploy_status`, `deploy_evidence_path`, `deployment_url`, `deployment_status`, and `deployment_evidence_path`. On blocked/failure results, update pipeline state and evidence paths only; do not invent successful Vercel truth fields. Resume must continue using already-persisted authoritative values when present, otherwise recompute candidate inputs transiently for the next helper call.</action>
  <verify>
    <automated>python -m unittest tests.test_project_delivery_pipeline_bootstrap tests.test_project_delivery_pipeline_resume tests.test_phase11_vercel_flow -v</automated>
  </verify>
  <done>The controller now writes real Vercel truth only after real helper success and cannot leak guessed metadata into authority records.</done>
</task>

<task type="auto">
  <name>Task 3: Tighten status and validator consumers around authoritative Vercel metadata/evidence</name>
  <files>scripts/render_approved_delivery_status.py, scripts/validate_approved_delivery_pipeline.py</files>
  <action>Update the status renderer and validator to treat the authoritative Vercel fields introduced by Task 2 as the proof surface for Phase 16. `render_approved_delivery_status.py` should continue rendering the existing sections, but it must expose the persisted `team_scope`, `project_name`, `project_url`, `deploy_url`, and deploy evidence fields without inferring them from defaults. `validate_approved_delivery_pipeline.py` should require those fields only when the authority record shows successful Vercel link/deploy, and it must keep Phase 17 truth-precedence concerns out of scope by validating linkage presence rather than redesigning current-vs-historical rendering rules.</action>
  <verify>
    <automated>python -m unittest tests.test_project_delivery_pipeline_bootstrap tests.test_project_delivery_pipeline_resume -v</automated>
  </verify>
  <done>Operator surfaces and validator rules now consume only authoritative Phase 16 Vercel metadata/evidence while leaving Phase 17 convergence work untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper results → authority record | External-service results cross into the canonical approved-project truth surface |
| authority record → status/validator consumers | Persisted Vercel metadata becomes operator-visible and validator-enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | T | scripts/start_approved_project_delivery.py Vercel persistence | mitigate | Stop writing guessed Vercel fields before link success and persist only current-run helper outputs into `shipping.vercel`. |
| T-16-05 | R | status/validator consumers | mitigate | Require operator-facing and validation surfaces to read persisted authoritative fields and evidence paths rather than reconstructing Vercel truth from defaults. |
| T-16-06 | I | approved-project authority metadata | mitigate | Prevent stale or inherited project/scope/URL values from being mistaken for real deployed-project truth. |
</threat_model>

<verification>
- `python -m unittest tests.test_project_delivery_pipeline_bootstrap tests.test_project_delivery_pipeline_resume -v`
- `python -m unittest tests.test_project_delivery_pipeline_bootstrap tests.test_project_delivery_pipeline_resume tests.test_phase11_vercel_flow -v`
- Confirm successful-link/deploy fixtures show persisted `project_name`, `project_url`, `team_scope`, `deploy_url`, and evidence-path fields sourced from current-run results only.
</verification>

<success_criteria>
- VERC-03: authority persistence no longer writes stale Vercel metadata before real linkage succeeds.
- VERC-03: successful Vercel link/deploy writes the real linked project, scope, deploy URL, status, and evidence paths for the approved project.
- VERC-04 downstream surface: operator status and validator consumers rely on authoritative Phase 16 metadata/evidence rather than inherited defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/16-vercel-auth-and-deploy-reliability/16-02-SUMMARY.md`
</output>
