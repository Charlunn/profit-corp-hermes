---
phase: 16-vercel-auth-and-deploy-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase11_vercel_flow.py
  - scripts/vercel_delivery_common.py
autonomous: true
requirements:
  - VERC-01
  - VERC-02
  - VERC-04
must_haves:
  truths:
    - "Approved delivery can use a valid local Vercel CLI login when `VERCEL_TOKEN` is absent."
    - "Approved delivery still prefers explicit `VERCEL_TOKEN` when it is intentionally supplied for non-interactive runs."
    - "Vercel helper failures distinguish missing CLI, invalid token/auth, inaccessible scope, and actual link/deploy execution failure."
  artifacts:
    - path: "scripts/vercel_delivery_common.py"
      provides: "Centralized Vercel auth-source resolution plus granular blocked/failure classification for link/env/deploy helpers."
    - path: "tests/test_phase11_vercel_flow.py"
      provides: "Regression coverage for local CLI auth, explicit token auth, and distinct Vercel failure taxonomy."
  key_links:
    - from: "scripts/vercel_delivery_common.py"
      to: "Vercel CLI auth probe"
      via: "shared auth-resolution helper used by link/env/deploy entry points"
      pattern: "whoami|auth_source|auth_source_details"
    - from: "scripts/vercel_delivery_common.py"
      to: ".hermes/vercel-linkage.json and .hermes/vercel-deploy.json"
      via: "granular blocked/failure evidence"
      pattern: "block_reason|stderr_summary|project_name|team_scope"
---

<objective>
Unify Vercel auth resolution and failure classification in the delivery helper layer so the pipeline can reuse the real operator auth path without losing the explicit-token automation path.

Purpose: Eliminate the token-only gate that broke the live run while preserving the existing approved-delivery helper architecture and operator-visible evidence boundaries.
Output: Tested helper changes in `scripts/vercel_delivery_common.py` that resolve auth source once and return Vercel-specific failure reasons that later controller/governance layers can persist unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-vercel-auth-and-deploy-reliability/16-RESEARCH.md
@.planning/phases/15-github-sync-reliability-on-real-workspaces/15-VERIFICATION.md
@scripts/vercel_delivery_common.py
@tests/test_phase11_vercel_flow.py

<interfaces>
From scripts/vercel_delivery_common.py:
```python
def _resolve_vercel_command(which: Which | None = None) -> list[str] | None: ...
def _require_vercel(...) -> tuple[list[str] | None, dict[str, Any] | None]: ...
def link_vercel_project(...) -> dict[str, Any]: ...
def apply_env_contract(...) -> dict[str, Any]: ...
def deploy_to_vercel(...) -> dict[str, Any]: ...
```

Current token-only seam:
```python
if not source.get("VERCEL_TOKEN"):
    return None, _blocked(
        workspace,
        evidence_name,
        "missing_vercel_auth",
        "VERCEL_TOKEN is required for non-interactive Vercel automation.",
    )
```

Validation contract from 16-RESEARCH.md:
```text
Quick run: python -m unittest tests.test_phase11_vercel_flow -v
Full targeted run: python -m unittest tests.test_phase11_vercel_flow tests.test_project_delivery_pipeline_bootstrap tests.test_project_delivery_pipeline_resume tests.test_phase12_credential_governance -v
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red tests for CLI-session auth reuse, explicit-token continuity, and distinct Vercel failure outcomes</name>
  <files>tests/test_phase11_vercel_flow.py</files>
  <behavior>
    - Test 1: `link_vercel_project()` succeeds without `VERCEL_TOKEN` when the helper can prove a valid local Vercel CLI session.
    - Test 2: `link_vercel_project()` and `deploy_to_vercel()` still honor explicit `VERCEL_TOKEN` for non-interactive automation.
    - Test 3: invalid token/auth, inaccessible scope, missing CLI, and actual deploy execution failure map to separate `block_reason` values instead of generic `missing_vercel_auth` / `vercel_*_failed` collapse.
  </behavior>
  <action>Expand `tests/test_phase11_vercel_flow.py` with failing coverage for VERC-01, VERC-02, and VERC-04. Model the authenticated-local-session path by stubbing a successful Vercel auth probe without `VERCEL_TOKEN`; model the explicit-token path by providing `VERCEL_TOKEN`; and model separate stderr signatures for invalid token/auth, inaccessible scope/team access, and real command failure. Keep the tests at the helper seam only so this plan hardens the shared Vercel helper contract before controller persistence work begins.</action>
  <verify>
    <automated>python -m unittest tests.test_phase11_vercel_flow -v</automated>
  </verify>
  <done>Red tests define the exact helper contract for env-token auth, CLI-session auth, and the required Vercel failure taxonomy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement centralized Vercel auth-source resolution and granular failure classification</name>
  <files>scripts/vercel_delivery_common.py, tests/test_phase11_vercel_flow.py</files>
  <behavior>
    - Test 1: helper returns `auth_source=vercel_token` when `VERCEL_TOKEN` is present and valid.
    - Test 2: helper returns `auth_source=vercel_cli_session` when `VERCEL_TOKEN` is absent but `vercel whoami` (or the chosen auth probe) succeeds.
    - Test 3: helper emits distinct failure reasons such as `invalid_vercel_auth`, `inaccessible_vercel_scope`, `missing_vercel_cli`, and `vercel_deploy_failed` / `vercel_linkage_failed` at the correct boundary.
  </behavior>
  <action>Create a shared auth-resolution helper inside `scripts/vercel_delivery_common.py` that first resolves the Vercel command, then resolves auth source using this order: explicit `VERCEL_TOKEN` first, otherwise a local CLI auth probe using the resolved Vercel command. Return `auth_source` and `auth_source_details` on success so downstream layers can persist evidence later. Update `_require_vercel()`, `link_vercel_project()`, `apply_env_contract()`, and `deploy_to_vercel()` to consume that shared result instead of re-checking `VERCEL_TOKEN`. Classify stderr/output boundaries deterministically: missing CLI remains `missing_vercel_cli`; auth probe or command failures with auth/token/login signatures become `invalid_vercel_auth`; scope/team/project-access failures become `inaccessible_vercel_scope`; command failures after auth/scope resolution remain `vercel_linkage_failed` or `vercel_deploy_failed`. Preserve the existing blocked-result envelope with `ok`, `block_reason`, `error`, and `evidence_path` so the approved-delivery architecture stays intact.</action>
  <verify>
    <automated>python -m unittest tests.test_phase11_vercel_flow -v</automated>
  </verify>
  <done>The Vercel helper layer now supports both approved auth paths and exposes operator-meaningful failure reasons without redesigning orchestration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local environment / CLI session → delivery helper | Machine-local Vercel credentials cross into governed delivery decisions |
| helper result payload → operator evidence JSON | Auth and failure outcomes become persisted operator-visible evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | S | scripts/vercel_delivery_common.py auth-source resolution | mitigate | Resolve auth source explicitly and record `auth_source`/`auth_source_details` so the helper cannot silently pretend token auth when it actually used a local CLI session. |
| T-16-02 | R | Vercel helper evidence files | mitigate | Persist granular `block_reason` and stderr summaries so auth, scope, and deploy failures remain distinguishable to the operator. |
| T-16-03 | D | token-only gate in `_require_vercel()` | mitigate | Replace the token-only gate with tested env-or-CLI auth resolution so healthy local operator auth no longer blocks delivery unnecessarily. |
</threat_model>

<verification>
- `python -m unittest tests.test_phase11_vercel_flow -v`
- Confirm helper evidence payloads include `auth_source` on successful link/deploy fixtures and granular `block_reason` on failure fixtures.
</verification>

<success_criteria>
- VERC-01: the helper can proceed with a proven local Vercel CLI session when `VERCEL_TOKEN` is absent.
- VERC-02: explicit `VERCEL_TOKEN` still works as the first-class non-interactive auth path.
- VERC-04 helper boundary: missing CLI, invalid auth, inaccessible scope, and real execution failure are distinct outcomes.
</success_criteria>

<output>
After completion, create `.planning/phases/16-vercel-auth-and-deploy-reliability/16-01-SUMMARY.md`
</output>
