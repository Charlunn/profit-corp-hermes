---
phase: 16-vercel-auth-and-deploy-reliability
plan: 03
type: execute
wave: 3
depends_on:
  - 16-01
  - 16-02
files_modified:
  - tests/test_phase11_vercel_flow.py
  - tests/test_phase12_credential_governance.py
  - tests/test_project_delivery_pipeline_bootstrap.py
  - tests/test_project_delivery_pipeline_resume.py
  - scripts/approved_delivery_governance.py
autonomous: true
requirements:
  - VERC-01
  - VERC-02
  - VERC-03
  - VERC-04
must_haves:
  truths:
    - "Regression coverage proves the pipeline works for both explicit-token and local-CLI Vercel auth paths."
    - "Governed audit/event artifacts preserve the real Vercel failure boundary and the real linked/deployed metadata."
    - "Bootstrap and resume paths remain coherent after helper and controller hardening, with no regression back to stale metadata or generic failure outcomes."
  artifacts:
    - path: "tests/test_phase11_vercel_flow.py"
      provides: "Final helper regression matrix for local CLI auth, explicit token auth, and Vercel failure-mode separation."
    - path: "tests/test_phase12_credential_governance.py"
      provides: "Governed audit/event assertions for Vercel auth source and failure-boundary visibility."
    - path: "tests/test_project_delivery_pipeline_bootstrap.py"
      provides: "Fresh-run integration coverage for current-run Vercel metadata/evidence persistence."
    - path: "tests/test_project_delivery_pipeline_resume.py"
      provides: "Resume/recovery coverage for Vercel metadata convergence after retry."
    - path: "scripts/approved_delivery_governance.py"
      provides: "Any minimal governed-action compatibility adjustments needed for the new Vercel helper fields without changing the architecture."
  key_links:
    - from: "tests/test_phase12_credential_governance.py"
      to: "scripts/approved_delivery_governance.py"
      via: "audit/event payload assertions for Vercel outcomes"
      pattern: "vercel_project_link|vercel_deploy|block_reason|target"
    - from: "tests/test_project_delivery_pipeline_bootstrap.py"
      to: "scripts/start_approved_project_delivery.py"
      via: "end-to-end current-run Vercel metadata persistence assertions"
      pattern: "shipping\['vercel'\]|project_url|team_scope|deploy_evidence_path"
    - from: "tests/test_project_delivery_pipeline_resume.py"
      to: "scripts/start_approved_project_delivery.py"
      via: "retry/recovery convergence assertions"
      pattern: "resume_from_stage|vercel_linkage|vercel_deploy|shipping\['vercel'\]"
---

<objective>
Close Phase 16 with regression and governance validation that proves helper hardening and controller persistence changes hold across fresh runs, resumes, and operator-visible audit artifacts.

Purpose: Prevent Phase 16 from regressing back to token-only auth, stale Vercel metadata, or coarse blocked outcomes once the implementation lands.
Output: Cross-suite regression coverage plus any minimal governance compatibility updates required for the refined Vercel helper/controller result shapes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-vercel-auth-and-deploy-reliability/16-RESEARCH.md
@.planning/phases/16-vercel-auth-and-deploy-reliability/16-VALIDATION.md
@scripts/approved_delivery_governance.py
@scripts/start_approved_project_delivery.py
@tests/test_phase11_vercel_flow.py
@tests/test_phase12_credential_governance.py
@tests/test_project_delivery_pipeline_bootstrap.py
@tests/test_project_delivery_pipeline_resume.py

<interfaces>
From scripts/approved_delivery_governance.py:
```python
def run_governed_action(*, action, authority_record_path, stage, helper: Helper, **kwargs): ...
def _status_and_outcome(result: dict[str, Any]) -> tuple[str, str]: ...
def _target_for(action: str, record: dict[str, Any], result: dict[str, Any]) -> dict[str, str]: ...
```

From tests/test_phase12_credential_governance.py:
```python
class CredentialGovernanceContractTest(unittest.TestCase):
    def test_persists_blocked_audit_and_append_only_event_linkage(self) -> None: ...
    def test_persists_failure_audit_with_explicit_reason_and_evidence(self) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand the final Vercel helper/controller regression matrix across fresh-run and resume flows</name>
  <files>tests/test_phase11_vercel_flow.py, tests/test_project_delivery_pipeline_bootstrap.py, tests/test_project_delivery_pipeline_resume.py</files>
  <behavior>
    - Test 1: explicit token and local CLI session both pass through the approved helper/controller path without metadata drift.
    - Test 2: successful link/deploy persists actual project/scope/deploy/evidence values across fresh runs and resumed runs.
    - Test 3: invalid auth, inaccessible scope, and deploy failure remain separated end to end instead of collapsing to one generic outcome.
  </behavior>
  <action>Consolidate the final Phase 16 regression matrix across helper, fresh-run bootstrap, and resume coverage. Reuse the proven Phase 14/15 pattern: scenario-oriented tests that prove the real failure signatures stay fixed after refactors. Keep the tests deterministic and command-stubbed; they should verify data contracts and stage progression, not depend on live Vercel access. Ensure the matrix covers both auth sources, current-run metadata persistence, and separated failure categories end to end.</action>
  <verify>
    <automated>python -m unittest tests.test_phase11_vercel_flow tests.test_project_delivery_pipeline_bootstrap tests.test_project_delivery_pipeline_resume -v</automated>
  </verify>
  <done>The core Phase 16 regression matrix now proves auth-path flexibility, authoritative metadata persistence, and failure-boundary separation across fresh and resumed runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock governed audit/event compatibility to the refined Vercel result contract</name>
  <files>tests/test_phase12_credential_governance.py, scripts/approved_delivery_governance.py</files>
  <behavior>
    - Test 1: governed Vercel link/deploy audits retain separated `block_reason` values and the correct target metadata.
    - Test 2: successful governed Vercel actions can carry `auth_source`, `project_url`, `team_scope`, and deploy evidence without dropping the existing audit/event contract.
    - Test 3: `_status_and_outcome()` still treats true missing prerequisites as blocked while invalid auth/scope/deploy failures remain operator-visible failures.
  </behavior>
  <action>Extend `tests/test_phase12_credential_governance.py` so the governed wrapper is pinned to the refined Vercel helper/controller contract from Plans 16-01 and 16-02. If a minimal compatibility update is required in `scripts/approved_delivery_governance.py`, keep it narrow: preserve the allowlist, append-only event model, and current schema while ensuring Vercel target metadata and separated outcomes survive audit/event wrapping. Do not invent a new governance model or reclassify unrelated GitHub behaviors.</action>
  <verify>
    <automated>python -m unittest tests.test_phase12_credential_governance -v</automated>
  </verify>
  <done>Governed audit/event artifacts now preserve the real Phase 16 Vercel outcomes and metadata without architectural drift.</done>
</task>

<task type="auto">
  <name>Task 3: Run the targeted Phase 16 validation suite and close the regression envelope</name>
  <files>tests/test_phase11_vercel_flow.py, tests/test_phase12_credential_governance.py, tests/test_project_delivery_pipeline_bootstrap.py, tests/test_project_delivery_pipeline_resume.py</files>
  <action>Use the Phase 16 validation contract to run the targeted suite after Tasks 1-2 are green. Treat any failing assertion as a contract mismatch in Phase 16 rather than a candidate for scope reduction. The exit condition for this task is a stable targeted suite that proves helper, controller, resume, and governance layers all preserve the repaired Vercel auth/deploy reliability contract.</action>
  <verify>
    <automated>python -m unittest tests.test_phase11_vercel_flow tests.test_project_delivery_pipeline_bootstrap tests.test_project_delivery_pipeline_resume tests.test_phase12_credential_governance -v</automated>
  </verify>
  <done>The targeted Phase 16 regression envelope is green and covers all four VERC requirements through the implemented layers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper/controller outputs → governance audit/events | Delivery outcomes cross into append-only operator evidence |
| fresh-run and resume persistence → later operator actions | Persisted Vercel truth drives subsequent resumes and operator decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | R | scripts/approved_delivery_governance.py and Vercel audit/events | mitigate | Lock separated Vercel outcomes and target metadata into governance regression tests so the audit layer cannot flatten or misreport them. |
| T-16-08 | T | bootstrap/resume Vercel persistence | mitigate | Require fresh-run and resume tests to assert current-run Vercel metadata/evidence replacement, preventing stale-authority reintroduction. |
| T-16-09 | D | Phase 16 regression envelope | mitigate | Run the targeted Phase 16 suite before closeout so helper/controller/governance mismatches are caught immediately rather than surfacing in a later live operator run. |
</threat_model>

<verification>
- `python -m unittest tests.test_phase11_vercel_flow tests.test_project_delivery_pipeline_bootstrap tests.test_project_delivery_pipeline_resume -v`
- `python -m unittest tests.test_phase12_credential_governance -v`
- `python -m unittest tests.test_phase11_vercel_flow tests.test_project_delivery_pipeline_bootstrap tests.test_project_delivery_pipeline_resume tests.test_phase12_credential_governance -v`
</verification>

<success_criteria>
- All Phase 16 requirements have automated regression coverage spanning helper, controller, resume, and governance seams.
- Append-only audit/event artifacts preserve the true Vercel auth, scope, and deploy outcome boundary.
- Fresh runs and resumed runs both retain current-run Vercel metadata/evidence instead of stale values.
</success_criteria>

<output>
After completion, create `.planning/phases/16-vercel-auth-and-deploy-reliability/16-03-SUMMARY.md`
</output>
